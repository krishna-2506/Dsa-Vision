{
  "problem_id": "317",
  "problem_name": "Count partitions with given difference",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 4,
  "substep_name": "DP on Subsequences",
  "difficulty": "Hard",
  "youtube_url": "https://www.youtube.com/watch?v=zoilQD1kYSg",
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/count-partitions-with-given-difference-dp-18/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/count-partitions-with-given-difference",
  "problem_statement": "Problem Statement : Given an array with N positive integers and an integer D, count the number of ways we can partition the given array into two subsets, S1 and S2 such that S1 - S2 = D and S1 is always greater than or equal to S2.",
  "examples": [
    "Input: arr = [1, 1, 2, 3], diff = 1\nOutput: 3\nExplanation: The subsets are [1, 2] and [1, 3], [1, 3] and [1, 2], [1, 1, 2] and [3]. \n\nInput: arr = [1, 2, 3, 4], diff = 2\nOutput: 2\nExplanation: The subsets are [1, 3] and [2, 4], [1, 2, 3] and [4]."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "We need to split the array into two groups (partitions) such that the difference \nbetween their sums is equal to a given number d.\nLet the first group have sum = S1 and the second group have sum = S2.\n\nWe know S1 - S2 = d\nWe also know S1 + S2 = totalSum\n\nIf we add both equations, we get 2 * S1 = d + totalSum.\n\nSo, S1 = (d + totalSum) / 2\nThis means the problem is now reduced to counting subsets of the array \n whose sum is (d + totalSum)/2\n\nThere are some conditions to check before solving:\n\nIf (d + totalSum) is odd, then division by 2 is not possible, thus, there is no valid partitions\nIf d is greater than totalSum, then it is also impossible, thus, there is no valid partitions\n\nNow the task becomes similar to the problem Count Subsets with Sum K.\n\nThe required sum target = (d + totalSum) / 2\nSo the new question is: How many subsets of the array have sum equal to target?\n\nWe already know how to count subsets with a given sum using Dynamic Programming. \nSo we just apply that solution here with target.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to count partitions with given difference\n    int countPartitions(vector<int>& arr, int d) {\n        // Calculate total sum of array\n        int totalSum = accumulate(arr.begin(), arr.end(), 0);\n\n        // Check if solution is possible\n        if ((totalSum + d) % 2 != 0 || d > totalSum) return 0;\n\n        // Calculate target sum\n        int K = (totalSum + d) / 2;\n\n        // Create dp array of size K+1\n        vector<int> dp(K + 1, 0);\n\n        // Base case: empty set makes sum 0\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Process remaining elements\n        for (int i = 1; i < arr.size(); i++) {\n            vector<int> curr(K + 1, 0);\n            curr[0] = 1;\n\n            for (int t = 0; t <= K; t++) {\n                int notTake = dp[t];\n                int take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n                curr[t] = take + notTake;\n            }\n            dp = curr;\n        }\n        return dp[K];\n    }\n};\n\nint main() {\n    Solution sol;\n    vector<int> arr = {1, 2, 3, 4};\n    int d = 1;\n    cout << sol.countPartitions(arr, d) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to count partitions with given difference\n    public int countPartitions(int[] arr, int d) {\n        // Calculate total sum of array\n        int totalSum = 0;\n        for (int num : arr) totalSum += num;\n\n        // Check if solution is possible\n        if ((totalSum + d) % 2 != 0 || d > totalSum) return 0;\n\n        // Calculate target sum\n        int K = (totalSum + d) / 2;\n\n        // Create dp array of size K+1\n        int[] dp = new int[K + 1];\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Process remaining elements\n        for (int i = 1; i < arr.length; i++) {\n            int[] curr = new int[K + 1];\n            curr[0] = 1;\n\n            for (int t = 0; t <= K; t++) {\n                int notTake = dp[t];\n                int take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n                curr[t] = take + notTake;\n            }\n            dp = curr;\n        }\n        return dp[K];\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] arr = {1, 2, 3, 4};\n        int d = 1;\n        System.out.println(sol.countPartitions(arr, d));\n    }\n}",
        "python": "class Solution:\n    # Function to count partitions with given difference\n    def countPartitions(self, arr, d):\n        # Calculate total sum of array\n        totalSum = sum(arr)\n\n        # Check if solution is possible\n        if (totalSum + d) % 2 != 0 or d > totalSum:\n            return 0\n\n        # Calculate target sum\n        K = (totalSum + d) // 2\n\n        # Create dp array of size K+1\n        dp = [0] * (K + 1)\n        dp[0] = 1\n\n        # If first element <= K, mark it\n        if arr[0] <= K:\n            dp[arr[0]] += 1\n\n        # Process remaining elements\n        for i in range(1, len(arr)):\n            curr = [0] * (K + 1)\n            curr[0] = 1\n            for t in range(K + 1):\n                notTake = dp[t]\n                take = 0\n                if arr[i] <= t:\n                    take = dp[t - arr[i]]\n                curr[t] = take + notTake\n            dp = curr\n\n        return dp[K]\n\n\nif __name__ == \"__main__\":\n    sol = Solution()\n    arr = [1, 2, 3, 4]\n    d = 1\n    print(sol.countPartitions(arr, d))",
        "javascript": "class Solution {\n    // Function to count partitions with given difference\n    countPartitions(arr, d) {\n        // Calculate total sum of array\n        let totalSum = arr.reduce((a, b) => a + b, 0);\n\n        // Check if solution is possible\n        if ((totalSum + d) % 2 !== 0 || d > totalSum) return 0;\n\n        // Calculate target sum\n        let K = (totalSum + d) / 2;\n\n        // Create dp array of size K+1\n        let dp = new Array(K + 1).fill(0);\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Process remaining elements\n        for (let i = 1; i < arr.length; i++) {\n            let curr = new Array(K + 1).fill(0);\n            curr[0] = 1;\n\n            for (let t = 0; t <= K; t++) {\n                let notTake = dp[t];\n                let take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n                curr[t] = take + notTake;\n            }\n            dp = curr;\n        }\n\n        return dp[K];\n    }\n}\n\n// Driver code\nlet sol = new Solution();\nlet arr = [1, 2, 3, 4];\nlet d = 1;\nconsole.log(sol.countPartitions(arr, d));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:23Z"
}