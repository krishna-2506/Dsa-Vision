{
  "problem_id": "568",
  "problem_name": "Maximum Sum Combination",
  "step_no": 11,
  "step_name": "Heaps [Learning, Medium, Hard Problems]",
  "substep_no": 3,
  "substep_name": "Hard Problems",
  "difficulty": "Hard",
  "youtube_url": null,
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/maximum-sum-combination",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/maximum-sum-combination",
  "problem_statement": "Given two integer arrays nums1 and nums2 and an integer k, return the maximum k valid sum combinations from all possible sum combinations using the elements of nums1 and nums2.\n\nA valid sum combination is made by adding one element from nums1 and one element from nums2. Return the answer in non-increasing order.",
  "examples": [
    "Input : nums1 = [7, 3], nums2 = [1, 6], k = 2\n Output : [13, 9]\n Explanation : The 2 maximum combinations are made by: nums1[0] + nums2[1] = 13 nums1[1] + nums2[1] = 9 \n\n Input : nums1 = [3, 4, 5], nums2 = [2, 6, 3], k = 2 \nOutput : [11, 10] \nExplanation : The 2 maximum combinations are made by: nums1[2] + nums2[1] = 11 nums1[1] + nums2[1] = 10"
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Brute force Approach",
      "algorithm": "We are given two integer arrays and asked to find the top k largest pairwise sums, where each pair is formed by picking one element from each array.\n\nIn the brute force approach, we can simply\n\ngenerate all possible pair sums.\n\nThen sort the resulting list in descending order.\n\nFinally, return the first k elements.\n\nThis approach is easy to implement but very inefficient when both arrays are large.\n\n Initialize an empty list to store pairwise sums. Iterate over every element in the first array. For each element in the first array, iterate over every element in the second array. For each pair, calculate the sum and store it in the list. After generating all combinations, sort the list in descending order. Return the first k elements from the sorted list.",
      "time_complexity": "O(n * m + nmlog(nm)",
      "space_complexity": "O(n * m + nmlog(nm)",
      "time_complexity_details": "Time Complexity: O(n * m + nmlog(nm)) ,We compute all n * m pairwise sums. Sorting takes O(nm log nm). Extracting top k is O(k).\nSpace Complexity:O(n * m) , We store all pairwise sums explicitly before sorting and slicing.",
      "space_complexity_details": "Time Complexity: O(n * m + nmlog(nm)) ,We compute all n * m pairwise sums. Sorting takes O(nm log nm). Extracting top k is O(k).\nSpace Complexity:O(n * m) , We store all pairwise sums explicitly before sorting and slicing.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\nclass Solution {\npublic:\n    // Function to return the k largest valid sum combinations\n    vector<int> maxCombinations(vector<int>& nums1, vector<int>& nums2, int k) {\n\n        // List to store all possible pair sums\n        vector<int> allSums;\n\n        // Loop over every element in nums1\n        for (int i = 0; i < nums1.size(); i++) {\n\n            // Loop over every element in nums2\n            for (int j = 0; j < nums2.size(); j++) {\n\n                // Compute the pair sum and store it\n                allSums.push_back(nums1[i] + nums2[j]);\n            }\n        }\n\n        // Sort all sums in descending order\n        sort(allSums.begin(), allSums.end(), greater<int>());\n\n        // Return first k elements from the sorted list\n        vector<int> result(allSums.begin(), allSums.begin() + k);\n        return result;\n    }\n};\n\n// Driver code\nint main() {\n    Solution obj;\n    vector<int> nums1 = {7, 3};\n    vector<int> nums2 = {1, 6};\n    int k = 2;\n    vector<int> result = obj.maxCombinations(nums1, nums2, k);\n\n    for (int val : result) {\n        cout << val << \" \";\n    }\n    cout << endl;\n    return 0;\n}",
        "java": "import java.util.*;\nclass Solution {\n    // Function to return k largest valid sum combinations\n    public List<Integer> maxCombinations(int[] nums1, int[] nums2, int k) {\n\n        // List to store all possible pair sums\n        List<Integer> allSums = new ArrayList<>();\n\n        // Iterate through each element in nums1\n        for (int i = 0; i < nums1.length; i++) {\n\n            // Iterate through each element in nums2\n            for (int j = 0; j < nums2.length; j++) {\n\n                // Store the sum of the pair\n                allSums.add(nums1[i] + nums2[j]);\n            }\n        }\n\n        // Sort all sums in descending order\n        allSums.sort(Collections.reverseOrder());\n\n        // Return the first k sums\n        return allSums.subList(0, k);\n    }\n}\n\n// Driver class\npublic class Main {\n    public static void main(String[] args) {\n        Solution obj = new Solution();\n        int[] nums1 = {7, 3};\n        int[] nums2 = {1, 6};\n        int k = 2;\n\n        List<Integer> result = obj.maxCombinations(nums1, nums2, k);\n        for (int val : result) {\n            System.out.print(val + \" \");\n        }\n        System.out.println();\n    }\n}",
        "python": "class Solution:\n\n    # Function to return k largest valid sum combinations\n    def maxCombinations(self, nums1, nums2, k):\n\n        # List to store all possible pair sums\n        all_sums = []\n\n        # Iterate through each element in nums1\n        for i in range(len(nums1)):\n\n            # Iterate through each element in nums2\n            for j in range(len(nums2)):\n\n                # Add the sum of the pair to the list\n                all_sums.append(nums1[i] + nums2[j])\n\n        # Sort the list in descending order\n        all_sums.sort(reverse=True)\n\n        # Return the first k elements\n        return all_sums[:k]\n\n# Driver code\nnums1 = [7, 3]\nnums2 = [1, 6]\nk = 2\nobj = Solution()\nprint(obj.maxCombinations(nums1, nums2, k))  # Output: [13, 9]",
        "javascript": "class Solution {\n\n    // Function to return k largest valid sum combinations\n    maxCombinations(nums1, nums2, k) {\n\n        // Array to store all pair sums\n        let allSums = [];\n\n        // Iterate through each element in nums1\n        for (let i = 0; i < nums1.length; i++) {\n\n            // Iterate through each element in nums2\n            for (let j = 0; j < nums2.length; j++) {\n\n                // Push the sum of the pair into allSums\n                allSums.push(nums1[i] + nums2[j]);\n            }\n        }\n\n        // Sort in descending order\n        allSums.sort((a, b) => b - a);\n\n        // Return first k elements\n        return allSums.slice(0, k);\n    }\n}\n\n// Driver code\nlet nums1 = [7, 3];\nlet nums2 = [1, 6];\nlet k = 2;\nlet obj = new Solution();\nconsole.log(obj.maxCombinations(nums1, nums2, k));  // Output: [13, 9]"
      }
    },
    {
      "approach_name": "Optimal Approach",
      "algorithm": "Instead of generating all combinations, we can only focus on the largest possible sums using a Max Heap. We begin with the sum of the largest elements in both arrays and then explore their neighboring sums using indices. To prevent duplicate work, we use a set to keep track of which index pairs we've already added to the heap.\n\nThis method simulates a best-first search where we always explore the next largest unexplored sum.\n\n Sort both input arrays in descending order. Use a max heap to store tuples of (sum, index1, index2). Push the initial max sum formed by the first elements of both arrays into the heap. Maintain a set to track visited index pairs. Repeat the following k times: Pop the current largest sum from the heap. Add it to the result. Push the next two possible combinations (moving one index forward in either array) into the heap if not visited. Return the result list after collecting k sums.",
      "time_complexity": "O(k * log k)",
      "space_complexity": "O(k * log k)",
      "time_complexity_details": "Time Complexity: O(k * log k),The loop runs k times, and in each iteration, we perform heap insertions and deletions, which cost O(log k). In total, this results in O(k log k) time.\nSpace Complexity: O(k),\nThe heap and visited set can grow to size k in the worst case.",
      "space_complexity_details": "Time Complexity: O(k * log k),The loop runs k times, and in each iteration, we perform heap insertions and deletions, which cost O(log k). In total, this results in O(k log k) time.\nSpace Complexity: O(k),\nThe heap and visited set can grow to size k in the worst case.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find k maximum sum combinations\n    vector<int> maxCombinations(vector<int>& nums1, vector<int>& nums2, int k) {\n\n        // Sort both arrays in descending order\n        sort(nums1.begin(), nums1.end(), greater<int>());\n        sort(nums2.begin(), nums2.end(), greater<int>());\n\n        // Max-heap to store pairs with their indices\n        priority_queue<tuple<int, int, int>> maxHeap;\n\n        // Set to keep track of visited index pairs\n        set<pair<int, int>> visited;\n\n        // Push the initial maximum pair (nums1[0] + nums2[0])\n        maxHeap.push({nums1[0] + nums2[0], 0, 0});\n        visited.insert({0, 0});\n\n        // Vector to store the result\n        vector<int> result;\n\n        // Extract top k elements from the heap\n        while(k-- && !maxHeap.empty()) {\n\n            // Get the current maximum sum and its indices\n            auto [sum, i, j] = maxHeap.top();\n            maxHeap.pop();\n\n            // Add this sum to the result\n            result.push_back(sum);\n\n            // If (i + 1, j) is valid and not visited, add it to the heap\n            if(i + 1 < nums1.size() && !visited.count({i + 1, j})) {\n                maxHeap.push({nums1[i + 1] + nums2[j], i + 1, j});\n                visited.insert({i + 1, j});\n            }\n\n            // If (i, j + 1) is valid and not visited, add it to the heap\n            if(j + 1 < nums2.size() && !visited.count({i, j + 1})) {\n                maxHeap.push({nums1[i] + nums2[j + 1], i, j + 1});\n                visited.insert({i, j + 1});\n            }\n        }\n\n        // Return the final k max combinations\n        return result;\n    }\n};\n\n// Driver code\nint main() {\n    Solution sol;\n    vector<int> nums1 = {7, 3};\n    vector<int> nums2 = {1, 6};\n    int k = 2;\n    vector<int> result = sol.maxCombinations(nums1, nums2, k);\n    for(int val : result) cout << val << \" \";\n    return 0;\n}",
        "java": "import java.util.*;\n\n// Class that implements the solution\nclass Solution {\n    // Function to find k maximum sum combinations\n    public List<Integer> maxCombinations(int[] nums1, int[] nums2, int k) {\n        // Sort both arrays in descending order\n        Arrays.sort(nums1);\n        Arrays.sort(nums2);\n        int n = nums1.length;\n\n        // Max heap to store sums and their indices\n        PriorityQueue<int[]> maxHeap = new PriorityQueue<>((a, b) -> b[0] - a[0]);\n\n        // Set to keep track of visited index pairs\n        Set<String> visited = new HashSet<>();\n\n        // Push initial max sum combination\n        maxHeap.offer(new int[]{nums1[n - 1] + nums2[n - 1], n - 1, n - 1});\n        visited.add((n - 1) + \",\" + (n - 1));\n\n        // Result list\n        List<Integer> result = new ArrayList<>();\n\n        // Extract top k combinations\n        while (k-- > 0 && !maxHeap.isEmpty()) {\n            int[] current = maxHeap.poll();\n            int sum = current[0], i = current[1], j = current[2];\n\n            result.add(sum);\n\n            // Check for new combination (i - 1, j)\n            if (i - 1 >= 0) {\n                String key1 = (i - 1) + \",\" + j;\n                if (!visited.contains(key1)) {\n                    maxHeap.offer(new int[]{nums1[i - 1] + nums2[j], i - 1, j});\n                    visited.add(key1);\n                }\n            }\n\n            // Check for new combination (i, j - 1)\n            if (j - 1 >= 0) {\n                String key2 = i + \",\" + (j - 1);\n                if (!visited.contains(key2)) {\n                    maxHeap.offer(new int[]{nums1[i] + nums2[j - 1], i, j - 1});\n                    visited.add(key2);\n                }\n            }\n        }\n\n        return result;\n    }\n}\n\n// Driver code in a separate class\nclass Main {\n    public static void main(String[] args) {\n        Solution obj = new Solution();\n        int[] nums1 = {7, 3};\n        int[] nums2 = {1, 6};\n        int k = 2;\n        System.out.println(obj.maxCombinations(nums1, nums2, k)); // Output: [13, 9]\n    }\n}",
        "python": "import heapq\n\nclass Solution:\n    # Function to find k maximum sum combinations\n    def maxCombinations(self, nums1, nums2, k):\n        # Sort both arrays in descending order\n        nums1.sort(reverse=True)\n        nums2.sort(reverse=True)\n\n        # Initialize max-heap with negative sums (since Python has min-heap)\n        max_heap = [(-(nums1[0] + nums2[0]), 0, 0)]\n\n        # Set to keep track of visited index pairs\n        visited = set()\n        visited.add((0, 0))\n\n        # Result list to store top k combinations\n        result = []\n\n        # Run loop k times\n        for _ in range(k):\n            # Extract max sum combination\n            sum_neg, i, j = heapq.heappop(max_heap)\n\n            # Convert sum back to positive\n            result.append(-sum_neg)\n\n            # Push next element from nums1 if not visited\n            if i + 1 < len(nums1) and (i + 1, j) not in visited:\n                heapq.heappush(max_heap, (-(nums1[i + 1] + nums2[j]), i + 1, j))\n                visited.add((i + 1, j))\n\n            # Push next element from nums2 if not visited\n            if j + 1 < len(nums2) and (i, j + 1) not in visited:\n                heapq.heappush(max_heap, (-(nums1[i] + nums2[j + 1]), i, j + 1))\n                visited.add((i, j + 1))\n\n        return result\n\n# Driver code\nobj = Solution()\nnums1 = [7, 3]\nnums2 = [1, 6]\nk = 2\nprint(obj.maxCombinations(nums1, nums2, k))  # Output: [13, 9]",
        "javascript": "class Solution {\n    // Function to find k maximum sum combinations from two arrays\n    maxCombinations(nums1, nums2, k) {\n        // Sort both arrays descending to prioritize largest values\n        nums1.sort((a, b) => b - a);\n        nums2.sort((a, b) => b - a);\n\n        // Use an array to simulate max-heap behavior\n        const heap = [];\n        // Set to track visited index pairs so we avoid duplicates\n        const visited = new Set();\n\n        // Push the initial best combination (0,0)\n        heap.push([nums1[0] + nums2[0], 0, 0]);\n        visited.add(`0,0`);\n\n        // Array to store results\n        const result = [];\n\n        // Loop k times to get top k sums\n        for (let count = 0; count < k; count++) {\n            // Sort heap descending by sum and pop the max each time\n            heap.sort((a, b) => b[0] - a[0]);\n            const [sum, i, j] = heap.shift();\n            result.push(sum);\n\n            // Next candidate (i+1, j)\n            if (i + 1 < nums1.length) {\n                const key1 = `${i + 1},${j}`;\n                if (!visited.has(key1)) {\n                    heap.push([nums1[i + 1] + nums2[j], i + 1, j]);\n                    visited.add(key1);\n                }\n            }\n\n            // Next candidate (i, j+1)\n            if (j + 1 < nums2.length) {\n                const key2 = `${i},${j + 1}`;\n                if (!visited.has(key2)) {\n                    heap.push([nums1[i] + nums2[j + 1], i, j + 1]);\n                    visited.add(key2);\n                }\n            }\n        }\n\n        // Return the final top k sums\n        return result;\n    }\n}\n\n// Driver code\nconst sol = new Solution();\nconst nums1 = [7, 3];\nconst nums2 = [1, 6];\nconst k = 2;\nconsole.log(sol.maxCombinations(nums1, nums2, k)); // Expected: [13, 9]"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:13Z"
}