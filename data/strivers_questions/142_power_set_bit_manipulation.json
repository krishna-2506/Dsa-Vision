{
  "problem_id": "142",
  "problem_name": "Power Set Bit Manipulation",
  "step_no": 8,
  "step_name": "Bit Manipulation [Concepts & Problems]",
  "substep_no": 2,
  "substep_name": "Interview Problems",
  "difficulty": "Medium",
  "youtube_url": "https://youtu.be/LqKaUv1G3_I?si=UXU_T5OsHiokPRvP",
  "leetcode_url": "https://leetcode.com/problems/subsets/",
  "article_url": "https://takeuforward.org/bit-manipulation/power-set-bit-manipulation",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/power-set-bit-manipulation",
  "problem_statement": "Given an array of numbers, print all subsets of it using bitwise operators.",
  "examples": [
    "Input: nums = [1, 2, 3]\nOutput: [[ ], [1], [2], [3], [1, 2], [2, 3], [3, 1], [1, 2, 3]]\nExplanation: A power set of an array is an array of all possible subarrays, including an empty array. It contains all combinations of elements from the original array. By iterating through all possible combinations of the elements in the input array we are able to get the power set of the array.\nInput: nums = [5, 7, 8]\nOutput: [[ ], [5], [7], [8], [5, 7], [7, 8], [8, 5], [5, 7, 8]]\nExplanation: A power set of an array is an array of all possible subarrays, including an empty array. It contains all combinations of elements from the original array. By iterating through all possible combinations of the elements in the input array we are able to get the power set of the array."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "This problem has already been previously solved using Recursion here:Power Set: Recursion.\n To solve it using bit wise operators, we observe a pattern that the number of subsets is dependant on the size of the input array as:\nN = 1, No. of subsets = 2N = 2, No. of subsets = 4N = 3, No. of subsets = 8 and so on…\nNo. of subsets of input array of size N = 2N = [1 << n]\n\n Hence we can associate each subset with a number in the range 0 to 2N-1. Each of these numbers represents a bitmask where each bit indicates whether the corresponding element from the input array is included in the current subset or not.\n\nCreate an empty array to hold all the subsets of the input array.\nDetermine the size of the power set, which is 2N, where N represents the number of elements in the input array.\nIterate through all possible numbers from 0 to 2N-1: Each number represents a unique combination of elements in the input array.\nFor each number, generate the corresponding subset.\nAfter constructing each subset, add it to the array of subsets.\nReturn the array containing all the subsets of the input array.",
      "time_complexity": "O(N x 2N)",
      "space_complexity": "O(N x 2N)",
      "time_complexity_details": "Time Complexity: O(N x 2N)\n where N is the number of elements in the input array.\n Iterating through all possible numbers from 0 to 2N-1 where N is the number of elements in the input array requires O(2N) iterations.For each iteration, we perform O(N) operations to construct the corresponding subset by interpreting the bits of the number.\nSpace Complexity: O(N x 2N) \n where N is the number of elements in the input array. We store all subsets in a list. Since there are 2N subsets in the power set, each subset can have at most N elements.",
      "space_complexity_details": "Time Complexity: O(N x 2N)\n where N is the number of elements in the input array.\n Iterating through all possible numbers from 0 to 2N-1 where N is the number of elements in the input array requires O(2N) iterations.For each iteration, we perform O(N) operations to construct the corresponding subset by interpreting the bits of the number.\nSpace Complexity: O(N x 2N) \n where N is the number of elements in the input array. We store all subsets in a list. Since there are 2N subsets in the power set, each subset can have at most N elements.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to generate all subsets (the power set) of the input array\n    vector<vector<int>> getPowerSet(vector<int>& nums) {\n        // Get the size of the input array\n        int n = nums.size();\n\n        // Calculate the total number of subsets (2^n) using bitwise shift\n        int subsets = 1 << n;\n\n        // Vector to store all subsets\n        vector<vector<int>> ans;\n\n        // Iterate through all numbers from 0 to 2^n - 1\n        for (int num = 0; num < subsets; num++) {\n            // Temporary vector to hold the current subset\n            vector<int> subset;\n\n            // Iterate through each bit of the number\n            for (int i = 0; i < n; i++) {\n                // If the ith bit is set, include nums[i] in the subset\n                if (num & (1 << i)) {\n                    subset.push_back(nums[i]);\n                }\n            }\n\n            // Add the constructed subset into the result\n            ans.push_back(subset);\n        }\n\n        // Return all subsets\n        return ans;\n    }\n};\n\n// Driver code\nint main() {\n    // Input array\n    vector<int> nums = {5, 7, 8};\n\n    // Create object of Solution\n    Solution obj;\n\n    // Call function to generate subsets\n    vector<vector<int>> subsets = obj.getPowerSet(nums);\n\n    // Print original input array\n    cout << \"Initial Input Array: \";\n    for (auto num : nums) {\n        cout << num << \" \";\n    }\n    cout << endl;\n\n    // Print all subsets\n    cout << \"Subsets: \" << endl;\n    for (auto subset : subsets) {\n        cout << \"[ \";\n        for (auto num : subset) {\n            cout << num << \" \";\n        }\n        cout << \"]\" << endl;\n    }\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to generate all subsets (power set) of the input array\n    public List<List<Integer>> getPowerSet(int[] nums) {\n        // Get the size of the input array\n        int n = nums.length;\n\n        // Calculate the total number of subsets (2^n)\n        int subsets = 1 << n;\n\n        // List to store all subsets\n        List<List<Integer>> ans = new ArrayList<>();\n\n        // Iterate through all numbers from 0 to 2^n - 1\n        for (int num = 0; num < subsets; num++) {\n            // Temporary list to hold current subset\n            List<Integer> subset = new ArrayList<>();\n\n            // Iterate through each bit of the number\n            for (int i = 0; i < n; i++) {\n                // If ith bit is set, include nums[i] in the subset\n                if ((num & (1 << i)) != 0) {\n                    subset.add(nums[i]);\n                }\n            }\n\n            // Add this subset into the final result\n            ans.add(subset);\n        }\n\n        // Return all subsets\n        return ans;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Input array\n        int[] nums = {5, 7, 8};\n\n        // Create object of Solution\n        Solution obj = new Solution();\n\n        // Generate power set\n        List<List<Integer>> subsets = obj.getPowerSet(nums);\n\n        // Print input array\n        System.out.print(\"Initial Input Array: \");\n        for (int num : nums) {\n            System.out.print(num + \" \");\n        }\n        System.out.println();\n\n        // Print subsets\n        System.out.println(\"Subsets:\");\n        for (List<Integer> subset : subsets) {\n            System.out.print(\"[ \");\n            for (int num : subset) {\n                System.out.print(num + \" \");\n            }\n            System.out.println(\"]\");\n        }\n    }\n}",
        "python": "class Solution:\n    # Function to generate all subsets (the power set) of the input list\n    def getPowerSet(self, nums):\n        # Get the size of the input list\n        n = len(nums)\n\n        # Calculate total number of subsets (2^n)\n        subsets = 1 << n\n\n        # List to store all subsets\n        ans = []\n\n        # Iterate through all numbers from 0 to 2^n - 1\n        for num in range(subsets):\n            # Temporary list for current subset\n            subset = []\n\n            # Check each bit of the number\n            for i in range(n):\n                # If ith bit is set, include nums[i] in the subset\n                if num & (1 << i):\n                    subset.append(nums[i])\n\n            # Add this subset to the result\n            ans.append(subset)\n\n        # Return all subsets\n        return ans\n\n\n# Driver code\nif __name__ == \"__main__\":\n    # Input array\n    nums = [5, 7, 8]\n\n    # Create object of Solution\n    obj = Solution()\n\n    # Generate power set\n    subsets = obj.getPowerSet(nums)\n\n    # Print input array\n    print(\"Initial Input Array:\", nums)\n\n    # Print subsets\n    print(\"Subsets:\")\n    for subset in subsets:\n        print(\"[\", \" \".join(map(str, subset)), \"]\")",
        "javascript": "class Solution {\n    // Function to generate all subsets (power set) of the input array\n    getPowerSet(nums) {\n        // Get the size of the input array\n        const n = nums.length;\n\n        // Calculate total number of subsets (2^n)\n        const subsets = 1 << n;\n\n        // Array to store all subsets\n        const ans = [];\n\n        // Iterate through all numbers from 0 to 2^n - 1\n        for (let num = 0; num < subsets; num++) {\n            // Temporary array to hold current subset\n            const subset = [];\n\n            // Check each bit of the number\n            for (let i = 0; i < n; i++) {\n                // If ith bit is set, include nums[i]\n                if (num & (1 << i)) {\n                    subset.push(nums[i]);\n                }\n            }\n\n            // Add this subset to the result\n            ans.push(subset);\n        }\n\n        // Return all subsets\n        return ans;\n    }\n}\n\n// Driver code\nfunction main() {\n    // Input array\n    const nums = [5, 7, 8];\n\n    // Create object of Solution\n    const obj = new Solution();\n\n    // Generate power set\n    const subsets = obj.getPowerSet(nums);\n\n    // Print input array\n    console.log(\"Initial Input Array:\", nums);\n\n    // Print subsets\n    console.log(\"Subsets:\");\n    for (const subset of subsets) {\n        console.log(\"[ \" + subset.join(\" \") + \" ]\");\n    }\n}\n\nmain();"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:06Z"
}