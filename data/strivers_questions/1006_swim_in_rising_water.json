{
  "problem_id": "1006",
  "problem_name": "Swim in Rising Water",
  "step_no": 15,
  "step_name": "Graphs [Concepts & Problems]",
  "substep_no": 5,
  "substep_name": "MinimumSpanningTree/Disjoint Set and Problems",
  "difficulty": "Medium",
  "youtube_url": null,
  "leetcode_url": "https://leetcode.com/problems/swim-in-rising-water/",
  "article_url": "https://takeuforward.org/data-structure/swim-in-rising-water",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/swim-in-rising-water",
  "problem_statement": "You are given an n × n matrix grid where grid[i][j] is the unique elevation of cell (i, j).\nRain starts falling at time t = 0.\nAt any later time t ≥ 0, every cell is covered by water to depth t.\nYou may move 4-directionally (up, down, left, right) between adjacent cells instantaneously iff the elevations of both cells are ≤ t.\nStarting from the top-left cell (0, 0), return the minimum time t at which you can reach the bottom-right cell (n − 1, n − 1).",
  "examples": [
    "Input: grid = [[0,2],[1,3]]\nOutput: 3\nExplanation: \nt = 0, only (0,0) is reachable. \nt = 1, cells {0,0},{1,0} reachable, but (1,1) still blocked (elevation 3). \nt = 2, cells {0,0},{0,1},{1,0} reachable. \nt = 3, all cells ≤ 3 are flooded, so we can swim to (1,1). Minimum t = 3.\n\nInput: grid = [[ 0, 1, 2, 3, 4],[24,23,22,21, 5],[12,13,14,15,16],[11,17,18,19,20],[10, 9, 8, 7, 6]]\nOutput: 16\nExplanation: The earliest moment the start and end cells belong to the same flooded component is t = 16."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "We want to reach the bottom-right cell while the water level keeps rising.\nAt any moment, we can only step into a cell if the water has risen above that cell’s elevation. Therefore, instead of waiting at each step, we check, what’s the lowest elevation we can move to next.\nTo always move through the least risky path, we simulate the rising water using a min-heap, where we always expand the lowest elevation cell available to us. The maximum elevation on the chosen path determines the earliest time we can reach the end.\n \nStart from the top-left cell of the grid. At each step, choose the cell with the lowest elevation among all reachable, unvisited cells.\nAs you move, keep track of the highest elevation you've had to cross so far.\nThe goal is to reach the bottom-right cell while keeping this highest elevation as low as possible.\nAlways expand your path from the lowest available elevation to simulate the rising water level.\nOnce you reach the destination, the highest elevation encountered on that path is the minimum time needed to swim across.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Minimum time to reach bottom right cell\n    int swimInWater(vector<vector<int>>& grid) {\n        // Get grid size\n        int n = grid.size();\n\n        // Create a min-heap for cells based on elevation\n        priority_queue<vector<int>, vector<vector<int>>, greater<vector<int>>> minHeap;\n\n        // Create visited matrix\n        vector<vector<int>> visited(n, vector<int>(n, 0));\n\n        // Push starting cell to heap\n        minHeap.push({grid[0][0], 0, 0});\n        visited[0][0] = 1;\n\n        // Direction vectors for movement\n        vector<pair<int, int>> dirs = {{0,1}, {1,0}, {0,-1}, {-1,0}};\n\n        // Process heap until destination is reached\n        while (!minHeap.empty()) {\n            // Extract cell with least elevation\n            auto curr = minHeap.top(); minHeap.pop();\n            int elevation = curr[0], r = curr[1], c = curr[2];\n\n            // If destination reached, return elevation\n            if (r == n - 1 && c == n - 1) return elevation;\n\n            // Explore neighboring cells\n            for (auto& dir : dirs) {\n                int nr = r + dir.first;\n                int nc = c + dir.second;\n\n                // Check bounds and if not visited\n                if (nr >= 0 && nc >= 0 && nr < n && nc < n && !visited[nr][nc]) {\n                    visited[nr][nc] = 1;\n                    minHeap.push({max(elevation, grid[nr][nc]), nr, nc});\n                }\n            }\n        }\n        return -1;\n    }\n};\n\nint main() {\n    vector<vector<int>> grid = {\n        {0,2}, {1,3}\n    };\n    Solution sol;\n    cout << \"Minimum time to reach destination: \" << sol.swimInWater(grid) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Minimum time to reach bottom right cell\n    public int swimInWater(int[][] grid) {\n        // Get grid size\n        int n = grid.length;\n\n        // Create min-heap for cells by elevation\n        PriorityQueue<int[]> minHeap = new PriorityQueue<>((a, b) -> a[0] - b[0]);\n\n        // Visited matrix\n        boolean[][] visited = new boolean[n][n];\n\n        // Push starting cell to heap\n        minHeap.add(new int[]{grid[0][0], 0, 0});\n        visited[0][0] = true;\n\n        // Direction vectors\n        int[][] dirs = {{0,1},{1,0},{0,-1},{-1,0}};\n\n        // Process heap until destination\n        while (!minHeap.isEmpty()) {\n            // Get cell with lowest elevation\n            int[] curr = minHeap.poll();\n            int elevation = curr[0], r = curr[1], c = curr[2];\n\n            // If destination reached\n            if (r == n - 1 && c == n - 1) return elevation;\n\n            // Check all neighbors\n            for (int[] d : dirs) {\n                int nr = r + d[0], nc = c + d[1];\n\n                // Check bounds and visited\n                if (nr >= 0 && nc >= 0 && nr < n && nc < n && !visited[nr][nc]) {\n                    visited[nr][nc] = true;\n                    minHeap.add(new int[]{Math.max(elevation, grid[nr][nc]), nr, nc});\n                }\n            }\n        }\n        return -1;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[][] grid = {\n            {0, 2},\n            {1, 3}\n        };\n        Solution sol = new Solution();\n        System.out.println(\"Minimum time to reach destination: \" + sol.swimInWater(grid));\n    }\n}",
        "python": "import heapq\n\nclass Solution:\n    # Minimum time to reach bottom right cell\n    def swimInWater(self, grid):\n        # Get grid size\n        n = len(grid)\n\n        # Initialize min-heap with starting cell\n        heap = [(grid[0][0], 0, 0)]\n\n        # Visited matrix\n        visited = [[False] * n for _ in range(n)]\n        visited[0][0] = True\n\n        # Direction vectors\n        dirs = [(0,1), (1,0), (0,-1), (-1,0)]\n\n        # Process heap\n        while heap:\n            # Pop cell with lowest elevation\n            elevation, r, c = heapq.heappop(heap)\n\n            # If destination reached\n            if r == n - 1 and c == n - 1:\n                return elevation\n\n            # Explore neighbors\n            for dr, dc in dirs:\n                nr, nc = r + dr, c + dc\n\n                # Check bounds and if unvisited\n                if 0 <= nr < n and 0 <= nc < n and not visited[nr][nc]:\n                    visited[nr][nc] = True\n                    heapq.heappush(heap, (max(elevation, grid[nr][nc]), nr, nc))\n        return -1\n\n# Driver code\nif __name__ == \"__main__\":\n    grid = [\n        [0, 2],\n        [1, 3]\n    ]\n    sol = Solution()\n    print(\"Minimum time to reach destination:\", sol.swimInWater(grid))",
        "javascript": "class Solution {\n    // Minimum time to reach bottom right cell\n    swimInWater(grid) {\n        // Get grid size\n        const n = grid.length;\n\n        // Min-heap using priority queue logic\n        const heap = [[grid[0][0], 0, 0]];\n\n        // Visited matrix\n        const visited = Array.from({ length: n }, () => Array(n).fill(false));\n        visited[0][0] = true;\n\n        // Direction vectors\n        const dirs = [[0,1], [1,0], [0,-1], [-1,0]];\n\n        // Custom min-heap sort function\n        const sortHeap = () => heap.sort((a, b) => a[0] - b[0]);\n\n        // Process heap\n        while (heap.length > 0) {\n            // Sort and pop lowest elevation\n            sortHeap();\n            const [elevation, r, c] = heap.shift();\n\n            // Check if destination\n            if (r === n - 1 && c === n - 1) return elevation;\n\n            // Explore neighbors\n            for (const [dr, dc] of dirs) {\n                const nr = r + dr, nc = c + dc;\n\n                // Check bounds and visited\n                if (nr >= 0 && nc >= 0 && nr < n && nc < n && !visited[nr][nc]) {\n                    visited[nr][nc] = true;\n                    heap.push([Math.max(elevation, grid[nr][nc]), nr, nc]);\n                }\n            }\n        }\n\n        return -1;\n    }\n}\n\n// Driver code\nconst grid = [\n    [0, 2],\n    [1, 3]\n];\nconst sol = new Solution();\nconsole.log(\"Minimum time to reach destination:\", sol.swimInWater(grid));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:21Z"
}