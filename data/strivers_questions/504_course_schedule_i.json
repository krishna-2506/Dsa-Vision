{
  "problem_id": "504",
  "problem_name": "Course Schedule I",
  "step_no": 15,
  "step_name": "Graphs [Concepts & Problems]",
  "substep_no": 3,
  "substep_name": "Topo Sort and Problems",
  "difficulty": "Hard",
  "youtube_url": "https://youtu.be/WAOfKpxYHR8",
  "leetcode_url": "https://leetcode.com/problems/course-schedule/",
  "article_url": "https://takeuforward.org/data-structure/course-schedule-i-and-ii-pre-requisite-tasks-topological-sort-g-24/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/course-schedule-i",
  "problem_statement": "Problem Statement I (Course Schedule): There are a total of n tasks you have to pick, labeled from 0 to n-1. Some tasks may have prerequisites tasks, for example, to pick task 0 you have to first finish tasks 1, which is expressed as a pair: [0, 1]",
  "examples": [
    "Input: N = 4, P = 3, array[] = {{1,0},{2,1},{3,2}}\nOutput: Yes\nExplanation: It is possible to finish all the tasks in the order : 3 2 1 0.\nFirst, we will finish task 3. Then we will finish task 2, task 1, and task 0. \n\nInput: N = 4, P = 4, array[] = {{1,2},{4,3},{2,4},{4,1}}\nOutput: No\nExplanation: It is impossible to finish all the tasks. Let’s analyze the pairs:\nFor pair {1, 2} -> we need to finish task 1 first and then task 2. (order : 1 2).\nFor pair{4, 3} -> we need to finish task 4 first and then task 3. (order: 4 3).\nFor pair {2, 4} -> we need to finish task 2 first and then task 4. (order: 1 2 4 3).\nBut for pair {4, 1} -> we need to finish task 4 first and then task 1 but this pair contradicts the previous pair. So, it is not possible to finish all the tasks."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Course Schedule I",
      "algorithm": "Think of the courses as nodes in a directed graph, where an edge from course A → B means you must complete A before B. The problem reduces to checking if this graph contains a cycle. If a cycle exists, it means some set of courses depend on each other circularly, making it impossible to finish them. If no cycle exists, then there is at least one valid order to complete all courses. So the core intuition is that finishing all courses is only possible if the graph of prerequisites forms a Directed Acyclic Graph (DAG). We can either use DFS cycle detection or BFS (Kahn’s algorithm).\n \n\nBuild a graph where each course is a node and an edge from b to a means course a depends on b.\nCompute in-degrees (number of prerequisites) for each course. Push all nodes with in-degree = 0 into a queue.\nRepeatedly pop from queue and reduce in-degrees of neighbors. Push new nodes with in-degree equal to 0.\nSum up the hours for all piles.\nCount number of processed nodes. If processed nodes is equal to total courses then return true, otherwise return false.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to verify if all courses can be finished\n    bool canFinish(int numCourses, \n                   vector<vector<int>>& prerequisites) {\n        // Build adjacency list\n        vector<vector<int>> adj(numCourses);\n        // Build in-degree array\n        vector<int> inDegree(numCourses, 0);\n\n        // Fill adjacency and in-degree\n        for (auto& pre : prerequisites) {\n            int a = pre[0], b = pre[1];\n            adj[b].push_back(a);\n            inDegree[a]++;\n        }\n\n        // Initialize queue with zero in-degree nodes\n        queue<int> q;\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) {\n                q.push(i);\n            }\n        }\n\n        // Count processed nodes\n        int count = 0;\n\n        // Process queue\n        while (!q.empty()) {\n            int node = q.front();\n            q.pop();\n            count++;\n\n            // Reduce in-degree of neighbors\n            for (int nei : adj[node]) {\n                inDegree[nei]--;\n                if (inDegree[nei] == 0) {\n                    q.push(nei);\n                }\n            }\n        }\n\n        // Return true if all nodes processed\n        return count == numCourses;\n    }\n};\n\nint main() {\n    Solution sol;\n    vector<vector<int>> prerequisites = {{1, 0}, {0, 1}};\n    int numCourses = 2;\n    cout << (sol.canFinish(numCourses, prerequisites) \n             ? \"true\" : \"false\");\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to verify if all courses can be finished\n    public boolean canFinish(int numCourses, \n                             int[][] prerequisites) {\n        // Build adjacency list\n        List<List<Integer>> adj = new ArrayList<>();\n        // Build in-degree array\n        int[] inDegree = new int[numCourses];\n\n        // Initialize adjacency list\n        for (int i = 0; i < numCourses; i++) {\n            adj.add(new ArrayList<>());\n        }\n\n        // Fill adjacency and in-degree\n        for (int[] pre : prerequisites) {\n            int a = pre[0], b = pre[1];\n            adj.get(b).add(a);\n            inDegree[a]++;\n        }\n\n        // Initialize queue with zero in-degree nodes\n        Queue<Integer> q = new LinkedList<>();\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) {\n                q.offer(i);\n            }\n        }\n\n        // Count processed nodes\n        int count = 0;\n\n        // Process queue\n        while (!q.isEmpty()) {\n            int node = q.poll();\n            count++;\n\n            // Reduce in-degree of neighbors\n            for (int nei : adj.get(node)) {\n                inDegree[nei]--;\n                if (inDegree[nei] == 0) {\n                    q.offer(nei);\n                }\n            }\n        }\n\n        // Return true if all nodes processed\n        return count == numCourses;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int numCourses = 2;\n        int[][] prerequisites = {{1, 0}, {0, 1}};\n        System.out.println(sol.canFinish(numCourses, \n                                         prerequisites));\n    }\n}",
        "python": "from collections import deque\n\nclass Solution:\n    # Function to verify if all courses can be finished\n    def canFinish(self, numCourses, prerequisites):\n        # Build adjacency list\n        adj = [[] for _ in range(numCourses)]\n        # Build in-degree array\n        inDegree = [0] * numCourses\n\n        # Fill adjacency and in-degree\n        for a, b in prerequisites:\n            adj[b].append(a)\n            inDegree[a] += 1\n\n        # Initialize queue with zero in-degree nodes\n        q = deque([i for i in range(numCourses) \n                   if inDegree[i] == 0])\n\n        # Count processed nodes\n        count = 0\n\n        # Process queue\n        while q:\n            node = q.popleft()\n            count += 1\n\n            # Reduce in-degree of neighbors\n            for nei in adj[node]:\n                inDegree[nei] -= 1\n                if inDegree[nei] == 0:\n                    q.append(nei)\n\n        # Return true if all nodes processed\n        return count == numCourses\n\n# Driver\nif __name__ == \"__main__\":\n    sol = Solution()\n    prerequisites = [[1, 0], [0, 1]]\n    numCourses = 2\n    print(sol.canFinish(numCourses, prerequisites))",
        "javascript": "class Solution {\n    // Function to verify if all courses can be finished\n    canFinish(numCourses, prerequisites) {\n        // Build adjacency list\n        const adj = Array.from({ length: numCourses }, \n                               () => []);\n        // Build in-degree array\n        const inDegree = new Array(numCourses).fill(0);\n\n        // Fill adjacency and in-degree\n        for (let [a, b] of prerequisites) {\n            adj[b].push(a);\n            inDegree[a]++;\n        }\n\n        // Initialize queue with zero in-degree nodes\n        const q = [];\n        for (let i = 0; i < numCourses; i++) {\n            if (inDegree[i] === 0) {\n                q.push(i);\n            }\n        }\n\n        // Count processed nodes\n        let count = 0;\n\n        // Process queue\n        while (q.length > 0) {\n            const node = q.shift();\n            count++;\n\n            // Reduce in-degree of neighbors\n            for (let nei of adj[node]) {\n                inDegree[nei]--;\n                if (inDegree[nei] === 0) {\n                    q.push(nei);\n                }\n            }\n        }\n\n        // Return true if all nodes processed\n        return count === numCourses;\n    }\n}\n\n// Driver\nconst sol = new Solution();\nconst prerequisites = [[1, 0], [0, 1]];\nconst numCourses = 2;\nconsole.log(sol.canFinish(numCourses, prerequisites));"
      }
    },
    {
      "approach_name": "Course Schedule II",
      "algorithm": "Here the setup is the same, but instead of just checking feasibility, we want the actual order of course completion. This order must respect the prerequisite rules, which is exactly what a topological sort does on a DAG. If a cycle exists, no valid order exists and we return an empty list. If no cycle exists, we can either perform a DFS-based topological sort or use BFS with in-degrees (Kahn’s algorithm) to construct a valid sequence. The key intuition is that finding the course order is equivalent to generating a topological ordering of the directed graph.\n Build an adjacency list where each prerequisite pair [a, b] creates a directed edge from b to a. Create an array to store the number of prerequisites for each course and fill it while building the graph. Put all courses with zero prerequisites into a queue. While the queue is not empty, remove a course, add it to the order, and decrease the prerequisite count of its neighbors. If a neighbor’s count becomes zero, add it to the queue. At the end, if the order contains all courses return it, otherwise return an empty list.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find ordering of courses\n    vector<int> findOrder(int numCourses, \n                          vector<vector<int>>& prerequisites) {\n        // Build adjacency list\n        vector<vector<int>> adj(numCourses);\n        // Build in-degree array\n        vector<int> inDegree(numCourses, 0);\n\n        // Fill adjacency and in-degree\n        for (auto& pre : prerequisites) {\n            int a = pre[0], b = pre[1];\n            adj[b].push_back(a);\n            inDegree[a]++;\n        }\n\n        // Initialize queue with zero in-degree nodes\n        queue<int> q;\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) {\n                q.push(i);\n            }\n        }\n\n        // Create result list\n        vector<int> order;\n\n        // Process queue\n        while (!q.empty()) {\n            int node = q.front();\n            q.pop();\n            order.push_back(node);\n\n            // Reduce in-degree of neighbors\n            for (int nei : adj[node]) {\n                inDegree[nei]--;\n                if (inDegree[nei] == 0) {\n                    q.push(nei);\n                }\n            }\n        }\n\n        // Return order if valid, else empty\n        if ((int)order.size() == numCourses) {\n            return order;\n        }\n        return {};\n    }\n};\n\nint main() {\n    Solution sol;\n    vector<vector<int>> prerequisites = {{1, 0}, {2, 0}, {3, 1}, {3, 2}};\n    int numCourses = 4;\n    vector<int> ans = sol.findOrder(numCourses, prerequisites);\n    for (int x : ans) cout << x << \" \";\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to find ordering of courses\n    public int[] findOrder(int numCourses, int[][] prerequisites) {\n        // Build adjacency list\n        List<List<Integer>> adj = new ArrayList<>();\n        // Build in-degree array\n        int[] inDegree = new int[numCourses];\n\n        // Initialize adjacency list\n        for (int i = 0; i < numCourses; i++) {\n            adj.add(new ArrayList<>());\n        }\n\n        // Fill adjacency and in-degree\n        for (int[] pre : prerequisites) {\n            int a = pre[0], b = pre[1];\n            adj.get(b).add(a);\n            inDegree[a]++;\n        }\n\n        // Initialize queue with zero in-degree nodes\n        Queue<Integer> q = new LinkedList<>();\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) {\n                q.offer(i);\n            }\n        }\n\n        // Create result list\n        int[] order = new int[numCourses];\n        int idx = 0;\n\n        // Process queue\n        while (!q.isEmpty()) {\n            int node = q.poll();\n            order[idx++] = node;\n\n            // Reduce in-degree of neighbors\n            for (int nei : adj.get(node)) {\n                inDegree[nei]--;\n                if (inDegree[nei] == 0) {\n                    q.offer(nei);\n                }\n            }\n        }\n\n        // Return order if valid, else empty\n        if (idx == numCourses) {\n            return order;\n        }\n        return new int[0];\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int numCourses = 4;\n        int[][] prerequisites = {{1,0},{2,0},{3,1},{3,2}};\n        int[] ans = sol.findOrder(numCourses, prerequisites);\n        System.out.println(Arrays.toString(ans));\n    }\n}",
        "python": "from collections import deque\n\nclass Solution:\n    # Function to find ordering of courses\n    def findOrder(self, numCourses, prerequisites):\n        # Build adjacency list\n        adj = [[] for _ in range(numCourses)]\n        # Build in-degree array\n        inDegree = [0] * numCourses\n\n        # Fill adjacency and in-degree\n        for a, b in prerequisites:\n            adj[b].append(a)\n            inDegree[a] += 1\n\n        # Initialize queue with zero in-degree nodes\n        q = deque([i for i in range(numCourses) \n                   if inDegree[i] == 0])\n\n        # Create result list\n        order = []\n\n        # Process queue\n        while q:\n            node = q.popleft()\n            order.append(node)\n\n            # Reduce in-degree of neighbors\n            for nei in adj[node]:\n                inDegree[nei] -= 1\n                if inDegree[nei] == 0:\n                    q.append(nei)\n\n        # Return order if valid, else empty\n        return order if len(order) == numCourses else []\n\n# Driver\nif __name__ == \"__main__\":\n    sol = Solution()\n    prerequisites = [[1,0],[2,0],[3,1],[3,2]]\n    numCourses = 4\n    print(sol.findOrder(numCourses, prerequisites))",
        "javascript": "class Solution {\n    // Function to find ordering of courses\n    findOrder(numCourses, prerequisites) {\n        // Build adjacency list\n        const adj = Array.from({ length: numCourses }, \n                               () => []);\n        // Build in-degree array\n        const inDegree = new Array(numCourses).fill(0);\n\n        // Fill adjacency and in-degree\n        for (let [a, b] of prerequisites) {\n            adj[b].push(a);\n            inDegree[a]++;\n        }\n\n        // Initialize queue with zero in-degree nodes\n        const q = [];\n        for (let i = 0; i < numCourses; i++) {\n            if (inDegree[i] === 0) {\n                q.push(i);\n            }\n        }\n\n        // Create result list\n        const order = [];\n\n        // Process queue\n        while (q.length > 0) {\n            const node = q.shift();\n            order.push(node);\n\n            // Reduce in-degree of neighbors\n            for (let nei of adj[node]) {\n                inDegree[nei]--;\n                if (inDegree[nei] === 0) {\n                    q.push(nei);\n                }\n            }\n        }\n\n        // Return order if valid, else empty\n        return order.length === numCourses ? order : [];\n    }\n}\n\n// Driver\nconst sol = new Solution();\nconst prerequisites = [[1,0],[2,0],[3,1],[3,2]];\nconst numCourses = 4;\nconsole.log(sol.findOrder(numCourses, prerequisites));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:19Z"
}