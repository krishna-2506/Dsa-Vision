{
  "problem_id": "851",
  "problem_name": "Print Longest Increasing Subsequence",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 7,
  "substep_name": "DP on LIS",
  "difficulty": "Medium",
  "youtube_url": "https://youtu.be/IFfYfonAFGc",
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/printing-longest-increasing-subsequence-dp-42/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/print-longest-increasing-subsequence",
  "problem_statement": "Problem Description: Given an array of n integers arr, return the Longest Increasing Subsequence (LIS) that is index-wise lexicographically smallest.The Longest Increasing Subsequence (LIS) is the longest subsequence where all elements are in strictly increasing order.\n\nA subsequence A1 is index-wise lexicographically smaller than another subsequence A2 if, at the first position where A1 and A2 differ, the element in A1 appears earlier in the array than corresponding element in A2.",
  "examples": [
    "Input: arr = [10, 22, 9, 33, 21, 50, 41, 60, 80]\nOutput: [10, 22, 33, 50, 60, 80]\nExplanation: The LIS is [10, 22, 33, 50, 60, 80] and it is the lexicographically smallest.\n\nInput: arr = [1, 3, 2, 4, 6, 5]\nOutput: [1, 3, 4, 6] \nExplanation: Possible LIS sequences are [1, 3, 4, 6] and [1, 2, 4, 6]. Since [1, 3, 4, 6] is index-wise lexicographically smaller, it is the result."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "To find the Longest Increasing Subsequence (LIS), we use a 1D DP array where each element at position i stores the length of the LIS ending at index i of the array.\nTo make this DP array we can follow any of the approach that we learned while finding the length of the Longest Increasing Subsequence.\n\nAfter computing the length of the Longest Increasing Subsequence (LIS) for each element using dynamic programming, start from the element with the maximum LIS value.\nInitialize an empty list or string to store the LIS elements in reverse order.\nFrom the element with the maximum LIS, move backward through the array to find elements that can form the LIS:\n \nFor the current element, look for a previous element that is smaller and whose LIS value is exactly one less than the current element's LIS.\nAdd that previous element to the LIS list and continue the process from that element.\n\nRepeat this process until you reach the start of the LIS (where LIS value is 1).\nReverse the collected elements since they were collected backwards to get the actual LIS.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Solution class for LIS with reconstruction using DP\nclass Solution {\npublic:\n    // Function to return the LIS as a sequence (vector)\n    vector<int> longestIncreasingSubsequence(vector<int> &nums) {\n        int n = nums.size();\n\n        // DP array to store length of LIS ending at each index\n        vector<int> dp(n, 1);\n\n        // Array to store previous index of LIS element for reconstruction\n        vector<int> prev(n, -1);\n\n        // Compute LIS length for each index\n        for (int i = 1; i < n; i++) {\n            for (int j = 0; j < i; j++) {\n                if (nums[j] < nums[i] && dp[j] + 1 > dp[i]) {\n                    // Update dp[i] and store previous index\n                    dp[i] = dp[j] + 1;\n                    prev[i] = j;\n                }\n            }\n        }\n\n        // Find the index of maximum LIS length\n        int maxLen = 0, maxIndex = 0;\n        for (int i = 0; i < n; i++) {\n            if (dp[i] > maxLen) {\n                maxLen = dp[i];\n                maxIndex = i;\n            }\n        }\n\n        // Reconstruct LIS sequence\n        vector<int> lisSeq;\n        int curr = maxIndex;\n        while (curr != -1) {\n            lisSeq.push_back(nums[curr]);\n            curr = prev[curr];\n        }\n\n        // Reverse sequence as it was built backwards\n        reverse(lisSeq.begin(), lisSeq.end());\n\n        return lisSeq;\n    }\n};\n\nint main() {\n    vector<int> nums = {10, 9, 2, 5, 3, 7, 101, 18};\n\n    Solution sol;\n    vector<int> lis = sol.longestIncreasingSubsequence(nums);\n\n    cout << \"LIS: \";\n    for (int x : lis) cout << x << \" \";\n    cout << endl;\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to return the LIS as a list\n    public List<Integer> longestIncreasingSubsequence(int[] nums) {\n        int n = nums.length;\n\n        // DP array to store length of LIS ending at each index\n        int[] dp = new int[n];\n\n        // Array to store previous index of LIS element for reconstruction\n        int[] prev = new int[n];\n\n        Arrays.fill(dp, 1);\n        Arrays.fill(prev, -1);\n\n        // Compute LIS length for each index\n        for (int i = 1; i < n; i++) {\n            for (int j = 0; j < i; j++) {\n                if (nums[j] < nums[i] && dp[j] + 1 > dp[i]) {\n                    // Update dp[i] and store previous index\n                    dp[i] = dp[j] + 1;\n                    prev[i] = j;\n                }\n            }\n        }\n\n        // Find the index of maximum LIS length\n        int maxLen = 0, maxIndex = 0;\n        for (int i = 0; i < n; i++) {\n            if (dp[i] > maxLen) {\n                maxLen = dp[i];\n                maxIndex = i;\n            }\n        }\n\n        // Reconstruct LIS sequence\n        List<Integer> lisSeq = new ArrayList<>();\n        int curr = maxIndex;\n        while (curr != -1) {\n            lisSeq.add(nums[curr]);\n            curr = prev[curr];\n        }\n\n        // Reverse sequence as it was built backwards\n        Collections.reverse(lisSeq);\n\n        return lisSeq;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[] nums = {10, 9, 2, 5, 3, 7, 101, 18};\n\n        Solution sol = new Solution();\n        List<Integer> lis = sol.longestIncreasingSubsequence(nums);\n\n        System.out.print(\"LIS: \");\n        for (int x : lis) System.out.print(x + \" \");\n        System.out.println();\n    }\n}",
        "python": "from typing import List\n\nclass Solution:\n    # Function to return the LIS as a list\n    def longestIncreasingSubsequence(self, nums: List[int]) -> List[int]:\n        n = len(nums)\n        \n        # DP array to store length of LIS ending at each index\n        dp = [1] * n\n        \n        # Array to store previous index of LIS element for reconstruction\n        prev = [-1] * n\n        \n        # Compute LIS length for each index\n        for i in range(1, n):\n            for j in range(i):\n                if nums[j] < nums[i] and dp[j] + 1 > dp[i]:\n                    # Update dp[i] and store previous index\n                    dp[i] = dp[j] + 1\n                    prev[i] = j\n        \n        # Find the index of maximum LIS length\n        max_len = 0\n        max_index = 0\n        for i in range(n):\n            if dp[i] > max_len:\n                max_len = dp[i]\n                max_index = i\n        \n        # Reconstruct LIS sequence\n        lis_seq = []\n        curr = max_index\n        while curr != -1:\n            lis_seq.append(nums[curr])\n            curr = prev[curr]\n        \n        # Reverse sequence as it was built backwards\n        lis_seq.reverse()\n        return lis_seq\n\nif __name__ == \"__main__\":\n    nums = [10, 9, 2, 5, 3, 7, 101, 18]\n    sol = Solution()\n    lis = sol.longestIncreasingSubsequence(nums)\n    print(\"LIS:\", *lis)",
        "javascript": "class Solution {\n    // Function to return the LIS as an array\n    longestIncreasingSubsequence(nums) {\n        const n = nums.length;\n\n        // DP array to store length of LIS ending at each index\n        const dp = new Array(n).fill(1);\n\n        // Array to store previous index of LIS element for reconstruction\n        const prev = new Array(n).fill(-1);\n\n        // Compute LIS length for each index\n        for (let i = 1; i < n; i++) {\n            for (let j = 0; j < i; j++) {\n                if (nums[j] < nums[i] && dp[j] + 1 > dp[i]) {\n                    // Update dp[i] and store previous index\n                    dp[i] = dp[j] + 1;\n                    prev[i] = j;\n                }\n            }\n        }\n\n        // Find the index of maximum LIS length\n        let maxLen = 0;\n        let maxIndex = 0;\n        for (let i = 0; i < n; i++) {\n            if (dp[i] > maxLen) {\n                maxLen = dp[i];\n                maxIndex = i;\n            }\n        }\n\n        // Reconstruct LIS sequence\n        let lisSeq = [];\n        let curr = maxIndex;\n        while (curr !== -1) {\n            lisSeq.push(nums[curr]);\n            curr = prev[curr];\n        }\n\n        // Reverse sequence as it was built backwards\n        lisSeq.reverse();\n        return lisSeq;\n    }\n}\n\n// Driver code\nconst nums = [10, 9, 2, 5, 3, 7, 101, 18];\n\nconst sol = new Solution();\nconst lis = sol.longestIncreasingSubsequence(nums);\nconsole.log(\"LIS:\", lis.join(\" \"));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:25Z"
}