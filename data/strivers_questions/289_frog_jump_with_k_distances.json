{
  "problem_id": "289",
  "problem_name": "Frog jump with K distances",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 2,
  "substep_name": "1D DP",
  "difficulty": "Medium",
  "youtube_url": "https://www.youtube.com/watch?v=Kmh3rhyEtB8",
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/dynamic-programming-frog-jump-with-k-distances-dp-4/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/frog-jump-with-k-distances",
  "problem_statement": "A frog wants to climb a staircase with n steps. Given an integer array heights, where heights[i] contains the height\n of the ith step, and an integer k.\n To jump from the ith step to the jth step, the frog requires abs(heights[i] - heights[j]) energy, where abs()\n denotes the absolute difference. The frog can jump from the ith step to any step in the range [i + 1, i + k],\n provided it exists. Return the minimum amount of energy required by the frog to go from the 0th step to the (n-1)th\n step.\n\n Examples\n \n\nExample 1:\nInput: heights = [10, 5, 20, 0, 15], k = 2\nOutput: 15\nExplanation:\n0th step -> 2nd step, cost = abs(10 - 20) = 10\n2nd step -> 4th step, cost = abs(20 - 15) = 5\nTotal cost = 10 + 5 = 15.\n\nExample 2:\nInput: heights = [15, 4, 1, 14, 15], k = 3\nOutput: 2\nExplanation:\n0th step -> 3rd step, cost = abs(15 - 14) = 1\n3rd step -> 4th step, cost = abs(14 - 15) = 1\nTotal cost = 1 + 1 = 2.\n \n\nDisclaimer: Here is the practice\n link to help you assess your knowledge better. It's highly recommend trying to\n solve it before looking at the solution. \n\n Memoization Approach\n \n\n Algorithm\n \n\n The problem is a variation of the \"Frog Jump\" dynamic programming problem where the frog can\n jump up to k steps. The goal is to reach the last stair (index\n n-1) with the minimum cost, where the cost of jumping from stair i\n to stair j is the absolute difference |height[i] - height[j]|.\n \n\n We use recursion with memoization (top-down dynamic programming) to avoid redundant\n calculations. The function solveUtil(ind) returns the minimum cost to reach\n stair ind.\n \nBase case: If we're at stair 0, the cost is 0.\nMemoization: If we’ve already computed the cost to reach index ind, we\n return it from the dp array.\nRecursive case: For each possible jump j from 1 to k, we\n calculate the cost to reach ind by jumping from ind-j and take\n the minimum of all such costs.\n\nCode\n\nCppJavaPythonJavascript\n\n123456789101112131415161718192021222324252627282930313233343536373839404142434445464748\n\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\nint solveUtil(int ind, vector<int>& height, vector<int>& dp, int k) {\n // Base case: starting point has zero cost\n if (ind == 0) return 0;\n\n // Return already computed result\n if (dp[ind] != -1) return dp[ind];\n\n // Initialize minimum steps as large value\n int mmSteps = INT_MAX;\n\n // Try all possible jumps from 1 to k\n for (int j = 1; j <= k; j++) {\n // Ensure jump does not go out of bounds\n if (ind - j >= 0) {\n // Cost of taking the jump\n int jump = solveUtil(ind - j, height, dp, k) + abs(height[ind] - height[ind - j]);\n // Store the minimum cost\n mmSteps = min(jump, mmSteps);\n }\n }\n // Save the result in dp array\n return dp[ind] = mmSteps;\n}\n\n// Function to get minimum cost to reach end\nint solve(int n, vector<int>& height, int k) {\n // DP array initialized to -1\n vector<int> dp(n, -1);\n // Start recursion from last index\n return solveUtil(n - 1, height, dp, k);\n}\n\n// Driver code\nint main() {\n // Heights of stones\n vector<int> height{30, 10, 60, 10, 60, 50};\n int n = height.size();\n int k = 2;\n\n // Output the minimum cost\n cout << solve(n, height, k) << endl;\n return 0;\n}\n\n1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253\n\nimport java.util.*;\n\nclass Solution {\n // Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\n public int solveUtil(int ind, List<Integer> height, int[] dp, int k) {\n // Base case\n if (ind == 0) return 0;\n\n // Return already computed result\n if (dp[ind] != -1) return dp[ind];\n\n // Initialize minimum steps as large value\n int mmSteps = Integer.MAX_VALUE;\n\n // Try all possible jumps from 1 to k\n for (int j = 1; j <= k; j++) {\n // Ensure jump does not go out of bounds\n if (ind - j >= 0) {\n // Cost of taking the jump\n int jump = solveUtil(ind - j, height, dp, k) + Math.abs(height.get(ind) - height.get(ind - j));\n // Store the minimum cost\n mmSteps = Math.min(jump, mmSteps);\n }\n }\n // Save the result in dp array\n return dp[ind] = mmSteps;\n }\n\n // Function to get minimum cost to reach end\n public int solve(int n, List<Integer> height, int k) {\n // DP array initialized to -1\n int[] dp = new int[n];\n Arrays.fill(dp, -1);\n // Start recursion from last index\n return solveUtil(n - 1, height, dp, k);\n }\n}\n\npublic class Main {\n public static void main(String[] args) {\n // Heights of stones\n List<Integer> height = Arrays.asList(30, 10, 60, 10, 60, 50);\n int n = height.size();\n int k = 2;\n\n // Create solution object\n Solution sol = new Solution();\n\n // Output the minimum cost\n System.out.println(sol.solve(n, height, k));\n }\n}\n\n1234567891011121314151617181920212223242526272829303132333435363738394041424344454647\n\nclass Solution:\n # Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\n def solveUtil(self, ind, height, dp, k):\n # Base case\n if ind == 0:\n return 0\n \n # Return already computed result\n if dp[ind] != -1:\n return dp[ind]\n \n # Initialize minimum steps as large value\n mmSteps = float('inf')\n \n # Try all possible jumps from 1 to k\n for j in range(1, k + 1):\n # Ensure jump does not go out of bounds\n if ind - j >= 0:\n # Cost of taking the jump\n jump = self.solveUtil(ind - j, height, dp, k) + abs(height[ind] - height[ind - j])\n # Store the minimum cost\n mmSteps = min(jump, mmSteps)\n \n # Save the result in dp array\n dp[ind] = mmSteps\n return dp[ind]\n\n # Function to get minimum cost to reach end\n def solve(self, n, height, k):\n # DP array initialized to -1\n dp = [-1] * n\n # Start recursion from last index\n return self.solveUtil(n - 1, height, dp, k)\n\n# Driver code\nif __name__ == \"__main__\":\n # Heights of stones\n height = [30, 10, 60, 10, 60, 50]\n n = len(height)\n k = 2\n\n # Create solution object\n sol = Solution()\n\n # Output the minimum cost\n print(sol.solve(n, height, k))\n\n123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051\n\nclass Solution {\n // Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\n solveUtil(ind, height, dp, k) {\n // Base case\n if (ind === 0) return 0;\n\n // Return already computed result\n if (dp[ind] !== -1) return dp[ind];\n\n // Initialize minimum steps as large value\n let mmSteps = Infinity;\n\n // Try all possible jumps from 1 to k\n for (let j = 1; j <= k; j++) {\n // Ensure jump does not go out of bounds\n if (ind - j >= 0) {\n // Cost of taking the jump\n let jump = this.solveUtil(ind - j, height, dp, k) + Math.abs(height[ind] - height[ind - j]);\n // Store the minimum cost\n mmSteps = Math.min(jump, mmSteps);\n }\n }\n\n // Save the result in dp array\n dp[ind] = mmSteps;\n return dp[ind];\n }\n\n // Function to get minimum cost to reach end\n solve(n, height, k) {\n // DP array initialized to -1\n let dp = Array(n).fill(-1);\n // Start recursion from last index\n return this.solveUtil(n - 1, height, dp, k);\n }\n}\n\n// Driver code\n(function () {\n // Heights of stones\n const height = [30, 10, 60, 10, 60, 50];\n const n = height.length;\n const k = 2;\n\n // Create solution object\n const sol = new Solution();\n\n // Output the minimum cost\n console.log(sol.solve(n, height, k));\n})();\n\n Complexity Analysis\n \n\nTime Complexity: O(n * k), where n is the number of stairs and\n k is the maximum jump allowed. For each index, we compute up to k\n recursive calls, each memoized.\n \nSpace Complexity: O(n), due to the memoization array dp storing\n results for all indices from 0 to n-1.\n\n Tabulation Approach\n \n\n Algorithm\n \n\n In this variation of the classic frog jump problem, the frog can jump from any stone to one\n of the previous k stones. The cost of each jump is the absolute difference in\n height between the stones. The goal is to minimize the total cost to reach the last stone\n starting from the first.\n \n\n Instead of using recursion with memoization, we can solve this iteratively using bottom-up\n dynamic programming. This helps avoid recursion stack overhead and provides an efficient way\n to build the solution from the base case.\n \n\n We use a dp[] array where dp[i] represents the minimum cost to\n reach the ith stone.\n \nWe initialize dp[0] = 0 because the frog starts there with no cost.\nFor every stone from index 1 to n-1, we try all possible jump\n lengths j from 1 to k.\nFor every valid jump, we compute the cost as\n dp[i - j] + abs(height[i] - height[i - j]).\n \nWe take the minimum of all such possible jump costs and store it in dp[i].\n \nFinally, dp[n - 1] contains the minimum cost to reach the last stone.\n\nCode\n\nCppJavaPythonJavascript\n\n12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152\n\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Function to compute the minimum cost to reach the end using at most 'k' jumps\nint solveUtil(int n, vector<int>& height, vector<int>& dp, int k) {\n // Base case: cost to reach the first stone is 0\n dp[0] = 0;\n\n // Iterate over each stone\n for (int i = 1; i < n; i++) {\n // Initialize the minimum cost for this stone as large value\n int mmSteps = INT_MAX;\n\n // Try all possible jump lengths from 1 to k\n for (int j = 1; j <= k; j++) {\n // Ensure jump doesn't go out of bounds\n if (i - j >= 0) {\n // Cost of jumping from (i - j) to i\n int jump = dp[i - j] + abs(height[i] - height[i - j]);\n // Keep track of the minimum cost\n mmSteps = min(mmSteps, jump);\n }\n }\n\n // Store the computed minimum cost for this stone\n dp[i] = mmSteps;\n }\n\n // The last element of dp stores the answer\n return dp[n - 1];\n}\n\n// Main function to solve the problem\nint solve(int n, vector<int>& height, int k) {\n // Initialize DP array with -1\n vector<int> dp(n, -1);\n // Fill DP array iteratively\n return solveUtil(n, height, dp, k);\n}\n\n// Driver code\nint main() {\n // Heights of stones\n vector<int> height{30, 10, 60, 10, 60, 50};\n int n = height.size();\n int k = 2;\n\n // Output result\n cout << solve(n, height, k) << endl;\n return 0;\n}\n\n1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556\n\nimport java.util.*;\n\nclass Solution {\n // Function to compute the minimum cost to reach the end using at most 'k' jumps\n public int solveUtil(int n, List<Integer> height, int[] dp, int k) {\n // Base case: cost to reach first stone is 0\n dp[0] = 0;\n\n // Iterate over each stone\n for (int i = 1; i < n; i++) {\n // Initialize minimum cost for this stone as large value\n int mmSteps = Integer.MAX_VALUE;\n\n // Try all possible jump lengths from 1 to k\n for (int j = 1; j <= k; j++) {\n // Ensure jump doesn't go out of bounds\n if (i - j >= 0) {\n // Cost of jumping from (i - j) to i\n int jump = dp[i - j] + Math.abs(height.get(i) - height.get(i - j));\n // Keep track of the minimum cost\n mmSteps = Math.min(mmSteps, jump);\n }\n }\n\n // Store computed minimum cost\n dp[i] = mmSteps;\n }\n\n // Last element of dp stores the answer\n return dp[n - 1];\n }\n\n // Function to solve the problem\n public int solve(int n, List<Integer> height, int k) {\n // Initialize DP array with -1\n int[] dp = new int[n];\n Arrays.fill(dp, -1);\n return solveUtil(n, height, dp, k);\n }\n}\n\npublic class Main {\n public static void main(String[] args) {\n // Heights of stones\n List<Integer> height = Arrays.asList(30, 10, 60, 10, 60, 50);\n int n = height.size();\n int k = 2;\n\n // Create solution object\n Solution sol = new Solution();\n\n // Output result\n System.out.println(sol.solve(n, height, k));\n }\n}\n\n123456789101112131415161718192021222324252627282930313233343536373839404142434445\n\nclass Solution:\n # Function to compute the minimum cost to reach the end using at most 'k' jumps\n def solveUtil(self, n, height, dp, k):\n # Base case: cost to reach first stone is 0\n dp[0] = 0\n\n # Iterate over each stone\n for i in range(1, n):\n # Initialize minimum cost for this stone as large value\n mmSteps = float('inf')\n\n # Try all possible jump lengths from 1 to k\n for j in range(1, k + 1):\n # Ensure jump doesn't go out of bounds\n if i - j >= 0:\n # Cost of jumping from (i - j) to i\n jump = dp[i - j] + abs(height[i] - height[i - j])\n # Keep track of the minimum cost\n mmSteps = min(mmSteps, jump)\n\n # Store computed minimum cost\n dp[i] = mmSteps\n\n # Last element of dp stores the answer\n return dp[n - 1]\n\n # Function to solve the problem\n def solve(self, n, height, k):\n # Initialize DP array with -1\n dp = [-1] * n\n return self.solveUtil(n, height, dp, k)\n\n# Driver code\nif __name__ == \"__main__\":\n # Heights of stones\n height = [30, 10, 60, 10, 60, 50]\n n = len(height)\n k = 2\n\n # Create solution object\n sol = Solution()\n\n # Output result\n print(sol.solve(n, height, k))\n\n12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152\n\nclass Solution {\n // Function to compute the minimum cost to reach the end using at most 'k' jumps\n solveUtil(n, height, dp, k) {\n // Base case: cost to reach first stone is 0\n dp[0] = 0;\n\n // Iterate over each stone\n for (let i = 1; i < n; i++) {\n // Initialize minimum cost for this stone as large value\n let mmSteps = Infinity;\n\n // Try all possible jump lengths from 1 to k\n for (let j = 1; j <= k; j++) {\n // Ensure jump doesn't go out of bounds\n if (i - j >= 0) {\n // Cost of jumping from (i - j) to i\n let jump = dp[i - j] + Math.abs(height[i] - height[i - j]);\n // Keep track of the minimum cost\n mmSteps = Math.min(mmSteps, jump);\n }\n }\n\n // Store computed minimum cost\n dp[i] = mmSteps;\n }\n\n // Last element of dp stores the answer\n return dp[n - 1];\n }\n\n // Function to solve the problem\n solve(n, height, k) {\n // Initialize DP array with -1\n let dp = new Array(n).fill(-1);\n return this.solveUtil(n, height, dp, k);\n }\n}\n\n// Driver code\n(function () {\n // Heights of stones\n const height = [30, 10, 60, 10, 60, 50];\n const n = height.length;\n const k = 2;\n\n // Create solution object\n const sol = new Solution();\n\n // Output result\n console.log(sol.solve(n, height, k));\n})();\n\n Complexity Analysis\n \n\nTime Complexity: O(n × k), where n is the number of stones and\n k is the maximum number of jumps. We process n elements and for each\n we check up to k previous jumps.\n \nSpace Complexity: O(n), for the dp array that stores the minimum\n cost to reach each stone.",
  "examples": [
    "Example 1:\nInput: heights = [10, 5, 20, 0, 15], k = 2\nOutput: 15\nExplanation:\n0th step -> 2nd step, cost = abs(10 - 20) = 10\n2nd step -> 4th step, cost = abs(20 - 15) = 5\nTotal cost = 10 + 5 = 15.\n\nExample 2:\nInput: heights = [15, 4, 1, 14, 15], k = 3\nOutput: 2\nExplanation:\n0th step -> 3rd step, cost = abs(15 - 14) = 1\n3rd step -> 4th step, cost = abs(14 - 15) = 1\nTotal cost = 1 + 1 = 2."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Memoization Approach",
      "algorithm": "The problem is a variation of the \"Frog Jump\" dynamic programming problem where the frog can\n jump up to k steps. The goal is to reach the last stair (index\n n-1) with the minimum cost, where the cost of jumping from stair i\n to stair j is the absolute difference |height[i] - height[j]|.\n \n\n We use recursion with memoization (top-down dynamic programming) to avoid redundant\n calculations. The function solveUtil(ind) returns the minimum cost to reach\n stair ind.\n \nBase case: If we're at stair 0, the cost is 0.\nMemoization: If we’ve already computed the cost to reach index ind, we\n return it from the dp array.\nRecursive case: For each possible jump j from 1 to k, we\n calculate the cost to reach ind by jumping from ind-j and take\n the minimum of all such costs.",
      "time_complexity": "O(n * k)",
      "space_complexity": "O(n * k)",
      "time_complexity_details": "Time Complexity: O(n * k), where n is the number of stairs and\n k is the maximum jump allowed. For each index, we compute up to k\n recursive calls, each memoized.\n \nSpace Complexity: O(n), due to the memoization array dp storing\n results for all indices from 0 to n-1.",
      "space_complexity_details": "Time Complexity: O(n * k), where n is the number of stairs and\n k is the maximum jump allowed. For each index, we compute up to k\n recursive calls, each memoized.\n \nSpace Complexity: O(n), due to the memoization array dp storing\n results for all indices from 0 to n-1.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\nint solveUtil(int ind, vector<int>& height, vector<int>& dp, int k) {\n    // Base case: starting point has zero cost\n    if (ind == 0) return 0;\n\n    // Return already computed result\n    if (dp[ind] != -1) return dp[ind];\n\n    // Initialize minimum steps as large value\n    int mmSteps = INT_MAX;\n\n    // Try all possible jumps from 1 to k\n    for (int j = 1; j <= k; j++) {\n        // Ensure jump does not go out of bounds\n        if (ind - j >= 0) {\n            // Cost of taking the jump\n            int jump = solveUtil(ind - j, height, dp, k) + abs(height[ind] - height[ind - j]);\n            // Store the minimum cost\n            mmSteps = min(jump, mmSteps);\n        }\n    }\n    // Save the result in dp array\n    return dp[ind] = mmSteps;\n}\n\n// Function to get minimum cost to reach end\nint solve(int n, vector<int>& height, int k) {\n    // DP array initialized to -1\n    vector<int> dp(n, -1);\n    // Start recursion from last index\n    return solveUtil(n - 1, height, dp, k);\n}\n\n// Driver code\nint main() {\n    // Heights of stones\n    vector<int> height{30, 10, 60, 10, 60, 50};\n    int n = height.size();\n    int k = 2;\n\n    // Output the minimum cost\n    cout << solve(n, height, k) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\n    public int solveUtil(int ind, List<Integer> height, int[] dp, int k) {\n        // Base case\n        if (ind == 0) return 0;\n\n        // Return already computed result\n        if (dp[ind] != -1) return dp[ind];\n\n        // Initialize minimum steps as large value\n        int mmSteps = Integer.MAX_VALUE;\n\n        // Try all possible jumps from 1 to k\n        for (int j = 1; j <= k; j++) {\n            // Ensure jump does not go out of bounds\n            if (ind - j >= 0) {\n                // Cost of taking the jump\n                int jump = solveUtil(ind - j, height, dp, k) + Math.abs(height.get(ind) - height.get(ind - j));\n                // Store the minimum cost\n                mmSteps = Math.min(jump, mmSteps);\n            }\n        }\n        // Save the result in dp array\n        return dp[ind] = mmSteps;\n    }\n\n    // Function to get minimum cost to reach end\n    public int solve(int n, List<Integer> height, int k) {\n        // DP array initialized to -1\n        int[] dp = new int[n];\n        Arrays.fill(dp, -1);\n        // Start recursion from last index\n        return solveUtil(n - 1, height, dp, k);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Heights of stones\n        List<Integer> height = Arrays.asList(30, 10, 60, 10, 60, 50);\n        int n = height.size();\n        int k = 2;\n\n        // Create solution object\n        Solution sol = new Solution();\n\n        // Output the minimum cost\n        System.out.println(sol.solve(n, height, k));\n    }\n}",
        "python": "class Solution:\n    # Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\n    def solveUtil(self, ind, height, dp, k):\n        # Base case\n        if ind == 0:\n            return 0\n        \n        # Return already computed result\n        if dp[ind] != -1:\n            return dp[ind]\n        \n        # Initialize minimum steps as large value\n        mmSteps = float('inf')\n        \n        # Try all possible jumps from 1 to k\n        for j in range(1, k + 1):\n            # Ensure jump does not go out of bounds\n            if ind - j >= 0:\n                # Cost of taking the jump\n                jump = self.solveUtil(ind - j, height, dp, k) + abs(height[ind] - height[ind - j])\n                # Store the minimum cost\n                mmSteps = min(jump, mmSteps)\n        \n        # Save the result in dp array\n        dp[ind] = mmSteps\n        return dp[ind]\n\n    # Function to get minimum cost to reach end\n    def solve(self, n, height, k):\n        # DP array initialized to -1\n        dp = [-1] * n\n        # Start recursion from last index\n        return self.solveUtil(n - 1, height, dp, k)\n\n# Driver code\nif __name__ == \"__main__\":\n    # Heights of stones\n    height = [30, 10, 60, 10, 60, 50]\n    n = len(height)\n    k = 2\n\n    # Create solution object\n    sol = Solution()\n\n    # Output the minimum cost\n    print(sol.solve(n, height, k))",
        "javascript": "class Solution {\n    // Function to find the minimum cost to reach index 'ind' using at most 'k' jumps\n    solveUtil(ind, height, dp, k) {\n        // Base case\n        if (ind === 0) return 0;\n\n        // Return already computed result\n        if (dp[ind] !== -1) return dp[ind];\n\n        // Initialize minimum steps as large value\n        let mmSteps = Infinity;\n\n        // Try all possible jumps from 1 to k\n        for (let j = 1; j <= k; j++) {\n            // Ensure jump does not go out of bounds\n            if (ind - j >= 0) {\n                // Cost of taking the jump\n                let jump = this.solveUtil(ind - j, height, dp, k) + Math.abs(height[ind] - height[ind - j]);\n                // Store the minimum cost\n                mmSteps = Math.min(jump, mmSteps);\n            }\n        }\n\n        // Save the result in dp array\n        dp[ind] = mmSteps;\n        return dp[ind];\n    }\n\n    // Function to get minimum cost to reach end\n    solve(n, height, k) {\n        // DP array initialized to -1\n        let dp = Array(n).fill(-1);\n        // Start recursion from last index\n        return this.solveUtil(n - 1, height, dp, k);\n    }\n}\n\n// Driver code\n(function () {\n    // Heights of stones\n    const height = [30, 10, 60, 10, 60, 50];\n    const n = height.length;\n    const k = 2;\n\n    // Create solution object\n    const sol = new Solution();\n\n    // Output the minimum cost\n    console.log(sol.solve(n, height, k));\n})();"
      }
    },
    {
      "approach_name": "Tabulation Approach",
      "algorithm": "In this variation of the classic frog jump problem, the frog can jump from any stone to one\n of the previous k stones. The cost of each jump is the absolute difference in\n height between the stones. The goal is to minimize the total cost to reach the last stone\n starting from the first.\n \n\n Instead of using recursion with memoization, we can solve this iteratively using bottom-up\n dynamic programming. This helps avoid recursion stack overhead and provides an efficient way\n to build the solution from the base case.\n \n\n We use a dp[] array where dp[i] represents the minimum cost to\n reach the ith stone.\n \nWe initialize dp[0] = 0 because the frog starts there with no cost.\nFor every stone from index 1 to n-1, we try all possible jump\n lengths j from 1 to k.\nFor every valid jump, we compute the cost as\n dp[i - j] + abs(height[i] - height[i - j]).\n \nWe take the minimum of all such possible jump costs and store it in dp[i].\n \nFinally, dp[n - 1] contains the minimum cost to reach the last stone.",
      "time_complexity": "O(n × k)",
      "space_complexity": "O(n × k)",
      "time_complexity_details": "Time Complexity: O(n × k), where n is the number of stones and\n k is the maximum number of jumps. We process n elements and for each\n we check up to k previous jumps.\n \nSpace Complexity: O(n), for the dp array that stores the minimum\n cost to reach each stone.",
      "space_complexity_details": "Time Complexity: O(n × k), where n is the number of stones and\n k is the maximum number of jumps. We process n elements and for each\n we check up to k previous jumps.\n \nSpace Complexity: O(n), for the dp array that stores the minimum\n cost to reach each stone.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Function to compute the minimum cost to reach the end using at most 'k' jumps\nint solveUtil(int n, vector<int>& height, vector<int>& dp, int k) {\n    // Base case: cost to reach the first stone is 0\n    dp[0] = 0;\n\n    // Iterate over each stone\n    for (int i = 1; i < n; i++) {\n        // Initialize the minimum cost for this stone as large value\n        int mmSteps = INT_MAX;\n\n        // Try all possible jump lengths from 1 to k\n        for (int j = 1; j <= k; j++) {\n            // Ensure jump doesn't go out of bounds\n            if (i - j >= 0) {\n                // Cost of jumping from (i - j) to i\n                int jump = dp[i - j] + abs(height[i] - height[i - j]);\n                // Keep track of the minimum cost\n                mmSteps = min(mmSteps, jump);\n            }\n        }\n\n        // Store the computed minimum cost for this stone\n        dp[i] = mmSteps;\n    }\n\n    // The last element of dp stores the answer\n    return dp[n - 1];\n}\n\n// Main function to solve the problem\nint solve(int n, vector<int>& height, int k) {\n    // Initialize DP array with -1\n    vector<int> dp(n, -1);\n    // Fill DP array iteratively\n    return solveUtil(n, height, dp, k);\n}\n\n// Driver code\nint main() {\n    // Heights of stones\n    vector<int> height{30, 10, 60, 10, 60, 50};\n    int n = height.size();\n    int k = 2;\n\n    // Output result\n    cout << solve(n, height, k) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to compute the minimum cost to reach the end using at most 'k' jumps\n    public int solveUtil(int n, List<Integer> height, int[] dp, int k) {\n        // Base case: cost to reach first stone is 0\n        dp[0] = 0;\n\n        // Iterate over each stone\n        for (int i = 1; i < n; i++) {\n            // Initialize minimum cost for this stone as large value\n            int mmSteps = Integer.MAX_VALUE;\n\n            // Try all possible jump lengths from 1 to k\n            for (int j = 1; j <= k; j++) {\n                // Ensure jump doesn't go out of bounds\n                if (i - j >= 0) {\n                    // Cost of jumping from (i - j) to i\n                    int jump = dp[i - j] + Math.abs(height.get(i) - height.get(i - j));\n                    // Keep track of the minimum cost\n                    mmSteps = Math.min(mmSteps, jump);\n                }\n            }\n\n            // Store computed minimum cost\n            dp[i] = mmSteps;\n        }\n\n        // Last element of dp stores the answer\n        return dp[n - 1];\n    }\n\n    // Function to solve the problem\n    public int solve(int n, List<Integer> height, int k) {\n        // Initialize DP array with -1\n        int[] dp = new int[n];\n        Arrays.fill(dp, -1);\n        return solveUtil(n, height, dp, k);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Heights of stones\n        List<Integer> height = Arrays.asList(30, 10, 60, 10, 60, 50);\n        int n = height.size();\n        int k = 2;\n\n        // Create solution object\n        Solution sol = new Solution();\n\n        // Output result\n        System.out.println(sol.solve(n, height, k));\n    }\n}",
        "python": "class Solution:\n    # Function to compute the minimum cost to reach the end using at most 'k' jumps\n    def solveUtil(self, n, height, dp, k):\n        # Base case: cost to reach first stone is 0\n        dp[0] = 0\n\n        # Iterate over each stone\n        for i in range(1, n):\n            # Initialize minimum cost for this stone as large value\n            mmSteps = float('inf')\n\n            # Try all possible jump lengths from 1 to k\n            for j in range(1, k + 1):\n                # Ensure jump doesn't go out of bounds\n                if i - j >= 0:\n                    # Cost of jumping from (i - j) to i\n                    jump = dp[i - j] + abs(height[i] - height[i - j])\n                    # Keep track of the minimum cost\n                    mmSteps = min(mmSteps, jump)\n\n            # Store computed minimum cost\n            dp[i] = mmSteps\n\n        # Last element of dp stores the answer\n        return dp[n - 1]\n\n    # Function to solve the problem\n    def solve(self, n, height, k):\n        # Initialize DP array with -1\n        dp = [-1] * n\n        return self.solveUtil(n, height, dp, k)\n\n# Driver code\nif __name__ == \"__main__\":\n    # Heights of stones\n    height = [30, 10, 60, 10, 60, 50]\n    n = len(height)\n    k = 2\n\n    # Create solution object\n    sol = Solution()\n\n    # Output result\n    print(sol.solve(n, height, k))",
        "javascript": "class Solution {\n    // Function to compute the minimum cost to reach the end using at most 'k' jumps\n    solveUtil(n, height, dp, k) {\n        // Base case: cost to reach first stone is 0\n        dp[0] = 0;\n\n        // Iterate over each stone\n        for (let i = 1; i < n; i++) {\n            // Initialize minimum cost for this stone as large value\n            let mmSteps = Infinity;\n\n            // Try all possible jump lengths from 1 to k\n            for (let j = 1; j <= k; j++) {\n                // Ensure jump doesn't go out of bounds\n                if (i - j >= 0) {\n                    // Cost of jumping from (i - j) to i\n                    let jump = dp[i - j] + Math.abs(height[i] - height[i - j]);\n                    // Keep track of the minimum cost\n                    mmSteps = Math.min(mmSteps, jump);\n                }\n            }\n\n            // Store computed minimum cost\n            dp[i] = mmSteps;\n        }\n\n        // Last element of dp stores the answer\n        return dp[n - 1];\n    }\n\n    // Function to solve the problem\n    solve(n, height, k) {\n        // Initialize DP array with -1\n        let dp = new Array(n).fill(-1);\n        return this.solveUtil(n, height, dp, k);\n    }\n}\n\n// Driver code\n(function () {\n    // Heights of stones\n    const height = [30, 10, 60, 10, 60, 50];\n    const n = height.length;\n    const k = 2;\n\n    // Create solution object\n    const sol = new Solution();\n\n    // Output result\n    console.log(sol.solve(n, height, k));\n})();"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:21Z"
}