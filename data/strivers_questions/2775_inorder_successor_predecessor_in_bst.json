{
  "problem_id": "2775",
  "problem_name": "Inorder Successor/Predecessor in BST",
  "step_no": 14,
  "step_name": "Binary Search Trees [Concept and Problems]",
  "substep_no": 2,
  "substep_name": "Practice Problems",
  "difficulty": "Medium",
  "youtube_url": "https://youtu.be/SXKAD2svfmI",
  "leetcode_url": "https://leetcode.com/problems/inorder-successor-in-bst/",
  "article_url": "https://takeuforward.org/data-structure/inorder-successorpredecessor-in-bst",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/inorder-successor-and-predecessor-in-bst",
  "problem_statement": "Given a Binary Search Tree and a ‘key’ value which represents the data data of a node in this tree. Return the inorder predecessor and successor of the given node in the BST.\n\nThe predecessor of a node in BST is that node that will be visited just before the given node in the inorder traversal of the tree. Return nullptr if the given node is the one that is visited first in the inorder traversal.\nThe successor of a node in BST is that node that will be visited immediately after the given node in the inorder traversal of the tree. Return nullptr if the given node is visited last in the inorder traversal.",
  "examples": [
    "Example 1:\nInput: Binary Search Tree: 5 3 7 2 4 6 9 1 -1 -1 -1 -1 -1 8 10, Key = 8\nOutput: Inorder Predecessor: 7, Inorder Successor: 9\nExplanation: \n\nExample 2:\nInput: Binary Search Tree: 5 2 7 1 4 6 9 -1 -1 3 -1 -1 -1 8 10, Key = 3\nOutput: Inorder Predecessor: 2, Inorder Successor: 4\nExplanation:"
  ],
  "total_approaches": 3,
  "approaches": [
    {
      "approach_name": "Brute Force",
      "algorithm": "First, we traverse the binary search tree (BST) in an in-order fashion to obtain the sorted sequence of node values. This way, we can directly use the sequence to determine the next node greater than the given key.\nOnce we have the in-order traversal of the BST, we can perform a binary search on this sorted list. The binary search will allow us to efficiently find the first value that is greater than the given key. This value is the in-order successor.\nIf the binary search finds a matching value, we return it as the successor. If not, we continue searching to identify the next value in the sorted sequence that is greater than the key.",
      "time_complexity": "O(N + logN)",
      "space_complexity": "O(N + logN)",
      "time_complexity_details": "Time Complexity: O(N + logN) where N is the number of nodes of the binary search tree. O(N) to traverse all nodes and store them in an inorder array and O(log N) for the binary search.\n\nSpace Complexity: O(N) as an array of size N is used to store the inorder traversal of the binary search tree.",
      "space_complexity_details": "Time Complexity: O(N + logN) where N is the number of nodes of the binary search tree. O(N) to traverse all nodes and store them in an inorder array and O(log N) for the binary search.\n\nSpace Complexity: O(N) as an array of size N is used to store the inorder traversal of the binary search tree.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// define tree node structure\nstruct TreeNode {\n    // value of the node\n    int val;\n    // pointer to the left child\n    TreeNode* left;\n    // pointer to the right child\n    TreeNode* right;\n    // constructor\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\n// solution class\nclass Solution {\npublic:\n    // find inorder successor by building inorder list and binary searching\n    TreeNode* inorderSuccessor(TreeNode* root, TreeNode* p) {\n        // container for inorder traversal\n        vector<int> inorder;\n        // fill inorder list\n        inorderTraversal(root, inorder);\n        // locate index of p->val (or insertion index)\n        int idx = binarySearch(inorder, p->val);\n        // handle edge cases: last element or invalid\n        if (idx == (int)inorder.size() - 1 || idx == -1) {\n            return nullptr;\n        }\n        // return a new node with successor value (mirroring given logic)\n        return new TreeNode(inorder[idx + 1]);\n    }\n\n    // inorder traversal helper\n    void inorderTraversal(TreeNode* root, vector<int>& inorder) {\n        // base case\n        if (root == nullptr) return;\n        // traverse left\n        inorderTraversal(root->left, inorder);\n        // visit node\n        inorder.push_back(root->val);\n        // traverse right\n        inorderTraversal(root->right, inorder);\n    }\n\n    // binary search helper\n    int binarySearch(vector<int>& arr, int target) {\n        // search bounds\n        int left = 0, right = (int)arr.size() - 1;\n        // standard binary search\n        while (left <= right) {\n            // midpoint\n            int mid = left + (right - left) / 2;\n            // found case\n            if (arr[mid] == target) return mid;\n            // move right\n            else if (arr[mid] < target) left = mid + 1;\n            // move left\n            else right = mid - 1;\n        }\n        // not found: return insertion index or -1 if at end\n        return left == (int)arr.size() ? -1 : left;\n    }\n};\n\n// inorder print helper\nvoid printInOrder(TreeNode* root) {\n    // base case\n    if (root == nullptr) return;\n    // left\n    printInOrder(root->left);\n    // node\n    cout << root->val << \" \";\n    // right\n    printInOrder(root->right);\n}\n\n// program entry\nint main() {\n    // construct BST\n    TreeNode* root = new TreeNode(5);\n    root->left = new TreeNode(3);\n    root->right = new TreeNode(6);\n    root->left->left = new TreeNode(2);\n    root->left->right = new TreeNode(4);\n    root->right->right = new TreeNode(7);\n\n    // show BST inorder\n    cout << \"BST: \";\n    printInOrder(root);\n    cout << endl;\n\n    // pick node p (value 4)\n    TreeNode* p = root->left->right;\n\n    // compute successor\n    Solution solution;\n    TreeNode* successor = solution.inorderSuccessor(root, p);\n\n    // print result\n    if (successor != nullptr) {\n        cout << \"Inorder Successor of \" << p->val << \" is: \" << successor->val << endl;\n    } else {\n        cout << \"Inorder Successor of \" << p->val << \" does not exist.\" << endl;\n    }\n\n    // done\n    return 0;\n}",
        "java": "import java.util.*;\n\n// tree node definition\nclass TreeNode {\n    // value of the node\n    int val;\n    // left child reference\n    TreeNode left;\n    // right child reference\n    TreeNode right;\n    // constructor\n    TreeNode(int x) { val = x; }\n}\n\n// solution class\nclass Solution {\n    // find inorder successor by building inorder list and binary searching\n    public TreeNode inorderSuccessor(TreeNode root, TreeNode p) {\n        // container for inorder traversal\n        List<Integer> inorder = new ArrayList<>();\n        // fill inorder list\n        inorderTraversal(root, inorder);\n        // locate index of p.val (or insertion index)\n        int idx = binarySearch(inorder, p.val);\n        // handle edge cases: last element or invalid\n        if (idx == inorder.size() - 1 || idx == -1) {\n            return null;\n        }\n        // return a new node with successor value (mirroring given logic)\n        return new TreeNode(inorder.get(idx + 1));\n    }\n\n    // inorder traversal helper\n    private void inorderTraversal(TreeNode root, List<Integer> inorder) {\n        // base case\n        if (root == null) return;\n        // traverse left\n        inorderTraversal(root.left, inorder);\n        // visit node\n        inorder.add(root.val);\n        // traverse right\n        inorderTraversal(root.right, inorder);\n    }\n\n    // binary search helper\n    private int binarySearch(List<Integer> arr, int target) {\n        // search bounds\n        int left = 0, right = arr.size() - 1;\n        // standard binary search\n        while (left <= right) {\n            // midpoint\n            int mid = left + (right - left) / 2;\n            // found case\n            if (arr.get(mid) == target) return mid;\n            // move right\n            else if (arr.get(mid) < target) left = mid + 1;\n            // move left\n            else right = mid - 1;\n        }\n        // not found: return insertion index or -1 if at end\n        return left == arr.size() ? -1 : left;\n    }\n}\n\n// separate main class\npublic class Main {\n    // inorder print helper\n    static void printInOrder(TreeNode root) {\n        // base case\n        if (root == null) return;\n        // left\n        printInOrder(root.left);\n        // node\n        System.out.print(root.val + \" \");\n        // right\n        printInOrder(root.right);\n    }\n\n    // program entry\n    public static void main(String[] args) {\n        // construct BST\n        TreeNode root = new TreeNode(5);\n        root.left = new TreeNode(3);\n        root.right = new TreeNode(6);\n        root.left.left = new TreeNode(2);\n        root.left.right = new TreeNode(4);\n        root.right.right = new TreeNode(7);\n\n        // show BST inorder\n        System.out.print(\"BST: \");\n        printInOrder(root);\n        System.out.println();\n\n        // pick node p (value 4)\n        TreeNode p = root.left.right;\n\n        // compute successor\n        Solution solution = new Solution();\n        TreeNode successor = solution.inorderSuccessor(root, p);\n\n        // print result\n        if (successor != null) {\n            System.out.println(\"Inorder Successor of \" + p.val + \" is: \" + successor.val);\n        } else {\n            System.out.println(\"Inorder Successor of \" + p.val + \" does not exist.\");\n        }\n    }\n}",
        "python": "# define tree node\nclass TreeNode:\n    # constructor\n    def __init__(self, x: int):\n        # value of the node\n        self.val = x\n        # left child reference\n        self.left = None\n        # right child reference\n        self.right = None\n\n# solution class\nclass Solution:\n    # find inorder successor by building inorder list and binary searching\n    def inorderSuccessor(self, root: TreeNode, p: TreeNode) -> TreeNode | None:\n        # container for inorder traversal\n        inorder: list[int] = []\n        # fill inorder list\n        self.inorderTraversal(root, inorder)\n        # locate index of p.val (or insertion index)\n        idx = self.binarySearch(inorder, p.val)\n        # handle edge cases: last element or invalid\n        if idx == len(inorder) - 1 or idx == -1:\n            return None\n        # return a new node with successor value (mirroring given logic)\n        return TreeNode(inorder[idx + 1])\n\n    # inorder traversal helper\n    def inorderTraversal(self, root: TreeNode | None, inorder: list[int]) -> None:\n        # base case\n        if root is None:\n            return\n        # traverse left\n        self.inorderTraversal(root.left, inorder)\n        # visit node\n        inorder.append(root.val)\n        # traverse right\n        self.inorderTraversal(root.right, inorder)\n\n    # binary search helper\n    def binarySearch(self, arr: list[int], target: int) -> int:\n        # search bounds\n        left, right = 0, len(arr) - 1\n        # standard binary search\n        while left <= right:\n            # midpoint\n            mid = left + (right - left) // 2\n            # found case\n            if arr[mid] == target:\n                return mid\n            # move right\n            elif arr[mid] < target:\n                left = mid + 1\n            # move left\n            else:\n                right = mid - 1\n        # not found: return insertion index or -1 if at end\n        return -1 if left == len(arr) else left\n\n# inorder print helper\ndef print_in_order(root: TreeNode | None) -> None:\n    # base case\n    if root is None:\n        return\n    # left\n    print_in_order(root.left)\n    # node\n    print(root.val, end=\" \")\n    # right\n    print_in_order(root.right)\n\n# program entry\ndef main():\n    # construct BST\n    root = TreeNode(5)\n    root.left = TreeNode(3)\n    root.right = TreeNode(6)\n    root.left.left = TreeNode(2)\n    root.left.right = TreeNode(4)\n    root.right.right = TreeNode(7)\n\n    # show BST inorder\n    print(\"BST: \", end=\"\")\n    print_in_order(root)\n    print()\n\n    # pick node p (value 4)\n    p = root.left.right\n\n    # compute successor\n    solution = Solution()\n    successor = solution.inorderSuccessor(root, p)\n\n    # print result\n    if successor is not None:\n        print(f\"Inorder Successor of {p.val} is: {successor.val}\")\n    else:\n        print(f\"Inorder Successor of {p.val} does not exist.\")\n\n# run main\nif __name__ == \"__main__\":\n    main()",
        "javascript": "// define tree node\nclass TreeNode {\n  // constructor\n  constructor(x) {\n    // value of the node\n    this.val = x;\n    // left child reference\n    this.left = null;\n    // right child reference\n    this.right = null;\n  }\n}\n\n// solution class\nclass Solution {\n  // find inorder successor by building inorder list and binary searching\n  inorderSuccessor(root, p) {\n    // container for inorder traversal\n    const inorder = [];\n    // fill inorder list\n    this.inorderTraversal(root, inorder);\n    // locate index of p.val (or insertion index)\n    const idx = this.binarySearch(inorder, p.val);\n    // handle edge cases: last element or invalid\n    if (idx === inorder.length - 1 || idx === -1) {\n      return null;\n    }\n    // return a new node with successor value (mirroring given logic)\n    return new TreeNode(inorder[idx + 1]);\n  }\n\n  // inorder traversal helper\n  inorderTraversal(root, inorder) {\n    // base case\n    if (root === null) return;\n    // traverse left\n    this.inorderTraversal(root.left, inorder);\n    // visit node\n    inorder.push(root.val);\n    // traverse right\n    this.inorderTraversal(root.right, inorder);\n  }\n\n    // binary search helper\n    binarySearch(arr, target) {\n        // search bounds\n        let left = 0, right = arr.length - 1;\n        // standard binary search\n        while (left <= right) {\n        // midpoint\n        const mid = left + Math.floor((right - left) / 2);\n        // found case\n        if (arr[mid] === target) return mid;\n        // move right\n        else if (arr[mid] < target) left = mid + 1;\n        // move left\n        else right = mid - 1;\n        }\n        // not found: return insertion index or -1 if at end\n        return left === arr.length ? -1 : left;\n    }\n}\n\n// inorder print helper\nfunction printInOrder(root) {\n    // base case\n    if (root === null) return;\n    // left\n    printInOrder(root.left);\n    // node\n    process.stdout.write(root.val + \" \");\n    // right\n    printInOrder(root.right);\n}\n\n// program entry\nfunction main() {\n    // construct BST\n    const root = new TreeNode(5);\n    root.left = new TreeNode(3);\n    root.right = new TreeNode(6);\n    root.left.left = new TreeNode(2);\n    root.left.right = new TreeNode(4);\n    root.right.right = new TreeNode(7);\n\n    // show BST inorder\n    process.stdout.write(\"BST: \");\n    printInOrder(root);\n    console.log();\n\n    // pick node p (value 4)\n    const p = root.left.right;\n\n    // compute successor\n    const solution = new Solution();\n    const successor = solution.inorderSuccessor(root, p);\n\n    // print result\n    if (successor !== null) {\n        console.log(\"Inorder Successor of \" + p.val + \" is: \" + successor.val);\n    } else {\n        console.log(\"Inorder Successor of \" + p.val + \" does not exist.\");\n    }\n}\n\n// run main\nmain();"
      }
    },
    {
      "approach_name": "Better Approach",
      "algorithm": "First, we perform an inorder or Morris traversal of the binary search tree (BST) to visit the nodes in sorted order.\nWhile traversing, we check each node and identify the first node with a value greater than the given key.\nOnce we find the node with the greater value, we know it is the inorder successor of the given key, so we return this node.",
      "time_complexity": "O(N)",
      "space_complexity": "O(N)",
      "time_complexity_details": "Time Complexity: O(N) where N is the number of nodes in the binary search tree. This complexity arises from the fact that we have to traverse all nodes in an inorder fashion to get to the inorder successor.\n\nSpace Complexity: O(1) as no additional data structure or memory allocation is done during the traversal and algorithm. Only a value comparison at each node.",
      "space_complexity_details": "Time Complexity: O(N) where N is the number of nodes in the binary search tree. This complexity arises from the fact that we have to traverse all nodes in an inorder fashion to get to the inorder successor.\n\nSpace Complexity: O(1) as no additional data structure or memory allocation is done during the traversal and algorithm. Only a value comparison at each node.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// define tree node structure\nstruct TreeNode {\n    // value of the node\n    int val;\n    // pointer to the left child\n    TreeNode* left;\n    // pointer to the right child\n    TreeNode* right;\n    // constructor\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\n// solution class\nclass Solution {\npublic:\n    // find inorder successor in BST by tracking candidate\n    TreeNode* inorderSuccessor(TreeNode* root, TreeNode* p) {\n        // initialize successor pointer\n        TreeNode* successor = nullptr;\n        // traverse until null\n        while (root != nullptr) {\n            // move left while updating successor when root > p\n            if (root->val > p->val) {\n                successor = root;\n                root = root->left;\n            }\n            // otherwise move right\n            else {\n                root = root->right;\n            }\n        }\n        // return final successor (or null)\n        return successor;\n    }\n};\n\n// print inorder traversal\nvoid printInOrder(TreeNode* root) {\n    // base case for recursion\n    if (root == nullptr) return;\n    // traverse left subtree\n    printInOrder(root->left);\n    // print current node\n    cout << root->val << \" \";\n    // traverse right subtree\n    printInOrder(root->right);\n}\n\n// program entry\nint main() {\n    // construct BST\n    TreeNode* root = new TreeNode(5);\n    root->left = new TreeNode(3);\n    root->right = new TreeNode(6);\n    root->left->left = new TreeNode(2);\n    root->left->right = new TreeNode(4);\n    root->right->right = new TreeNode(7);\n\n    // print BST inorder\n    cout << \"BST: \";\n    printInOrder(root);\n    cout << endl;\n\n    // choose node p (value 4)\n    TreeNode* p = root->left->right;\n\n    // find successor\n    Solution solution;\n    TreeNode* successor = solution.inorderSuccessor(root, p);\n\n    // print result\n    if (successor != nullptr) {\n        cout << \"Inorder Successor of \" << p->val << \" is: \" << successor->val << endl;\n    } else {\n        cout << \"Inorder Successor of \" << p->val << \" does not exist.\" << endl;\n    }\n\n    // finish\n    return 0;\n}",
        "java": "import java.util.*;\n\n// define tree node structure\nclass TreeNode {\n    // value of the node\n    int val;\n    // left child reference\n    TreeNode left;\n    // right child reference\n    TreeNode right;\n    // constructor\n    TreeNode(int x) { val = x; }\n}\n\n// solution class\nclass Solution {\n    // find inorder successor in BST by tracking candidate\n    public TreeNode inorderSuccessor(TreeNode root, TreeNode p) {\n        // initialize successor reference\n        TreeNode successor = null;\n        // traverse until null\n        while (root != null) {\n            // move left while updating successor when root > p\n            if (root.val > p.val) {\n                successor = root;\n                root = root.left;\n            }\n            // otherwise move right\n            else {\n                root = root.right;\n            }\n        }\n        // return final successor (or null)\n        return successor;\n    }\n}\n\n// separate main class\npublic class Main {\n    // print inorder traversal\n    static void printInOrder(TreeNode root) {\n        // base case for recursion\n        if (root == null) return;\n        // traverse left subtree\n        printInOrder(root.left);\n        // print current node\n        System.out.print(root.val + \" \");\n        // traverse right subtree\n        printInOrder(root.right);\n    }\n\n    // program entry\n    public static void main(String[] args) {\n        // construct BST\n        TreeNode root = new TreeNode(5);\n        root.left = new TreeNode(3);\n        root.right = new TreeNode(6);\n        root.left.left = new TreeNode(2);\n        root.left.right = new TreeNode(4);\n        root.right.right = new TreeNode(7);\n\n        // print BST inorder\n        System.out.print(\"BST: \");\n        printInOrder(root);\n        System.out.println();\n\n        // choose node p (value 4)\n        TreeNode p = root.left.right;\n\n        // find successor\n        Solution solution = new Solution();\n        TreeNode successor = solution.inorderSuccessor(root, p);\n\n        // print result\n        if (successor != null) {\n            System.out.println(\"Inorder Successor of \" + p.val + \" is: \" + successor.val);\n        } else {\n            System.out.println(\"Inorder Successor of \" + p.val + \" does not exist.\");\n        }\n    }\n}",
        "python": "# define tree node structure\nclass TreeNode:\n    # constructor\n    def __init__(self, x: int):\n        # value of the node\n        self.val = x\n        # left child reference\n        self.left = None\n        # right child reference\n        self.right = None\n\n# solution class\nclass Solution:\n    # find inorder successor in BST by tracking candidate\n    def inorderSuccessor(self, root: TreeNode, p: TreeNode) -> TreeNode | None:\n        # initialize successor reference\n        successor = None\n        # traverse until null\n        while root is not None:\n            # move left while updating successor when root > p\n            if root.val > p.val:\n                successor = root\n                root = root.left\n            # otherwise move right\n            else:\n                root = root.right\n        # return final successor (or None)\n        return successor\n\n# print inorder traversal\ndef print_in_order(root: TreeNode | None) -> None:\n    # base case for recursion\n    if root is None:\n        return\n    # traverse left subtree\n    print_in_order(root.left)\n    # print current node\n    print(root.val, end=\" \")\n    # traverse right subtree\n    print_in_order(root.right)\n\n# program entry\ndef main():\n    # construct BST\n    root = TreeNode(5)\n    root.left = TreeNode(3)\n    root.right = TreeNode(6)\n    root.left.left = TreeNode(2)\n    root.left.right = TreeNode(4)\n    root.right.right = TreeNode(7)\n\n    # print BST inorder\n    print(\"BST: \", end=\"\")\n    print_in_order(root)\n    print()\n\n    # choose node p (value 4)\n    p = root.left.right\n\n    # find successor\n    solution = Solution()\n    successor = solution.inorderSuccessor(root, p)\n\n    # print result\n    if successor is not None:\n        print(f\"Inorder Successor of {p.val} is: {successor.val}\")\n    else:\n        print(f\"Inorder Successor of {p.val} does not exist.\")\n\n# run main\nif __name__ == \"__main__\":\n    main()",
        "javascript": "// define tree node structure\nclass TreeNode {\n    // constructor\n    constructor(x) {\n        // value of the node\n        this.val = x;\n        // left child reference\n        this.left = null;\n        // right child reference\n        this.right = null;\n    }\n}\n\n// solution class\nclass Solution {\n    // find inorder successor in BST by tracking candidate\n    inorderSuccessor(root, p) {\n        // initialize successor reference\n        let successor = null;\n        // traverse until null\n        while (root !== null) {\n        // move left while updating successor when root > p\n        if (root.val > p.val) {\n            successor = root;\n            root = root.left;\n        }\n        // otherwise move right\n        else {\n            root = root.right;\n        }\n        }\n        // return final successor (or null)\n        return successor;\n    }\n}\n\n// print inorder traversal\nfunction printInOrder(root) {\n    // base case for recursion\n    if (root === null) return;\n    // traverse left subtree\n    printInOrder(root.left);\n    // print current node\n    process.stdout.write(root.val + \" \");\n    // traverse right subtree\n    printInOrder(root.right);\n}\n\n// program entry\nfunction main() {\n    // construct BST\n    const root = new TreeNode(5);\n    root.left = new TreeNode(3);\n    root.right = new TreeNode(6);\n    root.left.left = new TreeNode(2);\n    root.left.right = new TreeNode(4);\n    root.right.right = new TreeNode(7);\n\n    // print BST inorder\n    process.stdout.write(\"BST: \");\n    printInOrder(root);\n    process.stdout.write(\"\\n\");\n\n    // choose node p (value 4)\n    const p = root.left.right;\n\n    // find successor\n    const solution = new Solution();\n    const successor = solution.inorderSuccessor(root, p);\n\n    // print result\n    if (successor !== null) {\n        console.log(\"Inorder Successor of \" + p.val + \" is: \" + successor.val);\n    } else {\n        console.log(\"Inorder Successor of \" + p.val + \" does not exist.\");\n    }\n}\n\n// run main\nmain();"
      }
    },
    {
      "approach_name": "Optimal Approach",
      "algorithm": "Initialize a variable 'successor' as the maximum integer value.\nTraverse the Binary Search Tree (BST) based on the key value.\nIf the current node value is smaller than the key, move to its right child.\nIf the current node value is greater than the key, update 'successor' to the minimum between the current 'successor' and the current node value, then move to the left child.\nContinue this process until the traversal reaches a null node.",
      "time_complexity": "O(H)",
      "space_complexity": "O(H)",
      "time_complexity_details": "Time Complexity: O(H) where H is the height of the binary search tree as we are traversing along the height of the tree.\n\nSpace Complexity: O(1) as no additional data structure or memory allocation is done during the traversal and algorithm.",
      "space_complexity_details": "Time Complexity: O(H) where H is the height of the binary search tree as we are traversing along the height of the tree.\n\nSpace Complexity: O(1) as no additional data structure or memory allocation is done during the traversal and algorithm.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// define tree node structure\nstruct TreeNode {\n    // value of the node\n    int val;\n    // pointer to left child\n    TreeNode* left;\n    // pointer to right child\n    TreeNode* right;\n    // constructor\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\n// solution class\nclass Solution {\npublic:\n    // find inorder successor iteratively\n    TreeNode* inorderSuccessor(TreeNode* root, TreeNode* p) {\n        // initialize successor\n        TreeNode* successor = nullptr;\n        // traverse until root becomes null\n        while (root != nullptr) {\n            // when p is greater or equal, move right\n            if (p->val >= root->val) {\n                root = root->right;\n            }\n            // otherwise update successor and move left\n            else {\n                successor = root;\n                root = root->left;\n            }\n        }\n        // return successor (may be null)\n        return successor;\n    }\n};\n\n// inorder print helper\nvoid printInOrder(TreeNode* root) {\n    // base case\n    if (root == nullptr) return;\n    // traverse left\n    printInOrder(root->left);\n    // print node\n    cout << root->val << \" \";\n    // traverse right\n    printInOrder(root->right);\n}\n\n// program entry\nint main() {\n    // construct BST\n    TreeNode* root = new TreeNode(5);\n    root->left = new TreeNode(3);\n    root->right = new TreeNode(6);\n    root->left->left = new TreeNode(2);\n    root->left->right = new TreeNode(4);\n    root->right->right = new TreeNode(7);\n\n    // show inorder\n    cout << \"BST: \";\n    printInOrder(root);\n    cout << endl;\n\n    // target node p\n    TreeNode* p = root->left->right;\n\n    // find successor\n    Solution solution;\n    TreeNode* successor = solution.inorderSuccessor(root, p);\n\n    // print result\n    if (successor != nullptr) {\n        cout << \"Inorder Successor of \" << p->val << \" is: \" << successor->val << endl;\n    } else {\n        cout << \"Inorder Successor of \" << p->val << \" does not exist.\" << endl;\n    }\n\n    // exit\n    return 0;\n}",
        "java": "import java.util.*;\n\n// define tree node structure\nclass TreeNode {\n    // value of the node\n    int val;\n    // left child reference\n    TreeNode left;\n    // right child reference\n    TreeNode right;\n    // constructor\n    TreeNode(int x) { val = x; }\n}\n\n// solution class\nclass Solution {\n    // find inorder successor iteratively\n    public TreeNode inorderSuccessor(TreeNode root, TreeNode p) {\n        // initialize successor\n        TreeNode successor = null;\n        // traverse until root becomes null\n        while (root != null) {\n            // when p is greater or equal, move right\n            if (p.val >= root.val) {\n                root = root.right;\n            }\n            // otherwise update successor and move left\n            else {\n                successor = root;\n                root = root.left;\n            }\n        }\n        // return successor (may be null)\n        return successor;\n    }\n}\n\n// separate main class\npublic class Main {\n    // inorder print helper\n    static void printInOrder(TreeNode root) {\n        // base case\n        if (root == null) return;\n        // traverse left\n        printInOrder(root.left);\n        // print node\n        System.out.print(root.val + \" \");\n        // traverse right\n        printInOrder(root.right);\n    }\n\n    // program entry\n    public static void main(String[] args) {\n        // construct BST\n        TreeNode root = new TreeNode(5);\n        root.left = new TreeNode(3);\n        root.right = new TreeNode(6);\n        root.left.left = new TreeNode(2);\n        root.left.right = new TreeNode(4);\n        root.right.right = new TreeNode(7);\n\n        // show inorder\n        System.out.print(\"BST: \");\n        printInOrder(root);\n        System.out.println();\n\n        // target node p\n        TreeNode p = root.left.right;\n\n        // find successor\n        Solution solution = new Solution();\n        TreeNode successor = solution.inorderSuccessor(root, p);\n\n        // print result\n        if (successor != null) {\n            System.out.println(\"Inorder Successor of \" + p.val + \" is: \" + successor.val);\n        } else {\n            System.out.println(\"Inorder Successor of \" + p.val + \" does not exist.\");\n        }\n    }\n}",
        "python": "# define tree node structure\nclass TreeNode:\n    # constructor\n    def __init__(self, x: int):\n        # value of the node\n        self.val = x\n        # left child reference\n        self.left = None\n        # right child reference\n        self.right = None\n\n# solution class\nclass Solution:\n    # find inorder successor iteratively\n    def inorderSuccessor(self, root: 'TreeNode', p: 'TreeNode') -> 'TreeNode | None':\n        # initialize successor\n        successor = None\n        # traverse until root becomes None\n        while root is not None:\n            # when p is greater or equal, move right\n            if p.val >= root.val:\n                root = root.right\n            # otherwise update successor and move left\n            else:\n                successor = root\n                root = root.left\n        # return successor (may be None)\n        return successor\n\n# inorder print helper\ndef print_in_order(root: 'TreeNode | None') -> None:\n    # base case\n    if root is None:\n        return\n    # traverse left\n    print_in_order(root.left)\n    # print node\n    print(root.val, end=\" \")\n    # traverse right\n    print_in_order(root.right)\n\n# program entry\ndef main():\n    # construct BST\n    root = TreeNode(5)\n    root.left = TreeNode(3)\n    root.right = TreeNode(6)\n    root.left.left = TreeNode(2)\n    root.left.right = TreeNode(4)\n    root.right.right = TreeNode(7)\n\n    # show inorder\n    print(\"BST: \", end=\"\")\n    print_in_order(root)\n    print()\n\n    # target node p\n    p = root.left.right\n\n    # find successor\n    solution = Solution()\n    successor = solution.inorderSuccessor(root, p)\n\n    # print result\n    if successor is not None:\n        print(f\"Inorder Successor of {p.val} is: {successor.val}\")\n    else:\n        print(f\"Inorder Successor of {p.val} does not exist.\")\n\n# run main\nif __name__ == \"__main__\":\n    main()",
        "javascript": "// define tree node structure\nclass TreeNode {\n    // constructor\n    constructor(x) {\n        // value of the node\n        this.val = x;\n        // left child reference\n        this.left = null;\n        // right child reference\n        this.right = null;\n    }\n}\n\n// solution class\nclass Solution {\n    // find inorder successor iteratively\n    inorderSuccessor(root, p) {\n        // initialize successor\n        let successor = null;\n        // traverse until root becomes null\n        while (root !== null) {\n        // when p is greater or equal, move right\n        if (p.val >= root.val) {\n            root = root.right;\n        }\n        // otherwise update successor and move left\n        else {\n            successor = root;\n            root = root.left;\n        }\n        }\n        // return successor (may be null)\n        return successor;\n    }\n}\n\n// inorder print helper\nfunction printInOrder(root) {\n    // base case\n    if (root === null) return;\n    // traverse left\n    printInOrder(root.left);\n    // print node\n    process.stdout.write(root.val + \" \");\n    // traverse right\n    printInOrder(root.right);\n}\n\n// program entry\nfunction main() {\n    // construct BST\n    const root = new TreeNode(5);\n    root.left = new TreeNode(3);\n    root.right = new TreeNode(6);\n    root.left.left = new TreeNode(2);\n    root.left.right = new TreeNode(4);\n    root.right.right = new TreeNode(7);\n\n    // show inorder\n    process.stdout.write(\"BST: \");\n    printInOrder(root);\n    process.stdout.write(\"\\n\");\n\n    // target node p\n    const p = root.left.right;\n\n    // find successor\n    const solution = new Solution();\n    const successor = solution.inorderSuccessor(root, p);\n\n    // print result\n    if (successor !== null) {\n        console.log(\"Inorder Successor of \" + p.val + \" is: \" + successor.val);\n    } else {\n        console.log(\"Inorder Successor of \" + p.val + \" does not exist.\");\n    }\n}\n\n// run main\nmain();"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:17Z"
}