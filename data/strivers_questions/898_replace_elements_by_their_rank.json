{
  "problem_id": "898",
  "problem_name": "Replace Elements by Their Rank",
  "step_no": 11,
  "step_name": "Heaps [Learning, Medium, Hard Problems]",
  "substep_no": 2,
  "substep_name": "Medium Problems",
  "difficulty": "Easy",
  "youtube_url": null,
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/replace-elements-by-its-rank-in-the-array/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/replace-elements-by-their-rank",
  "problem_statement": "Given an array of N integers, the task is to replace each element of the array by its rank in the array.",
  "examples": [
    "Input: 20 15 26 2 98 6\nOutput: 4 3 5 1 6 2\nExplanation: When sorted,the array is 2,6,15,20,26,98. So the rank of 2 is 1,rank of 6 is 2,rank of 15 is 3 and so.\n\nInput: 1 5 8 15 8 25 9\nOutput: 1 2 3 5 3 6 4\nExplanation :When sorted,the array is 1,5,8,8,9,15,25. So the rank of 1 is 1,rank of 5 is 2,rank of 8 is 3 and so."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Brute Force Approach",
      "algorithm": "We are aiming to assign a rank to each element in the array based on how many unique elements are smaller than it. For each element, we iterate through the rest of the array, collect all smaller elements, and count the number of distinct ones.\n\n Initialize an empty list to store final ranks Loop over the array from index 0 to n-1 For each element: Initialize an empty set Loop over the array again from 0 to n-1 If another element is less than the current, insert into the set (to keep only unique ones) The size of the set gives the number of unique elements smaller than current Rank = size of set + 1 Append rank to result Return or print the rank list",
      "time_complexity": "O(N²)",
      "space_complexity": "O(N²)",
      "time_complexity_details": "Time Complexity: O(N²),Because for each element (N times), we iterate the full array again (N), making it N² overall. Space Complexity: O(N), Each iteration uses a set for unique elements which in worst case can hold up to N elements. Also, final result array uses O(N).",
      "space_complexity_details": "Time Complexity: O(N²),Because for each element (N times), we iterate the full array again (N), making it N² overall. Space Complexity: O(N), Each iteration uses a set for unique elements which in worst case can hold up to N elements. Also, final result array uses O(N).",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to replace elements with their rank\n    vector<int> replaceWithRank(vector<int>& arr) {\n        // Final answer vector\n        vector<int> rankArr;\n\n        // Loop through each element of the array\n        for (int i = 0; i < arr.size(); i++) {\n            // Set to store unique elements less than arr[i]\n            unordered_set<int> smaller;\n\n            // Loop again to compare with every other element\n            for (int j = 0; j < arr.size(); j++) {\n                if (arr[j] < arr[i]) {\n                    // Add to set if element is smaller\n                    smaller.insert(arr[j]);\n                }\n            }\n\n            // Rank is number of unique smaller elements + 1\n            int rank = smaller.size() + 1;\n\n            // Store the rank\n            rankArr.push_back(rank);\n        }\n\n        return rankArr;\n    }\n};\n\n// Driver code\nint main() {\n    vector<int> arr = {20, 15, 26, 2, 98, 6};\n    Solution sol;\n    vector<int> result = sol.replaceWithRank(arr);\n\n    for (int r : result) {\n        cout << r << \" \";\n    }\n    cout << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to replace elements with their rank\n    public List<Integer> replaceWithRank(int[] arr) {\n        List<Integer> rankList = new ArrayList<>();\n\n        // Loop through each element\n        for (int i = 0; i < arr.length; i++) {\n            // Set to store unique elements smaller than current\n            Set<Integer> smaller = new HashSet<>();\n\n            // Compare with all elements\n            for (int j = 0; j < arr.length; j++) {\n                if (arr[j] < arr[i]) {\n                    smaller.add(arr[j]);\n                }\n            }\n\n            // Rank = count of unique smaller elements + 1\n            int rank = smaller.size() + 1;\n            rankList.add(rank);\n        }\n\n        return rankList;\n    }\n}\n\n// Driver code\nclass Main {\n    public static void main(String[] args) {\n        int[] arr = {20, 15, 26, 2, 98, 6};\n        Solution sol = new Solution();\n        List<Integer> result = sol.replaceWithRank(arr);\n        for (int r : result) {\n            System.out.print(r + \" \");\n        }\n    }\n}",
        "python": "class Solution:\n    # Function to replace elements with their rank\n    def replaceWithRank(self, arr):\n        result = []\n\n        # Loop through each element\n        for i in range(len(arr)):\n            # Set to hold unique smaller elements\n            smaller = set()\n\n            # Compare with every other element\n            for j in range(len(arr)):\n                if arr[j] < arr[i]:\n                    smaller.add(arr[j])\n\n            # Rank = unique smaller count + 1\n            rank = len(smaller) + 1\n            result.append(rank)\n\n        return result\n\n# Driver code\narr = [20, 15, 26, 2, 98, 6]\nsol = Solution()\nprint(sol.replaceWithRank(arr))",
        "javascript": "class Solution {\n    // Function to replace elements with their rank\n    replaceWithRank(arr) {\n        const result = [];\n\n        // Loop through each element\n        for (let i = 0; i < arr.length; i++) {\n            const smaller = new Set();\n\n            // Compare with all other elements\n            for (let j = 0; j < arr.length; j++) {\n                if (arr[j] < arr[i]) {\n                    smaller.add(arr[j]);\n                }\n            }\n\n            // Rank = number of unique smaller + 1\n            result.push(smaller.size + 1);\n        }\n\n        return result;\n    }\n}\n\n// Driver code\nconst arr = [20, 15, 26, 2, 98, 6];\nconst sol = new Solution();\nconsole.log(sol.replaceWithRank(arr));"
      }
    },
    {
      "approach_name": "Optimal Approach",
      "algorithm": "We need to assign a rank to each element of the array based on how small or large it is compared to others. The smallest number gets rank 1, the next smallest gets rank 2, and so on.\nDuplicate elements should get the same rank.\n\n Make a copy of the array to avoid modifying the original. Sort the copied array in ascending order. Use a map or dictionary to assign rank starting from 1, skipping duplicates. Loop through the original array and replace each element with its rank using the map.",
      "time_complexity": "O(N log N)",
      "space_complexity": "O(N log N)",
      "time_complexity_details": "Time Complexity: O(N log N), We sort the array which takes O(N log N), then loop over the array multiple times all O(N) operations. Space Complexity: O(N), We store an extra hash map for ranks and copy of the array.",
      "space_complexity_details": "Time Complexity: O(N log N), We sort the array which takes O(N log N), then loop over the array multiple times all O(N) operations. Space Complexity: O(N), We store an extra hash map for ranks and copy of the array.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to replace elements by their rank in the array\n    vector<int> replaceWithRank(vector<int>& arr) {\n        // Copy the original array for sorting\n        vector<int> sortedArr = arr;\n\n        // Sort the array\n        sort(sortedArr.begin(), sortedArr.end());\n\n        // Create a map to store rank of each unique number\n        unordered_map<int, int> rankMap;\n\n        // Initialize rank counter\n        int rank = 1;\n\n        // Assign rank to each unique element in sorted order\n        for (int num : sortedArr) {\n            // If this number is not already assigned a rank\n            if (rankMap.find(num) == rankMap.end()) {\n                rankMap[num] = rank;\n                rank++;\n            }\n        }\n\n        // Replace each element in original array with its rank\n        vector<int> result;\n        for (int num : arr) {\n            result.push_back(rankMap[num]);\n        }\n\n        return result;\n    }\n};\n\n// Driver code\nint main() {\n    Solution obj;\n    vector<int> arr = {1, 5, 8, 15, 8, 25, 9};\n\n    vector<int> res = obj.replaceWithRank(arr);\n\n    for (int x : res) {\n        cout << x << \" \";\n    }\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to replace elements by their rank in the array\n    public int[] replaceWithRank(int[] arr) {\n        // Create a copy of the original array\n        int[] sortedArr = arr.clone();\n\n        // Sort the copied array\n        Arrays.sort(sortedArr);\n\n        // Map to store rank of each unique number\n        HashMap<Integer, Integer> rankMap = new HashMap<>();\n\n        int rank = 1;\n\n        // Assign rank to each unique number\n        for (int num : sortedArr) {\n            if (!rankMap.containsKey(num)) {\n                rankMap.put(num, rank);\n                rank++;\n            }\n        }\n\n        // Replace elements in original array with their ranks\n        int[] result = new int[arr.length];\n        for (int i = 0; i < arr.length; i++) {\n            result[i] = rankMap.get(arr[i]);\n        }\n\n        return result;\n    }\n}\n\n// Driver code\nclass Main {\n    public static void main(String[] args) {\n        Solution obj = new Solution();\n        int[] arr = {1, 5, 8, 15, 8, 25, 9};\n\n        int[] res = obj.replaceWithRank(arr);\n\n        for (int x : res) {\n            System.out.print(x + \" \");\n        }\n    }\n}",
        "python": "class Solution:\n    # Function to replace elements by their rank in the array\n    def replaceWithRank(self, arr):\n        # Make a sorted copy of the array\n        sorted_arr = sorted(arr)\n\n        # Create a dictionary to hold ranks\n        rank_map = {}\n\n        # Initialize rank counter\n        rank = 1\n\n        # Assign ranks to unique elements\n        for num in sorted_arr:\n            if num not in rank_map:\n                rank_map[num] = rank\n                rank += 1\n\n        # Build the result using rank map\n        result = [rank_map[num] for num in arr]\n        return result\n\n# Driver code\nobj = Solution()\narr = [1, 5, 8, 15, 8, 25, 9]\nres = obj.replaceWithRank(arr)\nprint(*res)",
        "javascript": "class Solution {\n    // Function to replace elements by their rank in the array\n    replaceWithRank(arr) {\n        // Make a sorted copy of the array\n        let sortedArr = [...arr].sort((a, b) => a - b);\n\n        // Create a map to store rank of each unique number\n        let rankMap = new Map();\n        let rank = 1;\n\n        // Assign ranks to unique elements\n        for (let num of sortedArr) {\n            if (!rankMap.has(num)) {\n                rankMap.set(num, rank);\n                rank++;\n            }\n        }\n\n        // Replace original array values with ranks\n        let result = arr.map(num => rankMap.get(num));\n        return result;\n    }\n}\n\n// Driver code\nlet obj = new Solution();\nlet arr = [1, 5, 8, 15, 8, 25, 9];\nlet res = obj.replaceWithRank(arr);\nconsole.log(res.join(\" \"));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:13Z"
}