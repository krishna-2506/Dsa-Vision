{
  "problem_id": "780",
  "problem_name": "Number of Longest Increasing Subsequences",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 7,
  "substep_name": "DP on LIS",
  "difficulty": "Medium",
  "youtube_url": "https://youtu.be/cKVl1TFdNXg",
  "leetcode_url": "https://leetcode.com/problems/number-of-longest-increasing-subsequence/",
  "article_url": "https://takeuforward.org/data-structure/number-of-longest-increasing-subsequences-dp-47/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/number-of-longest-increasing-subsequences",
  "problem_statement": "Problem Description: Given an integer array nums, find the number of Longest Increasing Subsequences (LIS) in the array.The Longest Increasing Subsequence (LIS) is the longest subsequence where all elements are in strictly increasing order.\n\nPrerequisite:Longest increasing subsequence, Printing Longest Increasing subsequence",
  "examples": [
    "Input: nums = [1, 3, 5, 4, 7]\nOutput: 2\nExplanation: There are two LIS of length 4: [1, 3, 4, 7] and [1, 3, 5, 7].\n\nInput: nums = [2, 2, 2, 2, 2]\nOutput: 5 \nExplanation: All elements are the same, so every single element can form an LIS of length 1. There are 5 such subsequences."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "In the normal LIS problem, we keep track of the length of the longest increasing subsequence ending at each element. However, when counting the total number of LIS, we also need to store how many such sequences of that length end at that element. As we iterate through the array, if we find a longer subsequence for a position, we update its length and replace the count with the number of ways to form that new longer subsequence. If we instead find another subsequence of the same maximum length ending at that position, we add to the existing count to account for the new path. In the end, we sum the counts of all elements whose LIS length matches the overall maximum length, giving us the total number of longest increasing subsequences in the array.\n\nStart by preparing two separate lists, one to store the lengths of the increasing subsequences ending at each position, and another to store how many such subsequences exist for that position.\nInitially, set the length at each position to 1 (since a single element itself is a valid subsequence) and the count at each position to 1 (since there is at least one such subsequence of length 1).\nFor each position in the sequence, compare it with all previous positions. If the previous element is smaller and using it leads to a longer subsequence, update the length for the current position and set its count equal to the count from that previous position.\nIf the previous element is smaller and using it leads to a subsequence of the same maximum length as already stored for the current position, add the count from the previous position to the current position’s count.\nAfter processing all positions, find the largest length value. This is the length of the longest increasing subsequence.\nSum up the counts of all positions whose length matches the maximum length. This total represents the number of longest increasing subsequences.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Class containing the method to find the count of Longest Increasing Subsequences\nclass Solution {\npublic:\n    // Method to calculate the count of LIS\n    int findNumberOfLIS(vector<int>& arr) {\n        // Store the size of the input sequence\n        int n = arr.size();\n\n        // Store the length of the increasing subsequence ending at each position\n        vector<int> dp(n, 1);\n\n        // Store the count of increasing subsequences ending at each position\n        vector<int> ct(n, 1);\n\n        // Track the maximum subsequence length found so far\n        int maxi = 1;\n\n        // Loop through each position in the sequence\n        for (int i = 0; i < n; i++) {\n            // Compare the current position with all previous positions\n            for (int j = 0; j < i; j++) {\n                // If an increasing sequence is found that is longer than the current best for this position\n                if (arr[j] < arr[i] && dp[j] + 1 > dp[i]) {\n                    // Update the best subsequence length for this position\n                    dp[i] = dp[j] + 1;\n\n                    // Reset the count to match the source subsequence count\n                    ct[i] = ct[j];\n                }\n                // If an increasing sequence of the same best length is found\n                else if (arr[j] < arr[i] && dp[j] + 1 == dp[i]) {\n                    // Add the count of subsequences from the previous position\n                    ct[i] = ct[i] + ct[j];\n                }\n            }\n            // Update the global maximum subsequence length if needed\n            maxi = max(maxi, dp[i]);\n        }\n\n        // Store the total number of subsequences of maximum length\n        int countLIS = 0;\n\n        // Sum the counts of all positions where the length equals the maximum length\n        for (int i = 0; i < n; i++) {\n            if (dp[i] == maxi) {\n                countLIS += ct[i];\n            }\n        }\n\n        // Return the total number of longest increasing subsequences\n        return countLIS;\n    }\n};\n\nint main() {\n    // Define the input sequence\n    vector<int> arr = {1, 5, 4, 3, 2, 6, 7, 2};\n\n    // Create an object of Solution class\n    Solution sol;\n\n    // Output the total count of longest increasing subsequences\n    cout << \"The count of Longest Increasing Subsequences (LIS) is \" \n         << sol.findNumberOfLIS(arr) << endl;\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Method to calculate the count of LIS\n    public int findNumberOfLIS(int[] arr) {\n        // Store the size of the input sequence\n        int n = arr.length;\n\n        // Store the length of the increasing subsequence ending at each position\n        int[] dp = new int[n];\n        Arrays.fill(dp, 1);\n\n        // Store the count of increasing subsequences ending at each position\n        int[] ct = new int[n];\n        Arrays.fill(ct, 1);\n\n        // Track the maximum subsequence length found so far\n        int maxi = 1;\n\n        // Loop through each position in the sequence\n        for (int i = 0; i < n; i++) {\n            // Compare the current position with all previous positions\n            for (int j = 0; j < i; j++) {\n                // If an increasing sequence is found that is longer than the current best for this position\n                if (arr[j] < arr[i] && dp[j] + 1 > dp[i]) {\n                    // Update the best subsequence length for this position\n                    dp[i] = dp[j] + 1;\n\n                    // Reset the count to match the source subsequence count\n                    ct[i] = ct[j];\n                }\n                // If an increasing sequence of the same best length is found\n                else if (arr[j] < arr[i] && dp[j] + 1 == dp[i]) {\n                    // Add the count of subsequences from the previous position\n                    ct[i] = ct[i] + ct[j];\n                }\n            }\n            // Update the global maximum subsequence length if needed\n            maxi = Math.max(maxi, dp[i]);\n        }\n\n        // Store the total number of subsequences of maximum length\n        int countLIS = 0;\n\n        // Sum the counts of all positions where the length equals the maximum length\n        for (int i = 0; i < n; i++) {\n            if (dp[i] == maxi) {\n                countLIS += ct[i];\n            }\n        }\n\n        // Return the total number of longest increasing subsequences\n        return countLIS;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Define the input sequence\n        int[] arr = {1, 5, 4, 3, 2, 6, 7, 2};\n\n        // Create an object of Solution class\n        Solution sol = new Solution();\n\n        // Output the total count of longest increasing subsequences\n        System.out.println(\"The count of Longest Increasing Subsequences (LIS) is \" \n                           + sol.findNumberOfLIS(arr));\n    }\n}",
        "python": "class Solution:\n    # Method to calculate the count of LIS\n    def findNumberOfLIS(self, arr):\n        # Store the size of the input sequence\n        n = len(arr)\n\n        # Store the length of the increasing subsequence ending at each position\n        dp = [1] * n\n\n        # Store the count of increasing subsequences ending at each position\n        ct = [1] * n\n\n        # Track the maximum subsequence length found so far\n        maxi = 1\n\n        # Loop through each position in the sequence\n        for i in range(n):\n            # Compare the current position with all previous positions\n            for j in range(i):\n                # If an increasing sequence is found that is longer than the current best for this position\n                if arr[j] < arr[i] and dp[j] + 1 > dp[i]:\n                    # Update the best subsequence length for this position\n                    dp[i] = dp[j] + 1\n\n                    # Reset the count to match the source subsequence count\n                    ct[i] = ct[j]\n                # If an increasing sequence of the same best length is found\n                elif arr[j] < arr[i] and dp[j] + 1 == dp[i]:\n                    # Add the count of subsequences from the previous position\n                    ct[i] += ct[j]\n\n            # Update the global maximum subsequence length if needed\n            maxi = max(maxi, dp[i])\n\n        # Store the total number of subsequences of maximum length\n        countLIS = sum(ct[i] for i in range(n) if dp[i] == maxi)\n\n        # Return the total number of longest increasing subsequences\n        return countLIS\n\n\nif __name__ == \"__main__\":\n    # Define the input sequence\n    arr = [1, 5, 4, 3, 2, 6, 7, 2]\n\n    # Create an object of Solution class\n    sol = Solution()\n\n    # Output the total count of longest increasing subsequences\n    print(\"The count of Longest Increasing Subsequences (LIS) is\", sol.findNumberOfLIS(arr))",
        "javascript": "class Solution {\n    // Method to calculate the count of LIS\n    findNumberOfLIS(arr) {\n        // Store the size of the input sequence\n        let n = arr.length;\n\n        // Store the length of the increasing subsequence ending at each position\n        let dp = new Array(n).fill(1);\n\n        // Store the count of increasing subsequences ending at each position\n        let ct = new Array(n).fill(1);\n\n        // Track the maximum subsequence length found so far\n        let maxi = 1;\n\n        // Loop through each position in the sequence\n        for (let i = 0; i < n; i++) {\n            // Compare the current position with all previous positions\n            for (let j = 0; j < i; j++) {\n                // If an increasing sequence is found that is longer than the current best for this position\n                if (arr[j] < arr[i] && dp[j] + 1 > dp[i]) {\n                    // Update the best subsequence length for this position\n                    dp[i] = dp[j] + 1;\n\n                    // Reset the count to match the source subsequence count\n                    ct[i] = ct[j];\n                }\n                // If an increasing sequence of the same best length is found\n                else if (arr[j] < arr[i] && dp[j] + 1 == dp[i]) {\n                    // Add the count of subsequences from the previous position\n                    ct[i] += ct[j];\n                }\n            }\n            // Update the global maximum subsequence length if needed\n            maxi = Math.max(maxi, dp[i]);\n        }\n\n        // Store the total number of subsequences of maximum length\n        let countLIS = 0;\n\n        // Sum the counts of all positions where the length equals the maximum length\n        for (let i = 0; i < n; i++) {\n            if (dp[i] === maxi) {\n                countLIS += ct[i];\n            }\n        }\n\n        // Return the total number of longest increasing subsequences\n        return countLIS;\n    }\n}\n\n// Define the input sequence\nlet arr = [1, 5, 4, 3, 2, 6, 7, 2];\n\n// Create an object of Solution class\nlet sol = new Solution();\n\n// Output the total count of longest increasing subsequences\nconsole.log(\"The count of Longest Increasing Subsequences (LIS) is \" + sol.findNumberOfLIS(arr));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:25Z"
}