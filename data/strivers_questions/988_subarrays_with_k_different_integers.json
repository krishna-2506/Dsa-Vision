{
  "problem_id": "988",
  "problem_name": "Subarrays with K Different Integers",
  "step_no": 10,
  "step_name": "Sliding Window & Two Pointer Combined Problems",
  "substep_no": 2,
  "substep_name": "Hard Problems",
  "difficulty": "Medium",
  "youtube_url": "https://youtu.be/7wYGbV_LsX4?si=KWa48RgLDCvdNqRb",
  "leetcode_url": "https://leetcode.com/problems/subarrays-with-k-different-integers/",
  "article_url": "https://takeuforward.org/data-structure/subarray-with-k-different-integers",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/subarrays-with-k-different-integers",
  "problem_statement": "You are given an integer array nums and an integer k.\nReturn the number of good subarrays of nums.",
  "examples": [
    "Input: nums = [1, 2, 1, 2, 3], k = 2 \nOutput: 7\nExplanation: The 7 subarrays with exactly 2 different integers are:\n[1,2], [2,1], [1,2], [2,3], [1,2,1], [2,1,2], [1,2,1,2]\nInput: nums = [1, 2, 1, 3, 4], k = 3 \nOutput: 3\nExplanation: The 3 subarrays with exactly 3 different integers are: \n[1,2,1,3], [2,1,3], [1,3,4]"
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Brute force Approach",
      "algorithm": "We can generate all possible subarrays and count how many distinct elements each subarray has. If a subarray has exactly K distinct integers, it is valid. But this approach is inefficient because we recalculate frequencies again and again.\n Loop through all starting indices of the array For each start, loop through all ending indices till end of array For every subarray from current start to end, count the number of distinct integers using a frequency map If the number of distinct integers equals the required value, increment the result count Return the total count after all iterations",
      "time_complexity": "O(N²*K)",
      "space_complexity": "O(N²*K)",
      "time_complexity_details": "Time Complexity:O(N²*K) ,We check all possible subarrays by iterating over all start and end indices. For each subarray, we count the number of distinct integers using a HashSet or frequency map, which can take up to O(K) time per check. So overall it becomes O(N²*K) where N is the size of the array and K is the number of unique elements allowed.\nSpace Complexity:O(K) ,For each subarray, we maintain a HashSet or HashMap to store the distinct elements in it. In the worst case, this set can grow to size K.",
      "space_complexity_details": "Time Complexity:O(N²*K) ,We check all possible subarrays by iterating over all start and end indices. For each subarray, we count the number of distinct integers using a HashSet or frequency map, which can take up to O(K) time per check. So overall it becomes O(N²*K) where N is the size of the array and K is the number of unique elements allowed.\nSpace Complexity:O(K) ,For each subarray, we maintain a HashSet or HashMap to store the distinct elements in it. In the worst case, this set can grow to size K.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to count subarrays with exactly K distinct integers\n    int subarraysWithKDistinct(vector<int>& nums, int k) {\n        // Get the size of the array\n        int n = nums.size();\n\n        // Variable to store the final result\n        int count = 0;\n\n        // Loop through all possible starting indices\n        for (int i = 0; i < n; i++) {\n\n            // Map to keep track of frequency of elements\n            unordered_map<int, int> freq;\n\n            // Loop through all possible ending indices\n            for (int j = i; j < n; j++) {\n\n                // Increment frequency of current element\n                freq[nums[j]]++;\n\n                // If the number of distinct integers equals k, increment count\n                if (freq.size() == k)\n                    count++;\n\n                // If it exceeds k, break out\n                if (freq.size() > k)\n                    break;\n            }\n        }\n\n        // Return the result\n        return count;\n    }\n};\n\n// Driver code\nint main() {\n    Solution obj;\n    vector<int> nums = {1,2,1,2,3};\n    int k = 2;\n    cout << obj.subarraysWithKDistinct(nums, k);\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to count subarrays with exactly K distinct integers\n    public int subarraysWithKDistinct(int[] nums, int k) {\n        // Store the total count of valid subarrays\n        int count = 0;\n\n        // Iterate through all possible starting points\n        for (int i = 0; i < nums.length; i++) {\n\n            // Use a hashmap to store frequency of elements\n            Map<Integer, Integer> map = new HashMap<>();\n\n            // Iterate through all possible end points\n            for (int j = i; j < nums.length; j++) {\n\n                // Update the frequency of the current number\n                map.put(nums[j], map.getOrDefault(nums[j], 0) + 1);\n\n                // If size equals k, it's a valid subarray\n                if (map.size() == k)\n                    count++;\n\n                // If more than k distinct, break early\n                if (map.size() > k)\n                    break;\n            }\n        }\n\n        // Return total valid subarrays\n        return count;\n    }\n}\n\n// Driver class\nclass Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] nums = {1, 2, 1, 2, 3};\n        int k = 2;\n        System.out.println(sol.subarraysWithKDistinct(nums, k));\n    }\n}",
        "python": "class Solution:\n    # Function to count subarrays with exactly K distinct integers\n    def subarraysWithKDistinct(self, nums, k):\n        # Get the length of the array\n        n = len(nums)\n\n        # Initialize the count\n        count = 0\n\n        # Iterate through all possible starting indices\n        for i in range(n):\n\n            # Dictionary to store frequency of elements\n            freq = {}\n\n            # Iterate through all possible end indices\n            for j in range(i, n):\n\n                # Increase frequency of current element\n                freq[nums[j]] = freq.get(nums[j], 0) + 1\n\n                # If we have exactly k distinct integers, increment result\n                if len(freq) == k:\n                    count += 1\n\n                # If we have more than k, stop processing further\n                if len(freq) > k:\n                    break\n\n        # Return the final result\n        return count\n\n# Driver code\nsol = Solution()\nprint(sol.subarraysWithKDistinct([1, 2, 1, 2, 3], 2))",
        "javascript": "class Solution {\n    // Function to count subarrays with exactly K distinct integers\n    subarraysWithKDistinct(nums, k) {\n        // Store the final count of valid subarrays\n        let count = 0;\n\n        // Loop through all starting indices\n        for (let i = 0; i < nums.length; i++) {\n\n            // Use a map to store frequency of elements\n            let freq = new Map();\n\n            // Loop through all end indices\n            for (let j = i; j < nums.length; j++) {\n\n                // Update the frequency of current element\n                freq.set(nums[j], (freq.get(nums[j]) || 0) + 1);\n\n                // If number of distinct elements is equal to k, increment count\n                if (freq.size === k)\n                    count++;\n\n                // If more than k, exit early\n                if (freq.size > k)\n                    break;\n            }\n        }\n\n        // Return total valid subarrays\n        return count;\n    }\n}\n\n// Driver code\nconst sol = new Solution();\nconsole.log(sol.subarraysWithKDistinct([1, 2, 1, 2, 3], 2));"
      }
    },
    {
      "approach_name": "Optimal Approach",
      "algorithm": "To solve this problem optimally, we utilize the concept of sliding windows to avoid brute force enumeration of all subarrays. The core idea is that calculating the number of subarrays with exactly K distinct integers can be derived by computing: countExactlyK = countAtMostK(K) - countAtMostK(K - 1) This works because countAtMostK(K) gives the number of\n subarrays with at most K distinct integers, and countAtMostK(K - 1) gives the number of subarrays with at most (K-1) distinct integers. Their difference gives the number of subarrays with exactly K distinct integers. The reason this is efficient is because countAtMostK can be computed using the sliding window approach in linear time. We maintain a dynamic window [left, right] such that the number of distinct elements inside it is at most K. As we move the right pointer and extend the window, we shrink the left pointer whenever the constraint is violated, and count all valid subarrays ending at the current right. This method efficiently avoids checking all subarrays explicitly and leverages properties of inclusion-exclusion over ranges with at most K and K-1 elements. Define a helper function that calculates the number of subarrays with at most K distinct elements using a sliding window. Initialize two pointers to represent the sliding window boundaries. Maintain a frequency map to track the count of each number in the window. Expand the window to the right by iterating over the array. If the current element is new to the window (i.e., not in the frequency map or has zero count), decrement the K counter. While K becomes negative (i.e., more than K distinct elements), shrink the window from the left until the constraint is restored. At each step, the number of valid subarrays ending at the current right index is (right - left + 1). Accumulate this count. Once you have the count for at most K and at most K-1, subtract to get the final result.",
      "time_complexity": "O(N)",
      "space_complexity": "O(N)",
      "time_complexity_details": "Time Complexity:O(N) ,where n is the length of the array. Both calls to atMostK() are linear.\nSpace Complexity:O(K) ,where k is the number of distinct elements in the current window. We use a hash map to store frequency counts, which in the worst case could grow to the number of unique elements in the array.",
      "space_complexity_details": "Time Complexity:O(N) ,where n is the length of the array. Both calls to atMostK() are linear.\nSpace Complexity:O(K) ,where k is the number of distinct elements in the current window. We use a hash map to store frequency counts, which in the worst case could grow to the number of unique elements in the array.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Helper function to count subarrays with at most K distinct integers\n    int atMostK(vector<int>& nums, int K) {\n        unordered_map<int, int> freq;\n        int left = 0, count = 0;\n\n        // Traverse the array with right pointer\n        for (int right = 0; right < nums.size(); right++) {\n            // If it's a new unique element, decrease K\n            if (freq[nums[right]] == 0) {\n                K--;\n            }\n\n            // Increment frequency of current element\n            freq[nums[right]]++;\n\n            // Shrink the window if distinct count > K\n            while (K < 0) {\n                freq[nums[left]]--;\n                if (freq[nums[left]] == 0) {\n                    K++;\n                }\n                left++;\n            }\n\n            // Count all subarrays ending at current right\n            count += (right - left + 1);\n        }\n\n        return count;\n    }\n\n    // Main function to return number of subarrays with exactly K distinct integers\n    int subarraysWithKDistinct(vector<int>& nums, int k) {\n        return atMostK(nums, k) - atMostK(nums, k - 1);\n    }\n};\n\n// Driver code\nint main() {\n    Solution sol;\n    vector<int> nums = {1, 2, 1, 2, 3};\n    int k = 2;\n    cout << sol.subarraysWithKDistinct(nums, k) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\npublic class Solution {\n    // Helper function to count subarrays with at most K distinct integers\n    public int atMostK(int[] nums, int K) {\n        Map<Integer, Integer> freq = new HashMap<>();\n        int left = 0, count = 0;\n\n        // Traverse the array with right pointer\n        for (int right = 0; right < nums.length; right++) {\n            freq.put(nums[right], freq.getOrDefault(nums[right], 0) + 1);\n            if (freq.get(nums[right]) == 1) {\n                K--;\n            }\n\n            // Shrink the window if K becomes negative\n            while (K < 0) {\n                freq.put(nums[left], freq.get(nums[left]) - 1);\n                if (freq.get(nums[left]) == 0) {\n                    K++;\n                }\n                left++;\n            }\n\n            count += (right - left + 1);\n        }\n\n        return count;\n    }\n\n    // Main function to return number of subarrays with exactly K distinct integers\n    public int subarraysWithKDistinct(int[] nums, int k) {\n        return atMostK(nums, k) - atMostK(nums, k - 1);\n    }\n\n    // Driver code\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] nums = {1, 2, 1, 2, 3};\n        int k = 2;\n        System.out.println(sol.subarraysWithKDistinct(nums, k));\n    }\n}",
        "python": "class Solution:\n    # Helper function to count subarrays with at most K distinct integers\n    def atMostK(self, nums, K):\n        freq = {}\n        left = 0\n        count = 0\n\n        # Traverse the array with right pointer\n        for right in range(len(nums)):\n            if nums[right] not in freq or freq[nums[right]] == 0:\n                K -= 1\n            freq[nums[right]] = freq.get(nums[right], 0) + 1\n\n            # Shrink the window if K becomes negative\n            while K < 0:\n                freq[nums[left]] -= 1\n                if freq[nums[left]] == 0:\n                    K += 1\n                left += 1\n\n            count += (right - left + 1)\n\n        return count\n\n    # Main function to return number of subarrays with exactly K distinct integers\n    def subarraysWithKDistinct(self, nums, k):\n        return self.atMostK(nums, k) - self.atMostK(nums, k - 1)\n\n# Driver code\nsol = Solution()\nnums = [1, 2, 1, 2, 3]\nk = 2\nprint(sol.subarraysWithKDistinct(nums, k))",
        "javascript": "var subarraysWithKDistinct = function(nums, k) {\n    // Helper function to count subarrays with at most K distinct integers\n    const atMostK = (K) => {\n        let freq = new Map();\n        let left = 0;\n        let count = 0;\n\n        // Traverse the array with right pointer\n        for (let right = 0; right < nums.length; right++) {\n            if (!freq.has(nums[right]) || freq.get(nums[right]) === 0) {\n                K--;\n            }\n\n            freq.set(nums[right], (freq.get(nums[right]) || 0) + 1);\n\n            // Shrink the window if K becomes negative\n            while (K < 0) {\n                freq.set(nums[left], freq.get(nums[left]) - 1);\n                if (freq.get(nums[left]) === 0) {\n                    K++;\n                }\n                left++;\n            }\n\n            count += (right - left + 1);\n        }\n\n        return count;\n    };\n\n    // Main result is difference of atMostK and atMostK-1\n    return atMostK(k) - atMostK(k - 1);\n};\n\n// Driver code\nlet nums = [1, 2, 1, 2, 3];\nlet k = 2;\nconsole.log(subarraysWithKDistinct(nums, k));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:12Z"
}