{
  "problem_id": "327",
  "problem_name": "Matrix chain multiplication",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 8,
  "substep_name": "MCM DP | Partition DP",
  "difficulty": "Hard",
  "youtube_url": "https://youtu.be/vRVfmbCFW7Y",
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/dynamic-programming/matrix-chain-multiplication-dp-48/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/matrix-chain-multiplication",
  "problem_statement": "Given a chain of matrices A1,..., An denoted by an array of size n+1, find out the minimum number of operations to multiply these n matrices .",
  "examples": [
    "Input : [40, 20, 30, 10, 30]\nOutput : 26000 \nExplanation : Best parenthesization is ( (A1 x (A2 x A3)) x A4 ).\nInput : [10, 20, 30, 40, 30] \nOutput : 30000 \nExplanation : Optimal parenthesization minimizes cost."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Recursive Approach",
      "algorithm": "Pattern Identification:\nWhenever we need to find the answer to a large problem such that the problem can be broken into subproblems and the final answer varies due to the order in which the subproblems are solved, we can think in terms of partition DP.\nLet us first understand the problem of matrix chain multiplication. In order to understand the problem we need to first understand the rules of matrix multiplication:\n Two matrices A1 and A2 of dimensions [p x q] and [r x s] can only be multiplied if and only if q=r. The total number of multiplications required to multiply A1 and A2 are: p*q*s As this problem of matrix multiplication solves one big problem (minimum operations to get A1.A2….An) and the answer varies in the order the subproblems are being solved, we can identify this problem as a partition DP problem. Rules to solve the problem of partition DP:\n Start with the full array: Represent the matrices with indexes i and j. We need to find the minimum cost to multiply matrices from i to j (f(i,j)). Check the base case: If i == j, there is only one matrix, so return 0 (no multiplication needed). Try all possible partitions: For k from i to j-1, split the array into two parts: f(i,k) and f(k+1,j). Calculate cost for each partition: Add the cost of multiplying the two subproblems plus the cost of multiplying their resulting matrices. Choose the minimum: Keep track of the minimum cost among all partitions. Return the result: After checking all partitions, return the minimum cost for multiplying matrices from i to j.",
      "time_complexity": "O(2^n)",
      "space_complexity": "O(2^n)",
      "time_complexity_details": "Time Complexity: O(2^n). This is because we try every possible parenthesization, which leads to exponential recursion. Space Complexity: O(n). This comes from the recursion call stack depth in the worst case.",
      "space_complexity_details": "Time Complexity: O(2^n). This is because we try every possible parenthesization, which leads to exponential recursion. Space Complexity: O(n). This comes from the recursion call stack depth in the worst case.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find the minimum multiplication cost\n    int matrixChainOrder(vector<int>& arr, int i, int j) {\n        // Base case: if only one matrix is present, no multiplication needed\n        if (i == j) return 0;\n\n        // Initialize minimum cost as very large\n        int minCost = INT_MAX;\n\n        // Try all possible partitions\n        for (int k = i; k < j; k++) {\n            // Cost of multiplying matrices from i to k\n            int cost1 = matrixChainOrder(arr, i, k);\n\n            // Cost of multiplying matrices from k+1 to j\n            int cost2 = matrixChainOrder(arr, k + 1, j);\n\n            // Cost of multiplying the two resulting matrices\n            int costMultiply = arr[i - 1] * arr[k] * arr[j];\n\n            // Total cost for this partition\n            int total = cost1 + cost2 + costMultiply;\n\n            // Update minimum\n            minCost = min(minCost, total);\n        }\n\n        return minCost;\n    }\n};\n\n// Driver code\nint main() {\n    Solution sol;\n    vector<int> arr = {40, 20, 30, 10, 30}; \n    int n = arr.size();\n\n    cout << \"Minimum number of multiplications is: \"\n         << sol.matrixChainOrder(arr, 1, n - 1) << endl;\n\n    return 0;\n}",
        "java": "import.java.util.*;\nclass Solution {\n    // Function to find the minimum multiplication cost\n    public int matrixChainOrder(int[] arr, int i, int j) {\n        // Base case: if only one matrix is present, no multiplication needed\n        if (i == j) return 0;\n\n        // Initialize minimum cost as very large\n        int minCost = Integer.MAX_VALUE;\n\n        // Try all possible partitions\n        for (int k = i; k < j; k++) {\n            // Cost of multiplying matrices from i to k\n            int cost1 = matrixChainOrder(arr, i, k);\n\n            // Cost of multiplying matrices from k+1 to j\n            int cost2 = matrixChainOrder(arr, k + 1, j);\n\n            // Cost of multiplying the two resulting matrices\n            int costMultiply = arr[i - 1] * arr[k] * arr[j];\n\n            // Total cost for this partition\n            int total = cost1 + cost2 + costMultiply;\n\n            // Update minimum\n            minCost = Math.min(minCost, total);\n        }\n\n        return minCost;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] arr = {40, 20, 30, 10, 30};\n        int n = arr.length;\n\n        System.out.println(\"Minimum number of multiplications is: \"\n                + sol.matrixChainOrder(arr, 1, n - 1));\n    }\n}",
        "python": "class Solution:\n    # Function to find the minimum multiplication cost\n    def matrixChainOrder(self, arr, i, j):\n        # Base case: if only one matrix is present, no multiplication needed\n        if i == j:\n            return 0\n\n        # Initialize minimum cost as very large\n        min_cost = float(\"inf\")\n\n        # Try all possible partitions\n        for k in range(i, j):\n            # Cost of multiplying matrices from i to k\n            cost1 = self.matrixChainOrder(arr, i, k)\n\n            # Cost of multiplying matrices from k+1 to j\n            cost2 = self.matrixChainOrder(arr, k + 1, j)\n\n            # Cost of multiplying the two resulting matrices\n            costMultiply = arr[i - 1] * arr[k] * arr[j]\n\n            # Total cost for this partition\n            total = cost1 + cost2 + costMultiply\n\n            # Update minimum\n            min_cost = min(min_cost, total)\n\n        return min_cost\n\n\n# Driver code\nif __name__ == \"__main__\":\n    sol = Solution()\n    arr = [40, 20, 30, 10, 30]\n    n = len(arr)\n    print(\"Minimum number of multiplications is:\",\n          sol.matrixChainOrder(arr, 1, n - 1))",
        "javascript": "class Solution {\n    // Function to find the minimum multiplication cost\n    matrixChainOrder(arr, i, j) {\n        // Base case: if only one matrix is present, no multiplication needed\n        if (i === j) return 0;\n\n        // Initialize minimum cost as very large\n        let minCost = Number.MAX_SAFE_INTEGER;\n\n        // Try all possible partitions\n        for (let k = i; k < j; k++) {\n            // Cost of multiplying matrices from i to k\n            let cost1 = this.matrixChainOrder(arr, i, k);\n\n            // Cost of multiplying matrices from k+1 to j\n            let cost2 = this.matrixChainOrder(arr, k + 1, j);\n\n            // Cost of multiplying the two resulting matrices\n            let costMultiply = arr[i - 1] * arr[k] * arr[j];\n\n            // Total cost for this partition\n            let total = cost1 + cost2 + costMultiply;\n\n            // Update minimum\n            minCost = Math.min(minCost, total);\n        }\n\n        return minCost;\n    }\n}\n\n// Driver code\nconst sol = new Solution();\nconst arr = [40, 20, 30, 10, 30];\nconst n = arr.length;\nconsole.log(\"Minimum number of multiplications is:\",\n    sol.matrixChainOrder(arr, 1, n - 1));"
      }
    },
    {
      "approach_name": "Memoization Approach",
      "algorithm": "Steps to memoize a recursive solution:\n\nAs there are overlapping subproblems in the recursive tree, we can memoize the recursive code to reduce the time complexity.\n\nCreate a dp array of size [n][n]. I and j can range from 0 to n-1 so we take the size n X n.We initialize the dp array to -1.Whenever we want to find the answer to particular parameters (say f(i,j)), we first check whether the answer is already calculated using the dp array(i.e dp[i][j]!= -1 ). If yes, simply return the value from the dp array.If not, then we are finding the answer for the given value for the first time, we will use the recursive relation as usual but before returning from the function, we will set dp[i][j] to the solution we get.",
      "time_complexity": "O(n^3)",
      "space_complexity": "O(n^3)",
      "time_complexity_details": "Time Complexity: O(n^3). There are O(n^2) subproblems (for each i, j), and for each we try O(n) partitions. Space Complexity: O(n^2). We use a 2D dp table of size n x n, plus recursion stack O(n).",
      "space_complexity_details": "Time Complexity: O(n^3). There are O(n^2) subproblems (for each i, j), and for each we try O(n) partitions. Space Complexity: O(n^2). We use a 2D dp table of size n x n, plus recursion stack O(n).",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Recursive function with memoization\n    int solve(vector<int>& arr, int i, int j, vector<vector<int>>& dp) {\n        // Base case: only one matrix, no multiplication\n        if (i == j) return 0;\n\n        // If already computed, return from dp\n        if (dp[i][j] != -1) return dp[i][j];\n\n        // Initialize min cost as very large\n        int minCost = INT_MAX;\n\n        // Try every partition\n        for (int k = i; k < j; k++) {\n            int cost1 = solve(arr, i, k, dp);\n            int cost2 = solve(arr, k + 1, j, dp);\n            int costMultiply = arr[i - 1] * arr[k] * arr[j];\n\n            int total = cost1 + cost2 + costMultiply;\n\n            minCost = min(minCost, total);\n        }\n\n        // Store and return result\n        return dp[i][j] = minCost;\n    }\n\n    // Function to start the memoization approach\n    int matrixChainOrder(vector<int>& arr) {\n        int n = arr.size();\n        vector<vector<int>> dp(n, vector<int>(n, -1));\n        return solve(arr, 1, n - 1, dp);\n    }\n};\n\n// Driver code\nint main() {\n    Solution sol;\n    vector<int> arr = {40, 20, 30, 10, 30};\n    cout << \"Minimum number of multiplications is: \"\n         << sol.matrixChainOrder(arr) << endl;\n    return 0;\n}",
        "java": "import.java.util.*;\nclass Solution {\n    // Recursive function with memoization\n    public int solve(int[] arr, int i, int j, int[][] dp) {\n        // Base case: only one matrix, no multiplication\n        if (i == j) return 0;\n\n        // If already computed, return from dp\n        if (dp[i][j] != -1) return dp[i][j];\n\n        int minCost = Integer.MAX_VALUE;\n\n        // Try every partition\n        for (int k = i; k < j; k++) {\n            int cost1 = solve(arr, i, k, dp);\n            int cost2 = solve(arr, k + 1, j, dp);\n            int costMultiply = arr[i - 1] * arr[k] * arr[j];\n\n            int total = cost1 + cost2 + costMultiply;\n            minCost = Math.min(minCost, total);\n        }\n\n        return dp[i][j] = minCost;\n    }\n\n    public int matrixChainOrder(int[] arr) {\n        int n = arr.length;\n        int[][] dp = new int[n][n];\n\n        // Initialize dp with -1\n        for (int i = 0; i < n; i++) {\n            for (int j = 0; j < n; j++) {\n                dp[i][j] = -1;\n            }\n        }\n        return solve(arr, 1, n - 1, dp);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] arr = {40, 20, 30, 10, 30};\n        System.out.println(\"Minimum number of multiplications is: \"\n                + sol.matrixChainOrder(arr));\n    }\n}",
        "python": "class Solution:\n    # Recursive function with memoization\n    def solve(self, arr, i, j, dp):\n        # Base case: only one matrix, no multiplication\n        if i == j:\n            return 0\n\n        # If already computed, return from dp\n        if dp[i][j] != -1:\n            return dp[i][j]\n\n        min_cost = float(\"inf\")\n\n        # Try every partition\n        for k in range(i, j):\n            cost1 = self.solve(arr, i, k, dp)\n            cost2 = self.solve(arr, k + 1, j, dp)\n            costMultiply = arr[i - 1] * arr[k] * arr[j]\n            total = cost1 + cost2 + costMultiply\n            min_cost = min(min_cost, total)\n\n        dp[i][j] = min_cost\n        return dp[i][j]\n\n    def matrixChainOrder(self, arr):\n        n = len(arr)\n        dp = [[-1] * n for _ in range(n)]\n        return self.solve(arr, 1, n - 1, dp)\n\n\n# Driver code\nif __name__ == \"__main__\":\n    sol = Solution()\n    arr = [40, 20, 30, 10, 30]\n    print(\"Minimum number of multiplications is:\",\n          sol.matrixChainOrder(arr))",
        "javascript": "class Solution {\n    // Recursive function with memoization\n    solve(arr, i, j, dp) {\n        // Base case: only one matrix, no multiplication\n        if (i === j) return 0;\n\n        // If already computed, return from dp\n        if (dp[i][j] !== -1) return dp[i][j];\n\n        let minCost = Number.MAX_SAFE_INTEGER;\n\n        // Try every partition\n        for (let k = i; k < j; k++) {\n            let cost1 = this.solve(arr, i, k, dp);\n            let cost2 = this.solve(arr, k + 1, j, dp);\n            let costMultiply = arr[i - 1] * arr[k] * arr[j];\n\n            let total = cost1 + cost2 + costMultiply;\n            minCost = Math.min(minCost, total);\n        }\n\n        dp[i][j] = minCost;\n        return dp[i][j];\n    }\n\n    matrixChainOrder(arr) {\n        let n = arr.length;\n        let dp = Array.from({ length: n }, () => Array(n).fill(-1));\n        return this.solve(arr, 1, n - 1, dp);\n    }\n}\n\n// Driver code\nconst sol = new Solution();\nconst arr = [40, 20, 30, 10, 30];\nconsole.log(\"Minimum number of multiplications is:\",\n    sol.matrixChainOrder(arr));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:25Z"
}