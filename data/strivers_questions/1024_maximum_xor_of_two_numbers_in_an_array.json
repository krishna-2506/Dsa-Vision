{
  "problem_id": "1024",
  "problem_name": "Maximum XOR of two numbers in an array",
  "step_no": 17,
  "step_name": "Tries",
  "substep_no": 2,
  "substep_name": "Problems",
  "difficulty": "Hard",
  "youtube_url": "https://www.youtube.com/watch?v=EIhAwfHubE8&list=PLgUwDviBIf0pcIDCZnxhv0LkHf5KzG9zp&index=6",
  "leetcode_url": "https://leetcode.com/problems/maximum-xor-of-two-numbers-in-an-array/",
  "article_url": "https://takeuforward.org/data-structure/maximum-xor-of-two-numbers-in-an-array/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/maximum-xor-of-two-numbers-in-an-array",
  "problem_statement": "Given an integer array nums, return the maximum result of nums[i] XOR nums[j], where 0 <= i <= j < n.",
  "examples": [
    "Input: nums = [3, 9, 10, 5, 1]\nOutput: 15\nExplanation: \nThe maximum XOR value is 10 XOR 5 = 15.\n\nInput: nums = [26, 49, 30, 15, 69]\nOutput: 116\nExplanation: \nThe maximum XOR value is 69 XOR 49 = 116."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "XOR, or exclusive OR, is a binary bitwise operation that returns true (1) only when the operands differ. If the bits are the same, the result is 0. If the bits are different, the result is 1.\n The goal is to maximise the XOR result between two numbers This is achieved by ensuring for each bit position, the XOR result is maximum hence we want to maximise the number of opposite bits between the numbers.\n To do this efficiently, we use a Trie which allows us to iterate through each bit of the numbers in the array and maximise XOR value by selecting the opposite bit when available hence ensuring the highest possible XOR result.\n \nCreate a Trie Node Structure. This structure represents a node in the Trie and contains an array to store links to child nodes (0 and 1). It also provides methods to interact with the child nodes like ‘containsKey’, ‘get’ and ‘put’.\nIterate through the given array and insert its bit values into the Trie from left to right.\nStart from the root node and initialise the maximum XOR values as 0. Iterate through each bit of the number (from left to right) and check if the complement of the current bit exists in the Trie.\nIf it exists, update the maximum XOR value with the current bit.Moves to the child node corresponding to the complement of the current bit.\nIf the complement of the current bit doesn’t exist in the Trie, move to the child node corresponding to the current bit.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Node structure for Trie\nclass Node {\npublic:\n    Node* links[2];\n\n    // Check if bit path exists\n    bool containsKey(int bit) {\n        return links[bit] != NULL;\n    }\n\n    // Get the child node for bit\n    Node* get(int bit) {\n        return links[bit];\n    }\n\n    // Create a link for the bit\n    void put(int bit, Node* node) {\n        links[bit] = node;\n    }\n};\n\nclass Solution {\npublic:\n    Node* root;\n\n    // Initialize root node\n    Solution() {\n        root = new Node();\n    }\n\n    // Insert a number into the Trie\n    void insert(int num) {\n        Node* node = root;\n        for (int i = 31; i >= 0; i--) {\n            // Get the i-th bit\n            int bit = (num >> i) & 1;\n\n            // Create path if it doesn't exist\n            if (!node->containsKey(bit)) {\n                node->put(bit, new Node());\n            }\n\n            // Move to next node\n            node = node->get(bit);\n        }\n    }\n\n    // Get max XOR for a given number\n    int getMaxXOR(int num) {\n        Node* node = root;\n        int maxXor = 0;\n\n        for (int i = 31; i >= 0; i--) {\n            // Get the i-th bit\n            int bit = (num >> i) & 1;\n\n            // Try to go opposite bit to maximize XOR\n            if (node->containsKey(1 - bit)) {\n                maxXor |= (1 << i);\n                node = node->get(1 - bit);\n            } else {\n                node = node->get(bit);\n            }\n        }\n\n        return maxXor;\n    }\n\n    // Find the maximum XOR among all pairs\n    int findMaximumXOR(vector<int>& nums) {\n        for (int num : nums) {\n            insert(num);\n        }\n\n        int maxResult = 0;\n        for (int num : nums) {\n            maxResult = max(maxResult, getMaxXOR(num));\n        }\n\n        return maxResult;\n    }\n};\n\n// Driver code\nint main() {\n    vector<int> nums = {3, 10, 5, 25, 2, 8};\n    Solution sol;\n    cout << sol.findMaximumXOR(nums) << endl;\n    return 0;\n}",
        "java": "// Trie node definition\nclass Node {\n    Node[] links = new Node[2];\n\n    // Check if bit path exists\n    public boolean containsKey(int bit) {\n        return links[bit] != null;\n    }\n\n    // Get child node for the bit\n    public Node get(int bit) {\n        return links[bit];\n    }\n\n    // Set child node for the bit\n    public void put(int bit, Node node) {\n        links[bit] = node;\n    }\n}\n\nclass Solution {\n    Node root = new Node();\n\n    // Insert number into the Trie\n    public void insert(int num) {\n        Node node = root;\n        for (int i = 31; i >= 0; i--) {\n            // Get the i-th bit\n            int bit = (num >> i) & 1;\n\n            // Create path if not present\n            if (!node.containsKey(bit)) {\n                node.put(bit, new Node());\n            }\n\n            // Move to next node\n            node = node.get(bit);\n        }\n    }\n\n    // Get max XOR for a given number\n    public int getMaxXOR(int num) {\n        Node node = root;\n        int maxXor = 0;\n\n        for (int i = 31; i >= 0; i--) {\n            // Get the i-th bit\n            int bit = (num >> i) & 1;\n\n            // Try opposite bit for max XOR\n            if (node.containsKey(1 - bit)) {\n                maxXor |= (1 << i);\n                node = node.get(1 - bit);\n            } else {\n                node = node.get(bit);\n            }\n        }\n\n        return maxXor;\n    }\n\n    // Find max XOR among all pairs\n    public int findMaximumXOR(int[] nums) {\n        for (int num : nums) {\n            insert(num);\n        }\n\n        int maxResult = 0;\n        for (int num : nums) {\n            maxResult = Math.max(maxResult, getMaxXOR(num));\n        }\n\n        return maxResult;\n    }\n}\n\n// Driver code\npublic class Main {\n    public static void main(String[] args) {\n        int[] nums = {3, 10, 5, 25, 2, 8};\n        Solution sol = new Solution();\n        System.out.println(sol.findMaximumXOR(nums));\n    }\n}",
        "python": "# Trie node definition\nclass Node:\n    def __init__(self):\n        self.links = [None, None]\n\n    # Check if path exists\n    def containsKey(self, bit):\n        return self.links[bit] is not None\n\n    # Get child node\n    def get(self, bit):\n        return self.links[bit]\n\n    # Create child node\n    def put(self, bit, node):\n        self.links[bit] = node\n\nclass Solution:\n    def __init__(self):\n        self.root = Node()\n\n    # Insert number into Trie\n    def insert(self, num):\n        node = self.root\n        for i in range(31, -1, -1):\n            # Get the i-th bit\n            bit = (num >> i) & 1\n\n            # Create path if not present\n            if not node.containsKey(bit):\n                node.put(bit, Node())\n\n            # Move to next node\n            node = node.get(bit)\n\n    # Get max XOR for number\n    def getMaxXOR(self, num):\n        node = self.root\n        maxXor = 0\n\n        for i in range(31, -1, -1):\n            # Get the i-th bit\n            bit = (num >> i) & 1\n\n            # Try opposite bit to maximize XOR\n            if node.containsKey(1 - bit):\n                maxXor |= (1 << i)\n                node = node.get(1 - bit)\n            else:\n                node = node.get(bit)\n\n        return maxXor\n\n    # Find maximum XOR among all pairs\n    def findMaximumXOR(self, nums):\n        for num in nums:\n            self.insert(num)\n\n        maxResult = 0\n        for num in nums:\n            maxResult = max(maxResult, self.getMaxXOR(num))\n\n        return maxResult\n\n# Driver code\nnums = [3, 10, 5, 25, 2, 8]\nsol = Solution()\nprint(sol.findMaximumXOR(nums))",
        "javascript": "// Trie node class\nclass Node {\n    constructor() {\n        this.links = [null, null];\n    }\n\n    // Check if bit path exists\n    containsKey(bit) {\n        return this.links[bit] !== null;\n    }\n\n    // Get child node\n    get(bit) {\n        return this.links[bit];\n    }\n\n    // Set child node\n    put(bit, node) {\n        this.links[bit] = node;\n    }\n}\n\nclass Solution {\n    constructor() {\n        this.root = new Node();\n    }\n\n    // Insert number into Trie\n    insert(num) {\n        let node = this.root;\n        for (let i = 31; i >= 0; i--) {\n            // Get the i-th bit\n            let bit = (num >> i) & 1;\n\n            // Create path if not present\n            if (!node.containsKey(bit)) {\n                node.put(bit, new Node());\n            }\n\n            // Move to next node\n            node = node.get(bit);\n        }\n    }\n\n    // Get max XOR for number\n    getMaxXOR(num) {\n        let node = this.root;\n        let maxXor = 0;\n\n        for (let i = 31; i >= 0; i--) {\n            // Get the i-th bit\n            let bit = (num >> i) & 1;\n\n            // Try opposite bit for better XOR\n            if (node.containsKey(1 - bit)) {\n                maxXor |= (1 << i);\n                node = node.get(1 - bit);\n            } else {\n                node = node.get(bit);\n            }\n        }\n\n        return maxXor;\n    }\n\n    // Find maximum XOR among all pairs\n    findMaximumXOR(nums) {\n        for (let num of nums) {\n            this.insert(num);\n        }\n\n        let maxResult = 0;\n        for (let num of nums) {\n            maxResult = Math.max(maxResult, this.getMaxXOR(num));\n        }\n\n        return maxResult;\n    }\n}\n\n// Driver code\nlet nums = [3, 10, 5, 25, 2, 8];\nlet sol = new Solution();\nconsole.log(sol.findMaximumXOR(nums));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:26Z"
}