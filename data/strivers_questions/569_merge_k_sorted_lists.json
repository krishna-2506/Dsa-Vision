{
  "problem_id": "569",
  "problem_name": "Merge K sorted Lists",
  "step_no": 11,
  "step_name": "Heaps [Learning, Medium, Hard Problems]",
  "substep_no": 2,
  "substep_name": "Medium Problems",
  "difficulty": "Hard",
  "youtube_url": null,
  "leetcode_url": "https://leetcode.com/problems/merge-k-sorted-lists/",
  "article_url": "https://takeuforward.org/data-structure/merge-m-sorted-lists",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/merge-k-sorted-lists",
  "problem_statement": "Given heads of k sorted linked lists as an array called heads, merge them into one single sorted linked list and return the head of that list.",
  "examples": [
    "Input : heads = [[head -> 1 -> 2 -> 3 -> 4], [head -> -4 -> -3], [head -> -5 -> -3 -> 1 -> 2 -> 3 -> 4]]\nOutput: head -> -5 -> -4 -> -3 -> -3 -> 1 -> 1 -> 2 -> 2 -> 3 -> 3 -> 4 -> 4\nExplanation: head -> -5 -> -4 -> -3 -> -3 -> 1 -> 1 -> 2 -> 2 -> 3 -> 3 -> 4 -> 4\nThe nodes in bold come from the 3rd list, the underlined nodes come from the 2nd list, rest are from the 1st list.\nInput : heads = [[head -> -5 -> -4 -> -1], [head -> 10 -> 11 -> 12]]\nOutput : head -> -5 -> -4 -> -1 -> 10 -> 11 -> 12\nExplanation : head -> -5 -> -4 -> -1 -> 10 -> 11 -> 12\nThe nodes in bold come from the 1st list, rest are from the 2nd list."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Brute Force Approach",
      "algorithm": "To merge `k sorted linked lists, the most basic thing we can do is just collect all the node values from every list, store them into one big container, sort it, and then rebuild a single sorted linked list out of those sorted values. This approach doesn’t leverage the fact that the input lists are already sorted so it’s inefficient but it’s a good starting point to understand the flow of the problem. It’s like gathering all the exam papers, sorting by marks again manually, and then preparing the final rank list. No merging, just raw sorting. This helps build clarity before jumping into optimized approach.\n\nInitialize an empty array to store all node values from every list.\nIterate through each linked list and push every node’s value into the array.\nSort the array of values.\nCreate a new dummy head for the final linked list.\nIterate over the sorted array and create a new node for each value, appending it to the final list.\nReturn the node next to the dummy head as the actual head of the merged list.",
      "time_complexity": "O(N log N)",
      "space_complexity": "O(N log N)",
      "time_complexity_details": "Time Complexity:O(N log N) ,We traverse all the nodes once to collect their values, which takes O(N) where N is the total number of nodes across all lists. Sorting these N values takes O(N log N). Building the new linked list from the sorted array takes O(N). So the overall time complexity is O(N log N).\nSpace Complexity:O(N) ,We use an additional array to store all the node values, which requires O(N) space. Plus, the new merged linked list also takes O(N) space, but since this is the required output, only the temporary storage is considered extra.",
      "space_complexity_details": "Time Complexity:O(N log N) ,We traverse all the nodes once to collect their values, which takes O(N) where N is the total number of nodes across all lists. Sorting these N values takes O(N log N). Building the new linked list from the sorted array takes O(N). So the overall time complexity is O(N log N).\nSpace Complexity:O(N) ,We use an additional array to store all the node values, which requires O(N) space. Plus, the new merged linked list also takes O(N) space, but since this is the required output, only the temporary storage is considered extra.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Definition for singly-linked list.\nstruct ListNode {\n    int val;\n    ListNode *next;\n\n    // Constructor to initialize a single node with a value\n    ListNode(int x) : val(x), next(NULL) {}\n};\n\nclass Solution {\npublic:\n    // Function to merge k sorted linked lists using brute force\n    ListNode* mergeKSortedLists(vector<ListNode*>& lists) {\n        // Vector to store all values from every node\n        vector<int> allValues;\n\n        // Loop through each linked list\n        for (auto list : lists) {\n            // Traverse the current list and store its values\n            while (list != NULL) {\n                allValues.push_back(list->val);\n                list = list->next;\n            }\n        }\n\n        // Sort all collected values\n        sort(allValues.begin(), allValues.end());\n\n        // Create a dummy head for the final result list\n        ListNode* dummy = new ListNode(0);\n        ListNode* curr = dummy;\n\n        // Create new linked list nodes from sorted values\n        for (int val : allValues) {\n            curr->next = new ListNode(val);\n            curr = curr->next;\n        }\n\n        // Return head of the merged linked list\n        return dummy->next;\n    }\n};\n\n// Driver code\nint main() {\n    // Example: [[1,4,5],[1,3,4],[2,6]]\n    ListNode* a = new ListNode(1);\n    a->next = new ListNode(4);\n    a->next->next = new ListNode(5);\n\n    ListNode* b = new ListNode(1);\n    b->next = new ListNode(3);\n    b->next->next = new ListNode(4);\n\n    ListNode* c = new ListNode(2);\n    c->next = new ListNode(6);\n\n    vector<ListNode*> lists = {a, b, c};\n\n    Solution obj;\n    ListNode* result = obj.mergeKSortedLists(lists);\n\n    while (result) {\n        cout << result->val << \" \";\n        result = result->next;\n    }\n\n    return 0;\n}",
        "java": "import java.util.*;\n// Definition for singly-linked list.\nclass ListNode {\n    int val;\n    ListNode next;\n\n    // Constructor to initialize node with value\n    ListNode(int val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nclass Solution {\n    // Function to merge k sorted linked lists using brute force\n    public ListNode mergeKSortedLists(ListNode[] lists) {\n        // List to collect all node values\n        List<Integer> allValues = new ArrayList<>();\n\n        // Traverse each linked list\n        for (ListNode head : lists) {\n            while (head != null) {\n                allValues.add(head.val);\n                head = head.next;\n            }\n        }\n\n        // Sort the collected values\n        Collections.sort(allValues);\n\n        // Create dummy node for result list\n        ListNode dummy = new ListNode(0);\n        ListNode curr = dummy;\n\n        // Create new nodes from sorted values\n        for (int val : allValues) {\n            curr.next = new ListNode(val);\n            curr = curr.next;\n        }\n\n        return dummy.next;\n    }\n}\n\n// Driver code\nclass Main {\n    public static void main(String[] args) {\n        // Example: [[1,4,5],[1,3,4],[2,6]]\n        ListNode a = new ListNode(1);\n        a.next = new ListNode(4);\n        a.next.next = new ListNode(5);\n\n        ListNode b = new ListNode(1);\n        b.next = new ListNode(3);\n        b.next.next = new ListNode(4);\n\n        ListNode c = new ListNode(2);\n        c.next = new ListNode(6);\n\n        ListNode[] lists = new ListNode[]{a, b, c};\n\n        Solution sol = new Solution();\n        ListNode result = sol.mergeKSortedLists(lists);\n\n        while (result != null) {\n            System.out.print(result.val + \" \");\n            result = result.next;\n        }\n    }\n}",
        "python": "# Definition for singly-linked list\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\nclass Solution:\n    # Function to merge k sorted linked lists using brute force\n    def mergeKSortedLists(self, lists):\n        # Store all node values\n        all_values = []\n\n        # Extract values from each list\n        for head in lists:\n            while head:\n                all_values.append(head.val)\n                head = head.next\n\n        # Sort all values\n        all_values.sort()\n\n        # Create dummy node for result\n        dummy = ListNode(0)\n        current = dummy\n\n        # Build linked list from sorted values\n        for val in all_values:\n            current.next = ListNode(val)\n            current = current.next\n\n        return dummy.next\n\n# Driver code\nif __name__ == \"__main__\":\n    # Example: [[1,4,5],[1,3,4],[2,6]]\n    a = ListNode(1, ListNode(4, ListNode(5)))\n    b = ListNode(1, ListNode(3, ListNode(4)))\n    c = ListNode(2, ListNode(6))\n\n    lists = [a, b, c]\n\n    sol = Solution()\n    result = sol.mergeKSortedLists(lists)\n\n    while result:\n        print(result.val, end=\" \")\n        result = result.next",
        "javascript": "// Definition for singly-linked list node\nclass ListNode {\n    constructor(val, next = null) {\n        this.val = val;\n        this.next = next;\n    }\n}\n\nclass Solution {\n    // Function to merge k sorted linked lists using brute force\n    mergeKSortedLists(lists) {\n        // Array to collect all values\n        let allValues = [];\n\n        // Traverse each list and collect values\n        for (let head of lists) {\n            while (head !== null) {\n                allValues.push(head.val);\n                head = head.next;\n            }\n        }\n\n        // Sort the values\n        allValues.sort((a, b) => a - b);\n\n        // Create dummy node for result list\n        let dummy = new ListNode(0);\n        let curr = dummy;\n\n        // Construct linked list from sorted values\n        for (let val of allValues) {\n            curr.next = new ListNode(val);\n            curr = curr.next;\n        }\n\n        // Return head of merged list\n        return dummy.next;\n    }\n}\n\n// Driver code\nfunction printList(head) {\n    while (head !== null) {\n        process.stdout.write(head.val + \" \");\n        head = head.next;\n    }\n}\n\n// Example: [[1,4,5],[1,3,4],[2,6]]\nlet a = new ListNode(1, new ListNode(4, new ListNode(5)));\nlet b = new ListNode(1, new ListNode(3, new ListNode(4)));\nlet c = new ListNode(2, new ListNode(6));\n\nlet lists = [a, b, c];\n\nlet sol = new Solution();\nlet result = sol.mergeKSortedLists(lists);\nprintList(result);"
      }
    },
    {
      "approach_name": "Optimal Approach",
      "algorithm": "Instead of extracting all values and sorting them again, we can merge the lists by always picking the smallest current node among all lists. This can be achieved efficiently using a min-heap (or priority queue), where we store the current node of each list and always extract the node with the smallest value. By pushing the next node of the extracted node back into the heap, we ensure that at any point, the heap contains the next smallest potential candidates. This approach avoids unnecessary sorting and leverages the fact that each list is already sorted.\n\n Create a min-heap that stores the current head node of each list. Push the head of each non-empty list into the min-heap. Initialize a dummy node to build the resulting merged list. While the heap is not empty: Pop the node with the smallest value from the heap. Append this node to the result list. If the popped node has a next node, push it into the heap. Return the node next to the dummy as the head of the merged list.",
      "time_complexity": "O(N * log K)",
      "space_complexity": "O(N * log K)",
      "time_complexity_details": "Time Complexity:O(N * log K) ,Where N is the total number of nodes across all K linked lists. Each insertion and extraction operation on the min-heap takes O(log K) time, and we perform such operations N times (once per node). Hence, the overall time complexity becomes O(N * log K). Space Complexity:O(K),The heap stores at most K nodes at any time one from each of the K linked lists. Therefore, the auxiliary space used by the priority queue is O(K). The final merged linked list uses O(1) extra space if we disregard the output list, as the nodes are rearranged rather than copied.",
      "space_complexity_details": "Time Complexity:O(N * log K) ,Where N is the total number of nodes across all K linked lists. Each insertion and extraction operation on the min-heap takes O(log K) time, and we perform such operations N times (once per node). Hence, the overall time complexity becomes O(N * log K). Space Complexity:O(K),The heap stores at most K nodes at any time one from each of the K linked lists. Therefore, the auxiliary space used by the priority queue is O(K). The final merged linked list uses O(1) extra space if we disregard the output list, as the nodes are rearranged rather than copied.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Definition for singly-linked list\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int x) : val(x), next(NULL) {}\n};\n\nclass Compare {\npublic:\n    // Comparator to order ListNode pointers based on node values\n    bool operator()(ListNode* a, ListNode* b) {\n        return a->val > b->val;\n    }\n};\n\nclass Solution {\npublic:\n    // Function to merge k sorted linked lists using a min-heap\n    ListNode* mergeKLists(vector<ListNode*>& lists) {\n        // Create a min-heap (priority queue) with custom comparator\n        priority_queue<ListNode*, vector<ListNode*>, Compare> pq;\n\n        // Push the head of each non-empty list into the heap\n        for (auto list : lists) {\n            if (list != NULL)\n                pq.push(list);\n        }\n\n        // Create a dummy node to build the result list\n        ListNode* dummy = new ListNode(0);\n        ListNode* tail = dummy;\n\n        // While the heap is not empty\n        while (!pq.empty()) {\n            // Extract the node with the smallest value\n            ListNode* smallest = pq.top();\n            pq.pop();\n\n            // Add it to the result list\n            tail->next = smallest;\n            tail = tail->next;\n\n            // If there's a next node, push it into the heap\n            if (smallest->next != NULL)\n                pq.push(smallest->next);\n        }\n\n        // Return the head of the merged list\n        return dummy->next;\n    }\n};\n\n// Driver code\nint main() {\n    Solution sol;\n\n    // Creating three linked lists:\n    // list1: 1 -> 4 -> 5\n    // list2: 1 -> 3 -> 4\n    // list3: 2 -> 6\n\n    ListNode* list1 = new ListNode(1);\n    list1->next = new ListNode(4);\n    list1->next->next = new ListNode(5);\n\n    ListNode* list2 = new ListNode(1);\n    list2->next = new ListNode(3);\n    list2->next->next = new ListNode(4);\n\n    ListNode* list3 = new ListNode(2);\n    list3->next = new ListNode(6);\n\n    vector<ListNode*> lists = {list1, list2, list3};\n    ListNode* result = sol.mergeKLists(lists);\n\n    // Print the merged list\n    while (result != NULL) {\n        cout << result->val << \" \";\n        result = result->next;\n    }\n    return 0;\n}",
        "java": "import java.util.*;\n\n// Definition for singly-linked list\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int x) {\n        val = x;\n        next = null;\n    }\n}\n\nclass Solution {\n    // Function to merge k sorted linked lists using a min-heap\n    public ListNode mergeKLists(ListNode[] lists) {\n        // Create a priority queue (min-heap) to store nodes\n        PriorityQueue<ListNode> pq = new PriorityQueue<>(\n            (a, b) -> a.val - b.val\n        );\n\n        // Push the head of each non-null list into the heap\n        for (ListNode node : lists) {\n            if (node != null) pq.add(node);\n        }\n\n        // Create a dummy node to build the result list\n        ListNode dummy = new ListNode(0);\n        ListNode tail = dummy;\n\n        // While the heap is not empty\n        while (!pq.isEmpty()) {\n            // Extract the node with the smallest value\n            ListNode smallest = pq.poll();\n\n            // Add it to the result list\n            tail.next = smallest;\n            tail = tail.next;\n\n            // If there's a next node, push it into the heap\n            if (smallest.next != null) {\n                pq.add(smallest.next);\n            }\n        }\n\n        // Return the head of the merged list\n        return dummy.next;\n    }\n}\n\n// Driver class\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n\n        // Creating three linked lists:\n        // list1: 1 -> 4 -> 5\n        // list2: 1 -> 3 -> 4\n        // list3: 2 -> 6\n\n        ListNode list1 = new ListNode(1);\n        list1.next = new ListNode(4);\n        list1.next.next = new ListNode(5);\n\n        ListNode list2 = new ListNode(1);\n        list2.next = new ListNode(3);\n        list2.next.next = new ListNode(4);\n\n        ListNode list3 = new ListNode(2);\n        list3.next = new ListNode(6);\n\n        ListNode[] lists = new ListNode[]{list1, list2, list3};\n\n        ListNode result = sol.mergeKLists(lists);\n\n        // Print the merged list\n        while (result != null) {\n            System.out.print(result.val + \" \");\n            result = result.next;\n        }\n    }\n}",
        "python": "import heapq\n\n# Definition for singly-linked list\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\nclass Solution:\n    # Function to merge k sorted linked lists using a min-heap\n    def mergeKLists(self, lists):\n        # Initialize a min-heap\n        min_heap = []\n\n        # Push the head node of each non-empty list into the heap\n        for i, node in enumerate(lists):\n            if node:\n                heapq.heappush(min_heap, (node.val, i, node))\n\n        # Create a dummy node to build the result list\n        dummy = ListNode(0)\n        tail = dummy\n\n        # While the heap is not empty\n        while min_heap:\n            # Extract the node with the smallest value\n            val, i, node = heapq.heappop(min_heap)\n\n            # Add it to the result list\n            tail.next = node\n            tail = tail.next\n\n            # If there's a next node, push it into the heap\n            if node.next:\n                heapq.heappush(min_heap, (node.next.val, i, node.next))\n\n        # Return the head of the merged list\n        return dummy.next\n\n# Driver code\nif __name__ == \"__main__\":\n    sol = Solution()\n\n    # Creating three linked lists:\n    # list1: 1 -> 4 -> 5\n    # list2: 1 -> 3 -> 4\n    # list3: 2 -> 6\n\n    list1 = ListNode(1, ListNode(4, ListNode(5)))\n    list2 = ListNode(1, ListNode(3, ListNode(4)))\n    list3 = ListNode(2, ListNode(6))\n\n    lists = [list1, list2, list3]\n    result = sol.mergeKLists(lists)\n\n    # Print the merged list\n    while result:\n        print(result.val, end=\" \")\n        result = result.next",
        "javascript": "// Definition for singly-linked list node\nclass ListNode {\n    constructor(val = 0, next = null) {\n        this.val = val;\n        this.next = next;\n    }\n}\n\nclass MinHeap {\n    constructor() {\n        // Array to simulate heap\n        this.heap = [];\n    }\n\n    // Function to insert a node into the min-heap\n    insert(node) {\n        // Add the node at the end\n        this.heap.push(node);\n        // Restore heap property by bubbling up\n        this.bubbleUp(this.heap.length - 1);\n    }\n\n    // Function to return the smallest node (top of heap)\n    getMin() {\n        return this.heap[0];\n    }\n\n    // Function to remove and return the smallest node\n    extractMin() {\n        if (this.heap.length === 0) return null;\n        // Swap first and last elements\n        const min = this.heap[0];\n        const end = this.heap.pop();\n        if (this.heap.length > 0) {\n            this.heap[0] = end;\n            // Restore heap property by bubbling down\n            this.bubbleDown(0);\n        }\n        return min;\n    }\n\n    // Bubble up the element at index i to its correct position\n    bubbleUp(i) {\n        let currentIndex = i;\n        while (currentIndex > 0) {\n            // Find parent index\n            let parentIndex = Math.floor((currentIndex - 1) / 2);\n            // If current node value is less than parent, swap\n            if (this.heap[currentIndex].val < this.heap[parentIndex].val) {\n                [this.heap[currentIndex], this.heap[parentIndex]] = [this.heap[parentIndex], this.heap[currentIndex]];\n                currentIndex = parentIndex;\n            } else break;\n        }\n    }\n\n    // Bubble down the element at index i to maintain heap\n    bubbleDown(i) {\n        let currentIndex = i;\n        const length = this.heap.length;\n        while (true) {\n            let left = 2 * currentIndex + 1;\n            let right = 2 * currentIndex + 2;\n            let smallest = currentIndex;\n\n            // Compare with left child\n            if (left < length && this.heap[left].val < this.heap[smallest].val) {\n                smallest = left;\n            }\n            // Compare with right child\n            if (right < length && this.heap[right].val < this.heap[smallest].val) {\n                smallest = right;\n            }\n\n            // If smallest is not current index, swap and continue\n            if (smallest !== currentIndex) {\n                [this.heap[currentIndex], this.heap[smallest]] = [this.heap[smallest], this.heap[currentIndex]];\n                currentIndex = smallest;\n            } else break;\n        }\n    }\n\n    // Return size of heap\n    size() {\n        return this.heap.length;\n    }\n}\n\nclass Solution {\n    /**\n     * Function to merge k sorted linked lists into a single sorted list\n     * @param {ListNode[]} lists - Array of k sorted linked list heads\n     * @return {ListNode} - Head of merged sorted linked list\n     */\n    mergeKSortedLists(lists) {\n        // Create a new min heap to keep track of the smallest nodes\n        const heap = new MinHeap();\n\n        // Push the head node of each list into the heap\n        for (let node of lists) {\n            if (node !== null) {\n                heap.insert(node);\n            }\n        }\n\n        // Create a dummy head for the merged list\n        const dummy = new ListNode(0);\n        let current = dummy;\n\n        // Repeat while heap is not empty\n        while (heap.size() > 0) {\n            // Extract node with the smallest value\n            const smallest = heap.extractMin();\n\n            // Add this node to the merged list\n            current.next = smallest;\n            current = current.next;\n\n            // If this node has a next node, push it into heap\n            if (smallest.next !== null) {\n                heap.insert(smallest.next);\n            }\n        }\n\n        // Return the actual head of merged list\n        return dummy.next;\n    }\n}\n\n// Driver code to test the function\nfunction printList(head) {\n    let res = [];\n    while (head !== null) {\n        res.push(head.val);\n        head = head.next;\n    }\n    console.log(res);\n}\n\n// Creating sample input: 3 sorted linked lists\nlet l1 = new ListNode(1, new ListNode(4, new ListNode(5)));\nlet l2 = new ListNode(1, new ListNode(3, new ListNode(4)));\nlet l3 = new ListNode(2, new ListNode(6));\n\n// Input array of linked lists\nlet lists = [l1, l2, l3];\n\n// Creating object of Solution class\nlet sol = new Solution();\n\n// Merging all k sorted lists\nlet mergedHead = sol.mergeKSortedLists(lists);\n\n// Output the merged sorted list\nprintList(mergedHead);"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:13Z"
}