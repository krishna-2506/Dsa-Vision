{
  "problem_id": "314",
  "problem_name": "Wildcard matching",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 5,
  "substep_name": "DP on Strings",
  "difficulty": "Hard",
  "youtube_url": "https://youtu.be/ZmlQ3vgAOMo",
  "leetcode_url": "https://leetcode.com/problems/wildcard-matching/",
  "article_url": "https://takeuforward.org/data-structure/wildcard-matching-dp-34/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/wildcard-matching",
  "problem_statement": "We are given two strings ‘S1’ and ‘S2’. String S1 can have the following two special characters.",
  "examples": [
    "Input: S1 = \"ab*cd\", S2 = \"abdefcd\"\nOutput: true\nExplanation: \"ab\" matches \"ab\", '*' matches \"def\", \"cd\" matches \"cd\". The strings match.\nInput: S1 = \"*a*b\", S2 = \"aaab\"\nOutput: true\nExplanation: First '*' matches \"aa\", 'a' matches 'a', second '*' matches empty string, 'b' matches 'b'. The strings match."
  ],
  "total_approaches": 3,
  "approaches": [
    {
      "approach_name": "Memoization Approach",
      "algorithm": "For every index of string S1, we have different options to match that index with string S2. We can think in terms of string matching paths, as in earlier problems:\n\nEither the characters match already.\nOr, if there is a ?, we can explicitly match a single character.\nFor a *, it can match zero or more characters.\n\nAs there is no uniformity in data, the only way is to try all possible ways, which means using recursion.\nSteps to form the recursive solution\n\nCompare positions in both strings\n Treat one string as the pattern (letters, '?' or '*') and the other as the text. Start from the end and move backwards.\n \nHandle current characters\n\nCharacters match: move back in both strings.\n'?' → matches exactly one character: move back in both.\n'*' → \n \nMatch empty sequence , move back in pattern.\nMatch one or more characters,move back in text.\n\nDifferent letters: no match.\n\nCombine results: If any option leads to a match, the strings match.\nBase cases\n\nBoth strings finished : match.\nPattern finished but text left: no match.\nText finished but pattern left : match only if all remaining are '*'.\n\nSteps to memoize a recursive solution:\nIf we draw the recursion tree, we will see that there are overlapping subproblems. In order to convert a recursive solution the following steps will be taken:\nCreate a dp array of size [n][m]. The size of S1 and S2 are n and m respectively, so the variable i will always lie between ‘0’ and ‘n-1’ and the variable j between ‘0’ and ‘m-1’.We initialize the dp array to -1.Whenever we want to find the answer to particular parameters (say f(i,j)), we first check whether the answer is already calculated using the dp array(i.e dp[i][j]!= -1 ). If yes, simply return the value from the dp array.If not, then we are finding the answer for the given value for the first time, we will use the recursive relation as usual but before returning from the function, we will set dp[i][j] to the solution we get.",
      "time_complexity": "O(N*M)",
      "space_complexity": "O(N*M)",
      "time_complexity_details": "Time Complexity: O(N*M),There are N*M states therefore at max ‘N*M’ new problems will be solved.\nSpace Complexity: O(N*M) + O(N+M), We are using a recursion stack space(O(N+M)) and a 2D array ( O(N*M)).",
      "space_complexity_details": "Time Complexity: O(N*M),There are N*M states therefore at max ‘N*M’ new problems will be solved.\nSpace Complexity: O(N*M) + O(N+M), We are using a recursion stack space(O(N+M)) and a 2D array ( O(N*M)).",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to check if the substring S1[0..i] contains only '*'\n    bool isAllStars(string &S1, int i) {\n        // Loop from 0 to i to verify all characters are '*'\n        for (int j = 0; j <= i; j++) {\n            if (S1[j] != '*')\n                return false;\n        }\n        return true;\n    }\n\n    // Recursive function with memoization to check wildcard matching\n    bool wildcardMatchingUtil(string &S1, string &S2, int i, int j, vector<vector<int>> &dp) {\n        // Base Case 1: Both strings are exhausted\n        if (i < 0 && j < 0)\n            return true;\n\n        // Base Case 2: Pattern exhausted but text remains\n        if (i < 0 && j >= 0)\n            return false;\n\n        // Base Case 3: Text exhausted but pattern may still have '*'\n        if (j < 0 && i >= 0)\n            return isAllStars(S1, i);\n\n        // If already computed, return stored value\n        if (dp[i][j] != -1)\n            return dp[i][j];\n\n        // If characters match or pattern has '?'\n        if (S1[i] == S2[j] || S1[i] == '?')\n            return dp[i][j] = wildcardMatchingUtil(S1, S2, i - 1, j - 1, dp);\n\n        // If pattern has '*', we have two choices:\n        // 1. Treat '*' as matching empty sequence -> move pattern index i-1\n        // 2. Treat '*' as matching one or more characters -> move text index j-1\n        if (S1[i] == '*')\n            return dp[i][j] = wildcardMatchingUtil(S1, S2, i - 1, j, dp) ||\n                               wildcardMatchingUtil(S1, S2, i, j - 1, dp);\n\n        // If no match\n        return dp[i][j] = false;\n    }\n\n    // Main function to be called from driver code\n    bool wildcardMatching(string &S1, string &S2) {\n        int n = S1.size();\n        int m = S2.size();\n\n        // DP array initialized to -1 to indicate uncomputed states\n        vector<vector<int>> dp(n, vector<int>(m, -1));\n\n        // Call the recursive utility function starting from the last indices\n        return wildcardMatchingUtil(S1, S2, n - 1, m - 1, dp);\n    }\n};\n\n// Driver Code\nint main() {\n    // Example pattern and text\n    string S1 = \"ab*cd\";\n    string S2 = \"abdefcd\";\n\n    // Create a Solution object\n    Solution obj;\n\n    // Call the matching function and display the result\n    if (obj.wildcardMatching(S1, S2))\n        cout << \"String S1 and S2 do match\";\n    else\n        cout << \"String S1 and S2 do not match\";\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n\n    // Function to check if substring S1[0..i] contains only '*'\n    public boolean isAllStars(String S1, int i) {\n        for (int j = 0; j <= i; j++) {\n            if (S1.charAt(j) != '*')\n                return false;\n        }\n        return true;\n    }\n\n    // Recursive function with memoization to check wildcard matching\n    public boolean wildcardMatchingUtil(String S1, String S2, int i, int j, int[][] dp) {\n        // Base Case 1: Both pattern and text are fully matched\n        if (i < 0 && j < 0) return true;\n\n        // Base Case 2: Pattern exhausted but text still remains\n        if (i < 0 && j >= 0) return false;\n\n        // Base Case 3: Text exhausted but pattern may have '*' left\n        if (j < 0 && i >= 0) return isAllStars(S1, i);\n\n        // If already computed, return stored result\n        if (dp[i][j] != -1) return dp[i][j] == 1;\n\n        // If characters match or pattern has '?'\n        if (S1.charAt(i) == S2.charAt(j) || S1.charAt(i) == '?')\n            dp[i][j] = wildcardMatchingUtil(S1, S2, i - 1, j - 1, dp) ? 1 : 0;\n\n        // If pattern has '*', check both possibilities\n        else if (S1.charAt(i) == '*')\n            dp[i][j] = (wildcardMatchingUtil(S1, S2, i - 1, j, dp) ||\n                        wildcardMatchingUtil(S1, S2, i, j - 1, dp)) ? 1 : 0;\n\n        // If characters don't match\n        else\n            dp[i][j] = 0;\n\n        return dp[i][j] == 1;\n    }\n\n    // Main function to start the matching process\n    public boolean wildcardMatching(String S1, String S2) {\n        int n = S1.length();\n        int m = S2.length();\n\n        // DP array initialized to -1 (uncomputed)\n        int[][] dp = new int[n][m];\n        for (int[] row : dp) Arrays.fill(row, -1);\n\n        return wildcardMatchingUtil(S1, S2, n - 1, m - 1, dp);\n    }\n}\n\n// Driver code\npublic class Main {\n    public static void main(String[] args) {\n        String S1 = \"ab*cd\";\n        String S2 = \"abdefcd\";\n\n        Solution obj = new Solution();\n\n        if (obj.wildcardMatching(S1, S2))\n            System.out.println(\"String S1 and S2 do match\");\n        else\n            System.out.println(\"String S1 and S2 do not match\");\n    }\n}",
        "python": "class Solution:\n    # Function to check if substring S1[0..i] contains only '*'\n    def isAllStars(self, S1, i):\n        for j in range(i + 1):\n            if S1[j] != '*':\n                return False\n        return True\n\n    # Recursive function with memoization\n    def wildcardMatchingUtil(self, S1, S2, i, j, dp):\n        # Base Case 1: Both pattern and text fully matched\n        if i < 0 and j < 0:\n            return True\n\n        # Base Case 2: Pattern exhausted but text remains\n        if i < 0 and j >= 0:\n            return False\n\n        # Base Case 3: Text exhausted but pattern may have '*'\n        if j < 0 and i >= 0:\n            return self.isAllStars(S1, i)\n\n        # If already computed, return stored result\n        if dp[i][j] != -1:\n            return dp[i][j] == 1\n\n        # If characters match or pattern has '?'\n        if S1[i] == S2[j] or S1[i] == '?':\n            dp[i][j] = 1 if self.wildcardMatchingUtil(S1, S2, i - 1, j - 1, dp) else 0\n\n        # If pattern has '*', try both possibilities\n        elif S1[i] == '*':\n            dp[i][j] = 1 if (self.wildcardMatchingUtil(S1, S2, i - 1, j, dp) or\n                              self.wildcardMatchingUtil(S1, S2, i, j - 1, dp)) else 0\n\n        # If characters don't match\n        else:\n            dp[i][j] = 0\n\n        return dp[i][j] == 1\n\n    # Main function to start matching\n    def wildcardMatching(self, S1, S2):\n        n = len(S1)\n        m = len(S2)\n\n        # DP array initialized to -1\n        dp = [[-1 for _ in range(m)] for _ in range(n)]\n\n        return self.wildcardMatchingUtil(S1, S2, n - 1, m - 1, dp)\n\n\n# Driver code\nif __name__ == \"__main__\":\n    S1 = \"ab*cd\"\n    S2 = \"abdefcd\"\n\n    obj = Solution()\n\n    if obj.wildcardMatching(S1, S2):\n        print(\"String S1 and S2 do match\")\n    else:\n        print(\"String S1 and S2 do not match\")",
        "javascript": "class Solution {\n    // Function to check if substring S1[0..i] contains only '*'\n    isAllStars(S1, i) {\n        for (let j = 0; j <= i; j++) {\n            if (S1[j] !== '*') return false;\n        }\n        return true;\n    }\n\n    // Recursive function with memoization\n    wildcardMatchingUtil(S1, S2, i, j, dp) {\n        // Base Case 1: Both pattern and text matched\n        if (i < 0 && j < 0) return true;\n\n        // Base Case 2: Pattern exhausted but text remains\n        if (i < 0 && j >= 0) return false;\n\n        // Base Case 3: Text exhausted but pattern may have '*'\n        if (j < 0 && i >= 0) return this.isAllStars(S1, i);\n\n        // If already computed, return stored value\n        if (dp[i][j] !== -1) return dp[i][j] === 1;\n\n        // If characters match or pattern has '?'\n        if (S1[i] === S2[j] || S1[i] === '?') {\n            dp[i][j] = this.wildcardMatchingUtil(S1, S2, i - 1, j - 1, dp) ? 1 : 0;\n        }\n        // If pattern has '*', try both options\n        else if (S1[i] === '*') {\n            dp[i][j] = (this.wildcardMatchingUtil(S1, S2, i - 1, j, dp) ||\n                        this.wildcardMatchingUtil(S1, S2, i, j - 1, dp)) ? 1 : 0;\n        }\n        // If characters don't match\n        else {\n            dp[i][j] = 0;\n        }\n\n        return dp[i][j] === 1;\n    }\n\n    // Main function\n    wildcardMatching(S1, S2) {\n        const n = S1.length;\n        const m = S2.length;\n\n        // Initialize DP array with -1\n        const dp = Array.from({ length: n }, () => Array(m).fill(-1));\n\n        return this.wildcardMatchingUtil(S1, S2, n - 1, m - 1, dp);\n    }\n}\n\n// Driver code\nconst S1 = \"ab*cd\";\nconst S2 = \"abdefcd\";\n\nconst obj = new Solution();\n\nif (obj.wildcardMatching(S1, S2))\n    console.log(\"String S1 and S2 do match\");\nelse\n    console.log(\"String S1 and S2 do not match\");"
      }
    },
    {
      "approach_name": "Tabulation Approach",
      "algorithm": "In the recursive logic, we set the base case too if(i<0 ) and if(j<0) but we can’t set the dp array’s index to -1. Therefore a hack for this issue is to shift every index by 1 towards the right.\n\nFirst we initialise the dp array of size [n+1][m+1] as zero.Next, we set the base condition (keep in mind 1-based indexing), we set the top-left cell as ‘true’, then we set the first column’s value as ‘false’; and for the first row, we will run isAllStars() for every cell value.Similarly, we will implement the recursive code by keeping in mind the shifting of indexes, therefore S1[i] will be converted to S1[i-1]. Same for S2.At last we will print dp[n][m] as our answer.",
      "time_complexity": "O(N*M)",
      "space_complexity": "O(N*M)",
      "time_complexity_details": "Time Complexity: O(N*M),There are two nested loops\nSpace Complexity: O(N*M),We are using an external array of size ‘N*M’. Stack Space is eliminated.",
      "space_complexity_details": "Time Complexity: O(N*M),There are two nested loops\nSpace Complexity: O(N*M),We are using an external array of size ‘N*M’. Stack Space is eliminated.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Helper function to check if all first i characters in the pattern are '*'\n    // This is important for the base case: '*' can match an empty string\n    bool isAllStars(string &S1, int i) {\n        // Loop from first character up to i-th character\n        for (int j = 1; j <= i; j++) {\n            // If any character is not '*', return false\n            if (S1[j - 1] != '*')\n                return false;\n        }\n        // All characters were '*', so return true\n        return true;\n    }\n\n    // Function to perform wildcard pattern matching using tabulation DP\n    bool wildcardMatching(string &S1, string &S2) {\n        // n = length of pattern\n        int n = S1.size();\n        // m = length of text\n        int m = S2.size();\n\n        // Create a DP table of size (n+1) x (m+1)\n        // dp[i][j] = true if pattern[0...i-1] matches text[0...j-1]\n        vector<vector<bool>> dp(n + 1, vector<bool>(m + 1, false));\n\n        // Base case: empty pattern matches empty string\n        dp[0][0] = true;\n\n        // Base case: empty pattern cannot match a non-empty string\n        for (int j = 1; j <= m; j++) {\n            dp[0][j] = false;\n        }\n\n        // Base case: pattern can match empty string only if it is made of all '*'\n        for (int i = 1; i <= n; i++) {\n            dp[i][0] = isAllStars(S1, i);\n        }\n\n        // Fill DP table row by row\n        for (int i = 1; i <= n; i++) {\n            for (int j = 1; j <= m; j++) {\n\n                // Case 1: Exact match or '?', which matches any single character\n                if (S1[i - 1] == S2[j - 1] || S1[i - 1] == '?') {\n                    // Take value from the diagonal cell\n                    dp[i][j] = dp[i - 1][j - 1];\n                }\n\n                // Case 2: '*' can match zero or more characters\n                else if (S1[i - 1] == '*') {\n                    // Match zero characters → take value from above\n                    // Match one or more characters → take value from left\n                    dp[i][j] = dp[i - 1][j] || dp[i][j - 1];\n                }\n\n                // Case 3: No match\n                else {\n                    dp[i][j] = false;\n                }\n            }\n        }\n\n        // Final result is stored in dp[n][m]\n        return dp[n][m];\n    }\n};\n\nint main() {\n    // Create an object of Solution class\n    Solution sol;\n\n    // Define the pattern string (S1) and text string (S2)\n    string S1 = \"ab*cd\";\n    string S2 = \"abdefcd\";\n\n    // Call the function and check if pattern matches text\n    bool result = sol.wildcardMatching(S1, S2);\n\n    // Output the result\n    if (result)\n        cout << \"String S1 and S2 do match\";\n    else\n        cout << \"String S1 and S2 do not match\";\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n\n    // Helper function to check if the first i characters of the pattern are all '*'\n    public boolean isAllStars(String S1, int i) {\n        // Loop through the first i characters\n        for (int j = 1; j <= i; j++) {\n            // If any character is not '*', return false\n            if (S1.charAt(j - 1) != '*')\n                return false;\n        }\n        // All were '*', so return true\n        return true;\n    }\n\n    // Function to check if the pattern S1 matches the string S2\n    public boolean wildcardMatching(String S1, String S2) {\n        // Get lengths of pattern and text\n        int n = S1.length();\n        int m = S2.length();\n\n        // Create a DP table where dp[i][j] tells if pattern[0...i-1] matches text[0...j-1]\n        boolean[][] dp = new boolean[n + 1][m + 1];\n\n        // Base case: empty pattern matches empty string\n        dp[0][0] = true;\n\n        // Base case: empty pattern cannot match non-empty string\n        for (int j = 1; j <= m; j++) {\n            dp[0][j] = false;\n        }\n\n        // Base case: pattern can match empty string only if it contains all '*'\n        for (int i = 1; i <= n; i++) {\n            dp[i][0] = isAllStars(S1, i);\n        }\n\n        // Fill the DP table row by row\n        for (int i = 1; i <= n; i++) {\n            for (int j = 1; j <= m; j++) {\n\n                // Case 1: Characters match exactly or pattern has '?'\n                if (S1.charAt(i - 1) == S2.charAt(j - 1) || S1.charAt(i - 1) == '?') {\n                    dp[i][j] = dp[i - 1][j - 1];\n                }\n\n                // Case 2: Pattern has '*'\n                else if (S1.charAt(i - 1) == '*') {\n                    // '*' matches zero characters (dp[i-1][j]) OR\n                    // '*' matches one/more characters (dp[i][j-1])\n                    dp[i][j] = dp[i - 1][j] || dp[i][j - 1];\n                }\n\n                // Case 3: No match\n                else {\n                    dp[i][j] = false;\n                }\n            }\n        }\n\n        // Final answer is stored at dp[n][m]\n        return dp[n][m];\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Create Solution object\n        Solution sol = new Solution();\n\n        // Define pattern and text\n        String S1 = \"ab*cd\";\n        String S2 = \"abdefcd\";\n\n        // Check match\n        boolean result = sol.wildcardMatching(S1, S2);\n\n        // Print result\n        if (result)\n            System.out.println(\"String S1 and S2 do match\");\n        else\n            System.out.println(\"String S1 and S2 do not match\");\n    }\n}",
        "python": "class Solution:\n    # Helper function to check if first i characters are all '*'\n    def isAllStars(self, S1, i):\n        # Loop from index 0 to i-1\n        for j in range(1, i + 1):\n            # If any character is not '*', return False\n            if S1[j - 1] != '*':\n                return False\n        # All were '*', so return True\n        return True\n\n    # Function to check if S1 matches S2\n    def wildcardMatching(self, S1, S2):\n        # Lengths of pattern and text\n        n = len(S1)\n        m = len(S2)\n\n        # Create DP table with default False\n        dp = [[False] * (m + 1) for _ in range(n + 1)]\n\n        # Base case: empty pattern matches empty string\n        dp[0][0] = True\n\n        # Base case: empty pattern cannot match non-empty string\n        for j in range(1, m + 1):\n            dp[0][j] = False\n\n        # Base case: pattern can match empty string if it has all '*'\n        for i in range(1, n + 1):\n            dp[i][0] = self.isAllStars(S1, i)\n\n        # Fill DP table\n        for i in range(1, n + 1):\n            for j in range(1, m + 1):\n\n                # Case 1: Exact match or '?'\n                if S1[i - 1] == S2[j - 1] or S1[i - 1] == '?':\n                    dp[i][j] = dp[i - 1][j - 1]\n\n                # Case 2: '*'\n                elif S1[i - 1] == '*':\n                    # Zero characters OR one/more characters\n                    dp[i][j] = dp[i - 1][j] or dp[i][j - 1]\n\n                # Case 3: No match\n                else:\n                    dp[i][j] = False\n\n        # Return final answer\n        return dp[n][m]\n\n\n# Driver code\nif __name__ == \"__main__\":\n    sol = Solution()\n    S1 = \"ab*cd\"\n    S2 = \"abdefcd\"\n\n    result = sol.wildcardMatching(S1, S2)\n    if result:\n        print(\"String S1 and S2 do match\")\n    else:\n        print(\"String S1 and S2 do not match\")",
        "javascript": "class Solution {\n    // Helper function to check if first i characters are all '*'\n    isAllStars(S1, i) {\n        for (let j = 1; j <= i; j++) {\n            if (S1[j - 1] !== '*') {\n                return false;\n            }\n        }\n        return true;\n    }\n\n    // Function to check if S1 matches S2\n    wildcardMatching(S1, S2) {\n        const n = S1.length;\n        const m = S2.length;\n\n        // Create 2D array filled with false\n        const dp = Array.from({ length: n + 1 }, () => Array(m + 1).fill(false));\n\n        // Base case: empty pattern matches empty string\n        dp[0][0] = true;\n\n        // Base case: empty pattern cannot match non-empty string\n        for (let j = 1; j <= m; j++) {\n            dp[0][j] = false;\n        }\n\n        // Base case: pattern matches empty string if it has all '*'\n        for (let i = 1; i <= n; i++) {\n            dp[i][0] = this.isAllStars(S1, i);\n        }\n\n        // Fill DP table\n        for (let i = 1; i <= n; i++) {\n            for (let j = 1; j <= m; j++) {\n\n                // Case 1: Exact match or '?'\n                if (S1[i - 1] === S2[j - 1] || S1[i - 1] === '?') {\n                    dp[i][j] = dp[i - 1][j - 1];\n                }\n\n                // Case 2: '*'\n                else if (S1[i - 1] === '*') {\n                    dp[i][j] = dp[i - 1][j] || dp[i][j - 1];\n                }\n\n                // Case 3: No match\n                else {\n                    dp[i][j] = false;\n                }\n            }\n        }\n\n        // Return final result\n        return dp[n][m];\n    }\n}\n\n// Driver code\nconst sol = new Solution();\nconst S1 = \"ab*cd\";\nconst S2 = \"abdefcd\";\n\nconst result = sol.wildcardMatching(S1, S2);\nif (result) {\n    console.log(\"String S1 and S2 do match\");\n} else {\n    console.log(\"String S1 and S2 do not match\");\n}"
      }
    },
    {
      "approach_name": "Space Optimization Approach",
      "algorithm": "If we closely look the relation,\ndp[i][j] =  dp[i-1][j-1], dp[i][j] = dp[i-1][j] ||dp[i][j-1] \nWe see that to calculate a value of a cell of the dp array, we need only the previous row values (say prev) and current row’s previous columns values. So, we don’t need to store an entire array. Hence we can space optimise it.\nWe take two rows ‘prev’ and ‘cur’.We initialize it to the base condition. We first initialize the prev row. Its first value needs to be true. Rest all the values of the prev row meeds to be false.Moreover, the cur variable whenever declared should have its first cell’s value given by isAllStarts() function.Next, we implement the memoization logic. We replace dp[i-1] with prev and dp[i] by cur.After every inner loop execution, we set prev=cur, for the next iteration.At last, we return prev[m] as our answer.",
      "time_complexity": "O(N*M)",
      "space_complexity": "O(N*M)",
      "time_complexity_details": "Time Complexity: O(N*M),There are two nested loops.\nSpace Complexity: O(M),We are using an external array of size ‘M+1’ to store two rows.",
      "space_complexity_details": "Time Complexity: O(N*M),There are two nested loops.\nSpace Complexity: O(M),We are using an external array of size ‘M+1’ to store two rows.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Class containing the logic for wildcard pattern matching\nclass Solution {\npublic:\n\n    // Function to check if all characters in the substring of S1 (up to index i) are '*'\n    bool isAllStars(string &S1, int i) {\n        // Loop through each character from index 0 to i-1\n        for (int j = 1; j <= i; j++) {\n            // If any character is not '*', return false\n            if (S1[j - 1] != '*')\n                return false;\n        }\n        // If loop finishes, it means all characters were '*'\n        return true;\n    }\n\n    // Function to perform wildcard pattern matching between S1 (pattern) and S2 (text)\n    bool wildcardMatching(string &S1, string &S2) {\n        // Store lengths of both strings\n        int n = S1.size();\n        int m = S2.size();\n\n        // Create two boolean arrays to store the matching results of the previous and current rows\n        vector<bool> prev(m + 1, false);\n        vector<bool> cur(m + 1, false);\n\n        // Empty pattern matches with empty string\n        prev[0] = true;\n\n        // Iterate through pattern characters\n        for (int i = 1; i <= n; i++) {\n            // For the first column, check if the pattern till i-th character is all '*'\n            cur[0] = isAllStars(S1, i);\n\n            // Iterate through text characters\n            for (int j = 1; j <= m; j++) {\n                // If characters match or pattern has '?', take diagonal value from DP table\n                if (S1[i - 1] == S2[j - 1] || S1[i - 1] == '?') {\n                    cur[j] = prev[j - 1];\n                }\n                // If pattern has '*', it can represent empty sequence or any character\n                else if (S1[i - 1] == '*') {\n                    cur[j] = prev[j] || cur[j - 1];\n                }\n                // If characters don't match and no wildcard present\n                else {\n                    cur[j] = false;\n                }\n            }\n\n            // Move current row to previous row for next iteration\n            prev = cur;\n        }\n\n        // Final answer is whether the whole pattern matches the whole text\n        return prev[m];\n    }\n};\n\n// Driver code\nint main() {\n    // Create object of Solution class\n    Solution obj;\n\n    // Example pattern with wildcard characters\n    string S1 = \"ab*cd\";\n\n    // Example text to match\n    string S2 = \"abdefcd\";\n\n    // Call the wildcardMatching function\n    bool result = obj.wildcardMatching(S1, S2);\n\n    // Print the result based on matching outcome\n    if (result)\n        cout << \"String S1 and S2 do match\";\n    else\n        cout << \"String S1 and S2 do not match\";\n\n    return 0;\n}",
        "java": "import java.util.*;\n\n// Class containing the logic for wildcard pattern matching\nclass Solution {\n\n    // Helper function to check if all characters in S1[0...i-1] are '*'\n    public boolean isAllStars(String S1, int i) {\n        for (int j = 1; j <= i; j++) {\n            if (S1.charAt(j - 1) != '*') return false;\n        }\n        return true;\n    }\n\n    // Function to perform wildcard matching\n    public boolean wildcardMatching(String S1, String S2) {\n        int n = S1.length();\n        int m = S2.length();\n\n        // Previous row results\n        boolean[] prev = new boolean[m + 1];\n        // Current row results\n        boolean[] cur = new boolean[m + 1];\n\n        // Empty pattern matches empty string\n        prev[0] = true;\n\n        // Iterate over each character of pattern\n        for (int i = 1; i <= n; i++) {\n            // Check if first column should be true\n            cur[0] = isAllStars(S1, i);\n\n            // Iterate over each character of text\n            for (int j = 1; j <= m; j++) {\n                char pChar = S1.charAt(i - 1);\n                char tChar = S2.charAt(j - 1);\n\n                if (pChar == tChar || pChar == '?') {\n                    cur[j] = prev[j - 1];\n                } else if (pChar == '*') {\n                    cur[j] = prev[j] || cur[j - 1];\n                } else {\n                    cur[j] = false;\n                }\n            }\n\n            // Move current row to previous\n            prev = Arrays.copyOf(cur, m + 1);\n        }\n\n        return prev[m];\n    }\n}\n\n// Driver code\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        String S1 = \"ab*cd\";\n        String S2 = \"abdefcd\";\n\n        if (sol.wildcardMatching(S1, S2))\n            System.out.println(\"String S1 and S2 do match\");\n        else\n            System.out.println(\"String S1 and S2 do not match\");\n    }\n}",
        "python": "class Solution:\n    # Helper function to check if all characters in S1[0...i-1] are '*'\n    # This matters because '*' can match an empty sequence\n    def isAllStars(self, S1, i):\n        # Loop through the first i characters\n        for j in range(1, i + 1):\n            # If any character is NOT '*', return False\n            if S1[j - 1] != '*':\n                return False\n        # If all are '*', return True\n        return True\n\n    # Main function to perform wildcard pattern matching\n    def wildcardMatching(self, S1, S2):\n        # Length of pattern\n        n = len(S1)\n        # Length of text\n        m = len(S2)\n\n        # prev[j] → stores result of matching pattern[0...i-1] with text[0...j-1] for previous row\n        prev = [False] * (m + 1)\n        # cur[j] → stores result for current row\n        cur = [False] * (m + 1)\n\n        # Base case: Empty pattern matches empty string\n        prev[0] = True\n\n        # Outer loop → iterate over each character in the pattern\n        for i in range(1, n + 1):\n            # For first column (j = 0), match is possible only if pattern so far is all '*'\n            cur[0] = self.isAllStars(S1, i)\n\n            # Inner loop → iterate over each character in the text\n            for j in range(1, m + 1):\n                pChar = S1[i - 1]  # Current pattern character\n                tChar = S2[j - 1]  # Current text character\n\n                # Case 1: Characters match OR '?' wildcard matches exactly one character\n                if pChar == tChar or pChar == '?':\n                    cur[j] = prev[j - 1]  # Take diagonal value\n\n                # Case 2: '*' wildcard can match:\n                #    - Zero characters → take value from prev[j]\n                #    - One or more characters → take value from cur[j - 1]\n                elif pChar == '*':\n                    cur[j] = prev[j] or cur[j - 1]\n\n                # Case 3: Characters don't match and not a wildcard\n                else:\n                    cur[j] = False\n\n            # Move current row into prev for next iteration\n            prev = cur[:]\n\n        # Answer is stored in prev[m]\n        return prev[m]\n\n\n# Driver code to test\nif __name__ == \"__main__\":\n    sol = Solution()\n    S1 = \"ab*cd\"       # Pattern containing wildcard '*'\n    S2 = \"abdefcd\"     # Text to match\n\n    # Call matching function and print result\n    if sol.wildcardMatching(S1, S2):\n        print(\"String S1 and S2 do match\")\n    else:\n        print(\"String S1 and S2 do not match\")",
        "javascript": "class Solution {\n    // Helper function: Check if all first i characters in pattern are '*'\n    // '*' can match empty string, so this is important for base cases\n    isAllStars(S1, i) {\n        for (let j = 1; j <= i; j++) {\n            if (S1[j - 1] !== '*') return false; // Found a non-* char → return false\n        }\n        return true; // All chars are '*'\n    }\n\n    // Main function: Wildcard pattern matching\n    wildcardMatching(S1, S2) {\n        const n = S1.length; // Length of pattern\n        const m = S2.length; // Length of text\n\n        // prev[j] → previous row DP state\n        let prev = new Array(m + 1).fill(false);\n        // cur[j] → current row DP state\n        let cur = new Array(m + 1).fill(false);\n\n        // Base case: Empty pattern matches empty text\n        prev[0] = true;\n\n        // Outer loop → iterate over each pattern character\n        for (let i = 1; i <= n; i++) {\n            // First column case → true only if pattern so far is all '*'\n            cur[0] = this.isAllStars(S1, i);\n\n            // Inner loop → iterate over each text character\n            for (let j = 1; j <= m; j++) {\n                let pChar = S1[i - 1]; // Current pattern char\n                let tChar = S2[j - 1]; // Current text char\n\n                // Case 1: Exact match or '?' wildcard (matches one character)\n                if (pChar === tChar || pChar === '?') {\n                    cur[j] = prev[j - 1]; // Take diagonal DP state\n                }\n                // Case 2: '*' wildcard\n                // '*' can represent:\n                // - zero characters (prev[j])\n                // - one or more characters (cur[j - 1])\n                else if (pChar === '*') {\n                    cur[j] = prev[j] || cur[j - 1];\n                }\n                // Case 3: No match\n                else {\n                    cur[j] = false;\n                }\n            }\n\n            // Copy current row into prev for next iteration\n            prev = [...cur];\n        }\n\n        // Final result is in prev[m]\n        return prev[m];\n    }\n}\n\n// Driver code to test\nconst sol = new Solution();\nconst S1 = \"ab*cd\";     // Pattern\nconst S2 = \"abdefcd\";   // Text\n\n// Call function and print result\nif (sol.wildcardMatching(S1, S2)) {\n    console.log(\"String S1 and S2 do match\");\n} else {\n    console.log(\"String S1 and S2 do not match\");\n}"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:24Z"
}