{
  "problem_id": "318",
  "problem_name": "Count subsets with sum K",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 4,
  "substep_name": "DP on Subsequences",
  "difficulty": "Hard",
  "youtube_url": "https://www.youtube.com/watch?v=ZHyb-A2Mte4",
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/count-subsets-with-sum-k-dp-17/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/count-subsets-with-sum-k",
  "problem_statement": "Problem Statement : Given an array arr of n integers and an integer K, count the number of subsets of the given array that have a sum equal to K.",
  "examples": [
    "Input: arr = [1, 2, 2, 3], K = 3\nOutput: 3\n\nExplanation: The subarrays [1,2], [1,2] and [3] have a sum of 3. \n\nInput: arr = [1, 2, 3, 4, 5], K = 5\nOutput: 3\nExplanation: The subsets are [5], [2, 3], and [1, 4]."
  ],
  "total_approaches": 3,
  "approaches": [
    {
      "approach_name": "Memoization",
      "algorithm": "The problem of counting subsets with a given sum K can be thought of as a decision process where, for each element in the array, we either include it in the subset or exclude it. If we include it, the target reduces by that element’s value and if we exclude it, the target remains the same. This naturally forms a recursion. However, many subproblems repeat, such as computing the number of ways to reach the same target with the same index again and again. To avoid redundant work, we use memoization, where each state defined by the pair (index, target) is stored after being computed once. This way, instead of exploring an exponential number of recursive paths, we reuse previously computed results, bringing the complexity down to polynomial time.\n\nDefine a recursive function that takes the current index and the remaining target sum as parameters\nIf the target becomes zero, return 1 because one valid subset is found\nIf the index is zero, check if the first element equals the target, and return 1 if true, else 0\nBefore solving, check if the result for this state is already stored in a dp table, and if yes, return it\nRecursively call the function by excluding the current element, keeping the target unchanged\nIf the current element is less than or equal to the target, also call the function by including it and reducing the target\nAdd the results of including and excluding to get the total number of subsets for this state\nStore the result in the dp table and return it",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to count subsets that sum up to target\n    int countSubsets(vector<int>& nums, int target) {\n        // Initialize dp table with -1 (uncomputed states)\n        vector<vector<int>> dp(nums.size(), vector<int>(target + 1, -1));\n        return solve(nums.size() - 1, target, nums, dp);\n    }\n\nprivate:\n    // Recursive helper with memoization\n    int solve(int index, int target, vector<int>& nums, vector<vector<int>>& dp) {\n        // Base case: if target is 0, we found a valid subset\n        if (target == 0) return 1;\n\n        // Base case: if we are at index 0, check if nums[0] equals target\n        if (index == 0) return (nums[0] == target ? 1 : 0);\n\n        // If already computed, return from dp\n        if (dp[index][target] != -1) return dp[index][target];\n\n        // Case 1: Exclude current element\n        int notTake = solve(index - 1, target, nums, dp);\n\n        // Case 2: Include current element (if it is not greater than target)\n        int take = 0;\n        if (nums[index] <= target) {\n            take = solve(index - 1, target - nums[index], nums, dp);\n        }\n\n        // Store result in dp and return\n        return dp[index][target] = take + notTake;\n    }\n};\n\nint main() {\n    vector<int> nums = {1, 2, 3, 3};\n    int target = 6;\n    Solution obj;\n    cout << obj.countSubsets(nums, target) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to count subsets that sum up to target\n    public int countSubsets(int[] nums, int target) {\n        // Initialize dp table with -1 (uncomputed states)\n        int[][] dp = new int[nums.length][target + 1];\n        for (int[] row : dp) Arrays.fill(row, -1);\n        return solve(nums.length - 1, target, nums, dp);\n    }\n\n    // Recursive helper with memoization\n    private int solve(int index, int target, int[] nums, int[][] dp) {\n        // Base case: if target is 0, we found a valid subset\n        if (target == 0) return 1;\n\n        // Base case: if we are at index 0, check if nums[0] equals target\n        if (index == 0) return (nums[0] == target ? 1 : 0);\n\n        // If already computed, return from dp\n        if (dp[index][target] != -1) return dp[index][target];\n\n        // Case 1: Exclude current element\n        int notTake = solve(index - 1, target, nums, dp);\n\n        // Case 2: Include current element (if it is not greater than target)\n        int take = 0;\n        if (nums[index] <= target) {\n            take = solve(index - 1, target - nums[index], nums, dp);\n        }\n\n        // Store result in dp and return\n        return dp[index][target] = take + notTake;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution obj = new Solution();\n        int[] nums = {1, 2, 3, 3};\n        int target = 6;\n        System.out.println(obj.countSubsets(nums, target));\n    }\n}",
        "python": "class Solution:\n    # Function to count subsets that sum up to target\n    def countSubsets(self, nums, target):\n        # Initialize dp table with -1 (uncomputed states)\n        dp = [[-1] * (target + 1) for _ in range(len(nums))]\n        return self.solve(len(nums) - 1, target, nums, dp)\n\n    # Recursive helper with memoization\n    def solve(self, index, target, nums, dp):\n        # Base case: if target is 0, we found a valid subset\n        if target == 0:\n            return 1\n\n        # Base case: if we are at index 0, check if nums[0] equals target\n        if index == 0:\n            return 1 if nums[0] == target else 0\n\n        # If already computed, return from dp\n        if dp[index][target] != -1:\n            return dp[index][target]\n\n        # Case 1: Exclude current element\n        notTake = self.solve(index - 1, target, nums, dp)\n\n        # Case 2: Include current element (if it is not greater than target)\n        take = 0\n        if nums[index] <= target:\n            take = self.solve(index - 1, target - nums[index], nums, dp)\n\n        # Store result in dp and return\n        dp[index][target] = take + notTake\n        return dp[index][target]\n\n\n# Driver code\nnums = [1, 2, 3, 3]\ntarget = 6\nobj = Solution()\nprint(obj.countSubsets(nums, target))",
        "javascript": "class Solution {\n    // Function to count subsets that sum up to target\n    countSubsets(nums, target) {\n        // Initialize dp table with -1 (uncomputed states)\n        let dp = Array.from({ length: nums.length }, () =>\n            Array(target + 1).fill(-1)\n        );\n        return this.solve(nums.length - 1, target, nums, dp);\n    }\n\n    // Recursive helper with memoization\n    solve(index, target, nums, dp) {\n        // Base case: if target is 0, we found a valid subset\n        if (target === 0) return 1;\n\n        // Base case: if we are at index 0, check if nums[0] equals target\n        if (index === 0) return nums[0] === target ? 1 : 0;\n\n        // If already computed, return from dp\n        if (dp[index][target] !== -1) return dp[index][target];\n\n        // Case 1: Exclude current element\n        let notTake = this.solve(index - 1, target, nums, dp);\n\n        // Case 2: Include current element (if it is not greater than target)\n        let take = 0;\n        if (nums[index] <= target) {\n            take = this.solve(index - 1, target - nums[index], nums, dp);\n        }\n\n        // Store result in dp and return\n        dp[index][target] = take + notTake;\n        return dp[index][target];\n    }\n}\n\n// Driver code\nlet nums = [1, 2, 3, 3];\nlet target = 6;\nlet obj = new Solution();\nconsole.log(obj.countSubsets(nums, target));"
      }
    },
    {
      "approach_name": "Tabulation",
      "algorithm": "The problem of counting subsets with a given sum K can be solved using tabulation by building the solution bottom-up. Instead of starting with recursion, we construct a dp table where each entry dp[i][t] represents the number of subsets that can be formed using the first i elements to achieve the sum t. We iteratively fill this table, considering for each element whether we include it or exclude it. This way, we systematically compute the answer without recursion and avoid stack overhead, while still ensuring that overlapping subproblems are reused effectively.\n\nCreate a 2D dp table where dp[i][t] means number of subsets using first i elements to make sum t\nInitialize dp[0][0] as 1 because with zero elements, only one subset (empty set) makes sum 0\nIf the first element is less than or equal to K, set dp[0][arr[0]] as 1 because a single element can form that sum\nIterate over all elements from index 1 to n-1\nFor each element, iterate over all target sums from 0 to K\nFor each dp[i][t], first take the value from dp[i-1][t] (excluding current element)\nIf current element is less than or equal to t, also add dp[i-1][t - arr[i]] (including current element)\nAt the end, dp[n-1][K] will hold the total number of subsets with sum equal to K",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    int countSubsets(vector<int>& arr, int K) {\n        // Get number of elements\n        int n = arr.size();\n\n        // Create dp table with dimensions n x (K+1)\n        vector<vector<int>> dp(n, vector<int>(K + 1, 0));\n\n        // Base case: one subset (empty set) makes sum 0\n        dp[0][0] = 1;\n\n        // If first element is <= K, mark dp[0][arr[0]] as 1\n        if (arr[0] <= K) dp[0][arr[0]] = 1;\n\n        // Fill the table\n        for (int i = 1; i < n; i++) {\n            for (int target = 0; target <= K; target++) {\n                // Exclude current element\n                int notTake = dp[i - 1][target];\n\n                // Include current element if possible\n                int take = 0;\n                if (arr[i] <= target) take = dp[i - 1][target - arr[i]];\n\n                // Total ways\n                dp[i][target] = notTake + take;\n            }\n        }\n\n        // Final answer\n        return dp[n - 1][K];\n    }\n};\n\nint main() {\n    Solution obj;\n    vector<int> arr = {1, 2, 3, 3};\n    int K = 6;\n    cout << obj.countSubsets(arr, K) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    public int countSubsets(int[] arr, int K) {\n        // Get number of elements\n        int n = arr.length;\n\n        // Create dp table with dimensions n x (K+1)\n        int[][] dp = new int[n][K + 1];\n\n        // Base case: one subset (empty set) makes sum 0\n        dp[0][0] = 1;\n\n        // If first element is <= K, mark dp[0][arr[0]] as 1\n        if (arr[0] <= K) dp[0][arr[0]] = 1;\n\n        // Fill the table\n        for (int i = 1; i < n; i++) {\n            for (int target = 0; target <= K; target++) {\n                // Exclude current element\n                int notTake = dp[i - 1][target];\n\n                // Include current element if possible\n                int take = 0;\n                if (arr[i] <= target) take = dp[i - 1][target - arr[i]];\n\n                // Total ways\n                dp[i][target] = notTake + take;\n            }\n        }\n\n        // Final answer\n        return dp[n - 1][K];\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution obj = new Solution();\n        int[] arr = {1, 2, 3, 3};\n        int K = 6;\n        System.out.println(obj.countSubsets(arr, K));\n    }\n}",
        "python": "class Solution:\n    def countSubsets(self, arr, K):\n        # Get number of elements\n        n = len(arr)\n\n        # Create dp table with dimensions n x (K+1)\n        dp = [[0] * (K + 1) for _ in range(n)]\n\n        # Base case: one subset (empty set) makes sum 0\n        dp[0][0] = 1\n\n        # If first element is <= K, mark dp[0][arr[0]] as 1\n        if arr[0] <= K:\n            dp[0][arr[0]] = 1\n\n        # Fill the table\n        for i in range(1, n):\n            for target in range(K + 1):\n                # Exclude current element\n                notTake = dp[i - 1][target]\n\n                # Include current element if possible\n                take = 0\n                if arr[i] <= target:\n                    take = dp[i - 1][target - arr[i]]\n\n                # Total ways\n                dp[i][target] = notTake + take\n\n        # Final answer\n        return dp[n - 1][K]\n\n\n# Driver code\narr = [1, 2, 3, 3]\nK = 6\nobj = Solution()\nprint(obj.countSubsets(arr, K))",
        "javascript": "class Solution {\n    countSubsets(arr, K) {\n        // Get number of elements\n        let n = arr.length;\n\n        // Create dp table with dimensions n x (K+1)\n        let dp = Array.from({ length: n }, () => Array(K + 1).fill(0));\n\n        // Base case: one subset (empty set) makes sum 0\n        dp[0][0] = 1;\n\n        // If first element is <= K, mark dp[0][arr[0]] as 1\n        if (arr[0] <= K) dp[0][arr[0]] = 1;\n\n        // Fill the table\n        for (let i = 1; i < n; i++) {\n            for (let target = 0; target <= K; target++) {\n                // Exclude current element\n                let notTake = dp[i - 1][target];\n\n                // Include current element if possible\n                let take = 0;\n                if (arr[i] <= target) take = dp[i - 1][target - arr[i]];\n\n                // Total ways\n                dp[i][target] = notTake + take;\n            }\n        }\n\n        // Final answer\n        return dp[n - 1][K];\n    }\n}\n\n// Driver code\nlet arr = [1, 2, 3, 3];\nlet K = 6;\nlet obj = new Solution();\nconsole.log(obj.countSubsets(arr, K));"
      }
    },
    {
      "approach_name": "Space Optimization",
      "algorithm": "The problem of counting subsets with a given sum K can also be solved with space optimization.\nInstead of storing results for all n rows, we notice that dp[i][t] only depends on the previous row. Thus instead of storing entire table, we can only store the previous row.\n\n Create a 1D dp array of size (K + 1), where dp[t] represents number of subsets that sum to t Initialize dp[0] = 1 because there’s always one subset (empty set) with sum 0 If the first element is less than or equal to K, set dp[arr[0]] = 1 Iterate through the array from index 1 to n-1 For each element, traverse target sums backwards from K down to arr[i] Update dp[t] by adding dp[t - arr[i]], meaning we include the current element Backward traversal ensures we don’t overwrite values that are yet to be used At the end, dp[K] will hold the total number of subsets that sum to K",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    int countSubsets(vector<int>& arr, int K) {\n        // Create a dp array of size K+1 initialized to 0\n        vector<int> dp(K + 1, 0);\n\n        // Base case: One subset (empty set) makes sum 0\n        dp[0] = 1;\n\n        // If first element <= K, mark it as a subset\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Loop through elements starting from index 1\n        for (int i = 1; i < arr.size(); i++) {\n            // Create a temporary dp array for current element\n            vector<int> curr(K + 1, 0);\n\n            // Empty set always makes sum 0\n            curr[0] = 1;\n\n            // Iterate over all possible targets\n            for (int t = 0; t <= K; t++) {\n                // Exclude current element\n                int notTake = dp[t];\n\n                // Include current element if possible\n                int take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n\n                // Total ways = include + exclude\n                curr[t] = take + notTake;\n            }\n\n            // Update dp for next iteration\n            dp = curr;\n        }\n\n        // Return answer for sum K\n        return dp[K];\n    }\n};\n\nint main() {\n    Solution sol;\n    vector<int> arr = {1, 2, 3};\n    int K = 4;\n    cout << sol.countSubsets(arr, K) << endl; \n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    public int countSubsets(int[] arr, int K) {\n        // Create dp array of size K+1\n        int[] dp = new int[K + 1];\n\n        // Base case: Empty set makes sum 0\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Loop over elements\n        for (int i = 1; i < arr.length; i++) {\n            // Temporary array for current iteration\n            int[] curr = new int[K + 1];\n\n            // Base case: Empty set makes sum 0\n            curr[0] = 1;\n\n            // Check all target sums\n            for (int t = 0; t <= K; t++) {\n                // Exclude current element\n                int notTake = dp[t];\n\n                // Include current element if possible\n                int take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n\n                // Sum of both choices\n                curr[t] = take + notTake;\n            }\n\n            // Update dp\n            dp = curr;\n        }\n\n        // Return answer\n        return dp[K];\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] arr = {1, 2, 3};\n        int K = 4;\n        System.out.println(sol.countSubsets(arr, K)); \n    }\n}",
        "python": "class Solution:\n    def countSubsets(self, arr, K):\n        # Initialize dp array with 0\n        dp = [0] * (K + 1)\n\n        # Base case: Empty set makes sum 0\n        dp[0] = 1\n\n        # If first element <= K, mark it\n        if arr[0] <= K:\n            dp[arr[0]] += 1\n\n        # Loop through elements\n        for i in range(1, len(arr)):\n            # Temporary array for current iteration\n            curr = [0] * (K + 1)\n            curr[0] = 1\n\n            # Iterate through target sums\n            for t in range(K + 1):\n                # Exclude current element\n                notTake = dp[t]\n\n                # Include current element if possible\n                take = 0\n                if arr[i] <= t:\n                    take = dp[t - arr[i]]\n\n                # Add both cases\n                curr[t] = take + notTake\n\n            # Update dp\n            dp = curr\n\n        # Return answer\n        return dp[K]\n\n\n# Driver code\nsol = Solution()\narr = [1, 2, 3]\nK = 4\nprint(sol.countSubsets(arr, K))",
        "javascript": "class Solution {\n    countSubsets(arr, K) {\n        // Initialize dp array\n        let dp = new Array(K + 1).fill(0);\n\n        // Base case: Empty set makes sum 0\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Loop through elements\n        for (let i = 1; i < arr.length; i++) {\n            // Temporary array for current element\n            let curr = new Array(K + 1).fill(0);\n            curr[0] = 1;\n\n            // Check all target sums\n            for (let t = 0; t <= K; t++) {\n                // Exclude current element\n                let notTake = dp[t];\n\n                // Include current element if possible\n                let take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n\n                // Sum of both choices\n                curr[t] = take + notTake;\n            }\n\n            // Update dp\n            dp = curr;\n        }\n\n        // Return answer\n        return dp[K];\n    }\n}\n\n// Driver code\nlet sol = new Solution();\nlet arr = [1, 2, 3];\nlet K = 4;\nconsole.log(sol.countSubsets(arr, K));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:23Z"
}