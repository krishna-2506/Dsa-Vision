{
  "problem_id": "768",
  "problem_name": "Next Smaller Element",
  "step_no": 9,
  "step_name": "Stack and Queues [Learning, Pre-In-Post-fix, Monotonic Stack, Implementation]",
  "substep_no": 3,
  "substep_name": "Monotonic Stack/Queue Problems [VVV. Imp]",
  "difficulty": "Medium",
  "youtube_url": null,
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/next-smaller-element",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/next-smaller-element",
  "problem_statement": "Given an array of integers arr, your task is to find the Next Smaller Element (NSE) for every element in the array.\nThe Next Smaller Element for an element x is defined as the first element to the right of x that is smaller than x.\nIf there is no smaller element to the right, then the NSE is -1.",
  "examples": [
    "Example 1:\nInput: arr = [4, 8, 5, 2, 25]\nOutput: [2, 5, 2, -1, -1]\nExplanation:\n- For 4, the next smaller element is 2.\n- For 8, the next smaller element is 5.\n- For 5, the next smaller element is 2.\n- For 2, there is no smaller element to its right → -1.\n- For 25, no smaller element exists → -1.\n\nExample 2:\nInput: arr = [10, 9, 8, 7]\nOutput: [9, 8, 7, -1]\nExplanation:\nEach element’s next right neighbor is smaller.\nEach element’s next right neighbor is smaller."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Brute Force",
      "algorithm": "Initialize an answer array filled with -1\nLoop from i = 0 to n - 1\nLoop from j = i + 1 to n - 1\nIf a smaller element is found at j, update answer[i] and break\nContinue to the next i\nReturn the answer array",
      "time_complexity": "O(N^2)",
      "space_complexity": "O(N^2)",
      "time_complexity_details": "Time Complexity: O(N^2), since for each of the N elements, we might need to look at up to N-1 elements ahead.\n \nSpace Complexity: O(N), since we are using an output array of size N.",
      "space_complexity_details": "Time Complexity: O(N^2), since for each of the N elements, we might need to look at up to N-1 elements ahead.\n \nSpace Complexity: O(N), since we are using an output array of size N.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n\n    /* Function to find the next smaller \n    element for each element in the array */\n    vector<int> nextSmallerElement(vector<int>& arr) {\n        \n        int n = arr.size(); // size of array\n        \n        // To store the next smaller elements\n        vector<int> ans(n, -1);\n        \n        for (int i = 0; i < n; ++i) {\n            \n            // Get the current element\n            int currEle = arr[i];\n            \n            /* Nested loop to get the \n            next smaller element */\n            for (int j = i + 1; j < n; ++j) {\n                \n                // If the next smaller element is found\n                if (arr[j] < currEle) {\n                    \n                    // Store the next smaller element\n                    ans[i] = arr[j];\n                    \n                    // Break from the loop\n                    break;\n                }\n            }\n        }\n        \n        // Return the answer\n        return ans;\n    }\n};\n\nint main() {\n    int n = 5;\n    vector<int> arr = {4, 8, 5, 2, 25};\n\n    /* Creating an instance of \n    Solution class */\n    Solution sol;\n    \n    /* Function call to find the next smaller element\n    for each element in the array */\n    vector<int> ans = sol.nextSmallerElement(arr);\n    \n    cout << \"The next smaller elements are: \";\n    for (int i = 0; i < n; ++i) {\n        cout << ans[i] << \" \";\n    }\n    \n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n\n    // Function to find the next smaller elements\n    public int[] nextSmallerElement(int[] arr) {\n        int n = arr.length;\n\n        // Initialize the answer array with -1\n        int[] ans = new int[n];\n        Arrays.fill(ans, -1);\n\n        // Traverse each element in the array\n        for (int i = 0; i < n; i++) {\n            int curr = arr[i];\n\n            // Look ahead to find the next smaller element\n            for (int j = i + 1; j < n; j++) {\n                if (arr[j] < curr) {\n                    // Store and break on finding the smaller element\n                    ans[i] = arr[j];\n                    break;\n                }\n            }\n        }\n\n        // Return the answer array\n        return ans;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Input array\n        int[] arr = {4, 8, 5, 2, 25};\n\n        // Create object of Solution\n        Solution sol = new Solution();\n\n        // Call function to get next smaller elements\n        int[] ans = sol.nextSmallerElement(arr);\n\n        // Print the result\n        System.out.print(\"The next smaller elements are: \");\n        for (int num : ans) {\n            System.out.print(num + \" \");\n        }\n    }\n}",
        "python": "class Solution:\n    def nextSmallerElement(self, arr):\n        # Get the size of the array\n        n = len(arr)\n\n        # Initialize the result array with -1\n        ans = [-1] * n\n\n        # Traverse each element\n        for i in range(n):\n            # Check subsequent elements for smaller value\n            for j in range(i + 1, n):\n                if arr[j] < arr[i]:\n                    # Store and break on first smaller element\n                    ans[i] = arr[j]\n                    break\n\n        # Return the result array\n        return ans\n\n# Input array\narr = [1, 3, 2, 4]\n\n# Create object of Solution\nsol = Solution()\n\n# Get the next smaller elements\nans = sol.nextSmallerElement(arr)\n\n# Print the result\nprint(\"The next smaller elements are:\", *ans)",
        "javascript": "class Solution {\n    nextSmallerElement(arr) {\n        const n = arr.length;\n\n        // Initialize result array with -1\n        const ans = new Array(n).fill(-1);\n\n        // Loop through each element in the array\n        for (let i = 0; i < n; i++) {\n            // Check subsequent elements\n            for (let j = i + 1; j < n; j++) {\n                // If smaller element found\n                if (arr[j] < arr[i]) {\n                    // Store it and break\n                    ans[i] = arr[j];\n                    break;\n                }\n            }\n        }\n\n        // Return result\n        return ans;\n    }\n}\n\n// Input array\nconst arr = [1, 3, 2, 4];\n\n// Create object of Solution\nconst sol = new Solution();\n\n// Get result\nconst ans = sol.nextSmallerElement(arr);\n\n// Print output\nconsole.log(\"The next smaller elements are:\", ans.join(\" \"));"
      }
    },
    {
      "approach_name": "Optimal Approach",
      "algorithm": "Initialize an answer array filled with -1\nInitialize an empty stack\nLoop from i = n - 1 to 0\nWhile stack is not empty and top is greater than or equal to current element, pop the stack\nIf stack is not empty, set answer[i] to top of stack\nPush current element to stack\nReturn the answer array",
      "time_complexity": "O(N)",
      "space_complexity": "O(N)",
      "time_complexity_details": "Time Complexity: O(N), since each element is pushed and popped at most once.\nSpace Complexity: O(N), since stack may hold up to N elements in the worst case.",
      "space_complexity_details": "Time Complexity: O(N), since each element is pushed and popped at most once.\nSpace Complexity: O(N), since stack may hold up to N elements in the worst case.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> nextSmallerElement(vector<int>& arr) {\n        int n = arr.size();\n\n        // Stack to store potential next smaller elements\n        stack<int> st;\n\n        // Answer array initialized with -1\n        vector<int> ans(n, -1);\n\n        // Traverse the array from right to left\n        for (int i = n - 1; i >= 0; i--) {\n\n            // Pop elements from stack while they are >= current element\n            while (!st.empty() && st.top() >= arr[i]) {\n                st.pop();\n            }\n\n            // If stack is not empty, top is the next smaller element\n            if (!st.empty()) {\n                ans[i] = st.top();\n            }\n\n            // Push current element to stack\n            st.push(arr[i]);\n        }\n\n        // Return result\n        return ans;\n    }\n};\n\nint main() {\n    vector<int> arr = {1, 3, 2, 4};\n    Solution sol;\n\n    // Get result\n    vector<int> ans = sol.nextSmallerElement(arr);\n\n    // Print result\n    cout << \"The next smaller elements are: \";\n    for (int val : ans) {\n        cout << val << \" \";\n    }\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n\n    // Function to find next smaller element using stack\n    public int[] nextSmallerElement(int[] arr) {\n        int n = arr.length;\n\n        // Stack to keep potential next smaller elements\n        Stack<Integer> st = new Stack<>();\n\n        // Initialize result array with -1\n        int[] ans = new int[n];\n        Arrays.fill(ans, -1);\n\n        // Traverse from right to left\n        for (int i = n - 1; i >= 0; i--) {\n\n            // Pop elements from stack that are not smaller\n            while (!st.isEmpty() && st.peek() >= arr[i]) {\n                st.pop();\n            }\n\n            // If stack not empty, top is the next smaller element\n            if (!st.isEmpty()) {\n                ans[i] = st.peek();\n            }\n\n            // Push current element to stack\n            st.push(arr[i]);\n        }\n\n        // Return result\n        return ans;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[] arr = {1, 3, 2, 4};\n        Solution sol = new Solution();\n\n        // Get next smaller elements\n        int[] ans = sol.nextSmallerElement(arr);\n\n        // Print result\n        System.out.print(\"The next smaller elements are: \");\n        for (int val : ans) {\n            System.out.print(val + \" \");\n        }\n    }\n}",
        "python": "class Solution:\n    def nextSmallerElement(self, arr):\n        n = len(arr)\n\n        # Stack to store potential next smaller elements\n        st = []\n\n        # Initialize result array with -1\n        ans = [-1] * n\n\n        # Traverse from right to left\n        for i in range(n - 1, -1, -1):\n\n            # Pop elements that are not smaller\n            while st and st[-1] >= arr[i]:\n                st.pop()\n\n            # If stack not empty, top is next smaller\n            if st:\n                ans[i] = st[-1]\n\n            # Push current element to stack\n            st.append(arr[i])\n\n        # Return result\n        return ans\n\n# Driver code\narr = [1, 3, 2, 4]\nsol = Solution()\n\n# Get result\nans = sol.nextSmallerElement(arr)\n\n# Print result\nprint(\"The next smaller elements are:\", *ans)",
        "javascript": "class Solution {\n    nextSmallerElement(arr) {\n        const n = arr.length;\n\n        // Stack to keep next smaller candidates\n        const st = [];\n\n        // Initialize result array with -1\n        const ans = new Array(n).fill(-1);\n\n        // Traverse from right to left\n        for (let i = n - 1; i >= 0; i--) {\n\n            // Pop elements that are not smaller\n            while (st.length && st[st.length - 1] >= arr[i]) {\n                st.pop();\n            }\n\n            // If stack not empty, top is the next smaller\n            if (st.length) {\n                ans[i] = st[st.length - 1];\n            }\n\n            // Push current element to stack\n            st.push(arr[i]);\n        }\n\n        // Return result\n        return ans;\n    }\n}\n\n// Driver code\nconst arr = [1, 3, 2, 4];\nconst sol = new Solution();\n\n// Get result\nconst ans = sol.nextSmallerElement(arr);\n\n// Print result\nconsole.log(\"The next smaller elements are:\", ans.join(\" \"));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:08Z"
}