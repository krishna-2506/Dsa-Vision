{
  "problem_id": "291",
  "problem_name": "Maximum sum of non adjacent elements",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 2,
  "substep_name": "1D DP",
  "difficulty": "Medium",
  "youtube_url": "https://www.youtube.com/watch?v=GrMBfJNk_NY",
  "leetcode_url": "https://leetcode.com/problems/house-robber/",
  "article_url": "https://takeuforward.org/data-structure/maximum-sum-of-non-adjacent-elements-dp-5/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/maximum-sum-of-non-adjacent-elements",
  "problem_statement": "Given an array of N positive integers, we need to return the maximum sum of the subsequence such that no two elements of the subsequence are adjacent elements in the array.\nNote: A subsequence of an array is a list with elements of the array where some elements are deleted (or not deleted at all) and the elements should be in the same order in the subsequence as in the array.",
  "examples": [
    "Input: nums = [1, 2, 4]\nOutput: 5\nExplanation: \nSubsequence {1,4} gives maximum sum.\n\nInput: [2, 1, 4, 9]\nOutput: 11\nExplanation: \nSubsequence {2,9} gives maximum sum"
  ],
  "total_approaches": 3,
  "approaches": [
    {
      "approach_name": "Memoization",
      "algorithm": "As we need to find the sum of subsequences, one approach that comes to our mind is to generate all subsequences and pick the one with the maximum sum.\nTo generate all the subsequences, we can use the pick/non-pick technique. This technique can be briefly explained as follows:\nAt every index of the array, we have two options.First, to pick the array element at that index and consider it in our subsequence.Second, to leave the array element at that index and not to consider it in our subsequence.\n\nWe can construct the recursive relation as follows:\n\nAt any index, pick the current element but then skip the next one\nOr you can skip the current element and try from the next one\n\nIf we implement this recursive logic directly, it will recompute the same subproblems multiple times, especially for large inputs.\nTo avoid recomputation, we use memoization i.e. we store the answer for each index once computed.\nThis way if we need the solution for index i again, we just look it up in the memo table instead of recalculating.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Recursive function with memoization\n    int solve(vector<int>& arr, int i, vector<int>& dp) {\n        // If index is negative, no element to pick\n        if (i < 0) return 0;\n\n        // If at first element, return its value\n        if (i == 0) return arr[0];\n\n        // Return already computed value\n        if (dp[i] != -1) return dp[i];\n\n        // Include current and move 2 back\n        int pick = arr[i] + solve(arr, i - 2, dp);\n\n        // Exclude current and move 1 back\n        int notPick = solve(arr, i - 1, dp);\n\n        // Store and return max of both choices\n        return dp[i] = max(pick, notPick);\n    }\n\n    // Main function to be called externally\n    int maximumNonAdjacentSum(vector<int>& arr) {\n        int n = arr.size();\n\n        // DP array initialized with -1\n        vector<int> dp(n, -1);\n\n        // Start solving from last index\n        return solve(arr, n - 1, dp);\n    }\n};\n\nint main() {\n    vector<int> arr = {2, 1, 4, 9};\n    Solution obj;\n\n    // Output the result\n    cout << obj.maximumNonAdjacentSum(arr);\n    return 0;\n}",
        "java": "class Solution {\n    // Recursive helper function with memoization\n    public int solve(int[] arr, int i, int[] dp) {\n        // No element to pick\n        if (i < 0) return 0;\n\n        // Only one element\n        if (i == 0) return arr[0];\n\n        // Return memoized result\n        if (dp[i] != -1) return dp[i];\n\n        // Pick current and move 2 back\n        int pick = arr[i] + solve(arr, i - 2, dp);\n\n        // Don't pick current, move 1 back\n        int notPick = solve(arr, i - 1, dp);\n\n        // Memoize and return result\n        return dp[i] = Math.max(pick, notPick);\n    }\n\n    // Main function to compute result\n    public int maximumNonAdjacentSum(int[] arr) {\n        int n = arr.length;\n\n        // Initialize DP array\n        int[] dp = new int[n];\n        Arrays.fill(dp, -1);\n\n        // Start recursion\n        return solve(arr, n - 1, dp);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[] arr = {2, 1, 4, 9};\n        Solution obj = new Solution();\n\n        // Print result\n        System.out.println(obj.maximumNonAdjacentSum(arr));\n    }\n}",
        "python": "class Solution:\n    # Recursive helper with memoization\n    def solve(self, arr, i, dp):\n        # No elements to pick\n        if i < 0:\n            return 0\n\n        # One element case\n        if i == 0:\n            return arr[0]\n\n        # Return memoized result\n        if dp[i] != -1:\n            return dp[i]\n\n        # Pick current and go 2 steps back\n        pick = arr[i] + self.solve(arr, i - 2, dp)\n\n        # Skip current and go 1 step back\n        notPick = self.solve(arr, i - 1, dp)\n\n        # Store and return max\n        dp[i] = max(pick, notPick)\n        return dp[i]\n\n    # Main function to call\n    def maximumNonAdjacentSum(self, arr):\n        n = len(arr)\n\n        # Initialize DP array\n        dp = [-1] * n\n\n        # Start recursion\n        return self.solve(arr, n - 1, dp)\n\n# Driver code\narr = [2, 1, 4, 9]\nobj = Solution()\nprint(obj.maximumNonAdjacentSum(arr))",
        "javascript": "class Solution {\n    // Recursive helper with memoization\n    solve(arr, i, dp) {\n        // No element to pick\n        if (i < 0) return 0;\n\n        // Only one element\n        if (i === 0) return arr[0];\n\n        // Return memoized value\n        if (dp[i] !== -1) return dp[i];\n\n        // Pick current and go 2 back\n        let pick = arr[i] + this.solve(arr, i - 2, dp);\n\n        // Skip current and go 1 back\n        let notPick = this.solve(arr, i - 1, dp);\n\n        // Memoize and return\n        dp[i] = Math.max(pick, notPick);\n        return dp[i];\n    }\n\n    // Main function to start computation\n    maximumNonAdjacentSum(arr) {\n        let n = arr.length;\n\n        // Initialize DP array\n        let dp = new Array(n).fill(-1);\n\n        // Start solving\n        return this.solve(arr, n - 1, dp);\n    }\n}\n\n// Driver code\nlet arr = [2, 1, 4, 9];\nlet obj = new Solution();\nconsole.log(obj.maximumNonAdjacentSum(arr));"
      }
    },
    {
      "approach_name": "Tabulation",
      "algorithm": "To prevent auxiliary stack space used by recursion in memoization, and to optimize space and runtime, we use the tabulation (bottom-up DP) approach.\n \nCreate a DP array to store the maximum sum at each index.\nInitialize the first value in the DP array as the first element of the input array because if there's only one element, that’s the only sum we can take.\nFor the second element, take the maximum between the first and the second element.\nFrom the third element onward, for each index, calculate two options:\n\nIf we pick the current element, we add its value to the value at index two positions back in the DP array\nIf we don’t pick, just take the value from the previous index.\n\nTake the maximum of the two options and store it in the current position in the DP array.\nAfter processing all elements, the last value in the DP array will contain the maximum sum possible without picking adjacent elements.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to return maximum sum of non-adjacent elements\n    int maximumNonAdjacentSum(vector<int>& arr) {\n        // Get the size of array\n        int n = arr.size();\n\n        // If array has only one element, return it\n        if (n == 1) return arr[0];\n\n        // Initialize dp array of size n\n        vector<int> dp(n);\n\n        // Base case: only one element\n        dp[0] = arr[0];\n\n        // Base case: max of first or second element\n        dp[1] = max(arr[0], arr[1]);\n\n        // Iterate from 3rd element onwards\n        for (int i = 2; i < n; i++) {\n            // Either take current and add dp[i-2]\n            // Or skip current and take dp[i-1]\n            dp[i] = max(arr[i] + dp[i - 2], dp[i - 1]);\n        }\n\n        // Final result at last index\n        return dp[n - 1];\n    }\n};\n\nint main() {\n    vector<int> arr = {2, 1, 4, 9};\n    Solution obj;\n    cout << obj.maximumNonAdjacentSum(arr);\n    return 0;\n}",
        "java": "class Solution {\n    // Function to return maximum sum of non-adjacent elements\n    public int maximumNonAdjacentSum(int[] arr) {\n        // Get the size of array\n        int n = arr.length;\n\n        // If only one element, return it\n        if (n == 1) return arr[0];\n\n        // Initialize dp array\n        int[] dp = new int[n];\n\n        // Base case\n        dp[0] = arr[0];\n\n        // Compare first two elements\n        dp[1] = Math.max(arr[0], arr[1]);\n\n        // Iterate from index 2\n        for (int i = 2; i < n; i++) {\n            // Take current and dp[i-2] or just dp[i-1]\n            dp[i] = Math.max(arr[i] + dp[i - 2], dp[i - 1]);\n        }\n\n        // Return result from last index\n        return dp[n - 1];\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] arr = {2, 1, 4, 9};\n        System.out.println(sol.maximumNonAdjacentSum(arr));\n    }\n}",
        "python": "class Solution:\n    # Function to return maximum sum of non-adjacent elements\n    def maximumNonAdjacentSum(self, arr):\n        # Get size of array\n        n = len(arr)\n\n        # If array has one element\n        if n == 1:\n            return arr[0]\n\n        # Initialize dp array\n        dp = [0] * n\n\n        # Base cases\n        dp[0] = arr[0]\n        dp[1] = max(arr[0], arr[1])\n\n        # Iterate from 3rd element\n        for i in range(2, n):\n            # Pick current + dp[i-2] or skip current\n            dp[i] = max(arr[i] + dp[i - 2], dp[i - 1])\n\n        # Final result at last index\n        return dp[n - 1]\n\n\n# Driver code\narr = [2, 1, 4, 9]\nsol = Solution()\nprint(sol.maximumNonAdjacentSum(arr))",
        "javascript": "class Solution {\n    // Function to return maximum sum of non-adjacent elements\n    maximumNonAdjacentSum(arr) {\n        // Get the size of the array\n        const n = arr.length;\n\n        // If only one element, return it\n        if (n === 1) return arr[0];\n\n        // Initialize dp array\n        const dp = new Array(n).fill(0);\n\n        // Base cases\n        dp[0] = arr[0];\n        dp[1] = Math.max(arr[0], arr[1]);\n\n        // Iterate from 3rd element\n        for (let i = 2; i < n; i++) {\n            // Take current + dp[i-2] or just dp[i-1]\n            dp[i] = Math.max(arr[i] + dp[i - 2], dp[i - 1]);\n        }\n\n        // Final result at last index\n        return dp[n - 1];\n    }\n}\n\n// Driver code\nconst arr = [2, 1, 4, 9];\nconst sol = new Solution();\nconsole.log(sol.maximumNonAdjacentSum(arr));"
      }
    },
    {
      "approach_name": "Space Optimization",
      "algorithm": "In the tabulation approach, we stored the best possible answer up to every index in an array. But if we look closely, to compute the value at any position, we only need the answers from the previous two positions and not the entire array. Therefore, instead of storing the entire array, we can just keep track of the best possible sum at previous index and one before that. For every index:\n \nIf we include the current element, we add it to the best sum from two positions back.\nIf we don’t include it, we just take the last best sum from the previous position.\nThen we update our tracking values to reflect the new best sum for the previous two positions.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to return maximum sum of non-adjacent elements\n    int maxSum(vector<int>& nums) {\n        // Handle edge case for empty input\n        if (nums.empty()) return 0;\n\n        // Initialize two tracking sums\n        int prev2 = 0;\n        int prev = nums[0];\n\n        // Traverse through the array\n        for (int i = 1; i < nums.size(); i++) {\n            // Include current by adding it to two steps back\n            int include = nums[i] + prev2;\n\n            // Exclude current by taking previous best\n            int exclude = prev;\n\n            // Choose max of include and exclude\n            int curr = max(include, exclude);\n\n            // Update tracking variables\n            prev2 = prev;\n            prev = curr;\n        }\n\n        // Final result is stored in prev\n        return prev;\n    }\n};\n\nint main() {\n    vector<int> arr = {3, 2, 5, 10, 7};\n    Solution obj;\n    cout << obj.maxSum(arr) << endl;\n    return 0;\n}",
        "java": "class Solution {\n    // Function to return maximum sum of non-adjacent elements\n    public int maxSum(int[] nums) {\n        // Handle edge case for empty input\n        if (nums.length == 0) return 0;\n\n        // Initialize two tracking sums\n        int prev2 = 0;\n        int prev = nums[0];\n\n        // Traverse through the array\n        for (int i = 1; i < nums.length; i++) {\n            // Include current by adding it to two steps back\n            int include = nums[i] + prev2;\n\n            // Exclude current by taking previous best\n            int exclude = prev;\n\n            // Choose max of include and exclude\n            int curr = Math.max(include, exclude);\n\n            // Update tracking variables\n            prev2 = prev;\n            prev = curr;\n        }\n\n        // Final result is stored in prev\n        return prev;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[] arr = {3, 2, 5, 10, 7};\n        Solution obj = new Solution();\n        System.out.println(obj.maxSum(arr));\n    }\n}",
        "python": "class Solution:\n    # Function to return maximum sum of non-adjacent elements\n    def maxSum(self, nums):\n        # Handle edge case for empty input\n        if not nums:\n            return 0\n\n        # Initialize two tracking sums\n        prev2 = 0\n        prev = nums[0]\n\n        # Traverse through the array\n        for i in range(1, len(nums)):\n            # Include current by adding it to two steps back\n            include = nums[i] + prev2\n\n            # Exclude current by taking previous best\n            exclude = prev\n\n            # Choose max of include and exclude\n            curr = max(include, exclude)\n\n            # Update tracking variables\n            prev2 = prev\n            prev = curr\n\n        # Final result is stored in prev\n        return prev\n\n\n# Driver code\narr = [3, 2, 5, 10, 7]\nobj = Solution()\nprint(obj.maxSum(arr))",
        "javascript": "class Solution {\n    // Function to return maximum sum of non-adjacent elements\n    maxSum(nums) {\n        // Handle edge case for empty input\n        if (nums.length === 0) return 0;\n\n        // Initialize two tracking sums\n        let prev2 = 0;\n        let prev = nums[0];\n\n        // Traverse through the array\n        for (let i = 1; i < nums.length; i++) {\n            // Include current by adding it to two steps back\n            let include = nums[i] + prev2;\n\n            // Exclude current by taking previous best\n            let exclude = prev;\n\n            // Choose max of include and exclude\n            let curr = Math.max(include, exclude);\n\n            // Update tracking variables\n            prev2 = prev;\n            prev = curr;\n        }\n\n        // Final result is stored in prev\n        return prev;\n    }\n}\n\n// Driver code\nlet arr = [3, 2, 5, 10, 7];\nlet obj = new Solution();\nconsole.log(obj.maxSum(arr));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:22Z"
}