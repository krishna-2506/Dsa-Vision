{
  "problem_id": "2828",
  "problem_name": "Why priority Queue is used in Djisktra's Algorithm",
  "step_no": 15,
  "step_name": "Graphs [Concepts & Problems]",
  "substep_no": 4,
  "substep_name": "Shortest Path Algorithms and Problems",
  "difficulty": "Hard",
  "youtube_url": "https://www.youtube.com/watch?v=rp1SMw7HSO8&list=PLgUwDviBIf0oE3gA41TKO2H5bHpPd7fzn&index=35",
  "leetcode_url": null,
  "article_url": "https://takeuforward.org/data-structure/dijkstras-algorithm-using-priority-queue-g-32/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/dijkstra's-algorithm",
  "problem_statement": "Given a weighted, undirected, and connected graph of V vertices and E edges, find the shortest distance of all the vertex's from the source vertex S.\n\nNote: The Graph doesn't contain any negative weight cycle.",
  "examples": [
    "Input : V = 2, E = 1\n\nOutput : [0, 9]\nExplanation : The source vertex is 0. Hence, the shortest distance of node 0 from the source is 0 and the shortest distance of node 1 from source will be 9.\n\nInput : V = 3, E = 3\n\nOutput : [4, 3, 0]\nExplanation : For nodes 2 to 0, we can follow the path 2-1-0. This has a distance of 1+3 = 4, whereas the path 2-0 has a distance of 6. So, the Shortest path from 2 to 0 is 4."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "In a weighted graph, the goal of Dijkstra’s algorithm is to find the shortest path from a source node to all other nodes. The idea is to always expand the closest node not yet processed.\nUsing a priority queue (min-heap) ensures that we can efficiently pick the node with the smallest current distance, instead of scanning all nodes each time.\n\nThe algorithm starts with the source node at distance 0. At each step, the priority queue pops the node with the smallest distance. For every neighbor of that node, if the new distance through this node is shorter than the current stored distance, we update it and push the neighbor into the priority queue. This process continues until all nodes are processed.\n Initialize a distance array with infinity for all nodes except the source (0). Use a priority queue (min-heap) to store pairs of (distance, node). Push the source with distance 0 into the queue. While the queue is not empty, pop the node with the smallest distance. If the popped distance is greater than the stored distance, skip it (already processed). For each neighbor of the current node, calculate the new distance. If the new distance is smaller than the stored one, update it and push the neighbor into the queue. After processing, the distance array will hold the shortest path distances from the source to all nodes.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to implement Dijkstra's Algorithm\n    vector<int> dijkstra(int V, vector<vector<pair<int,int>>>& adj, int src) {\n        // Distance array initialized to large value\n        vector<int> dist(V, 1e9);\n\n        // Min-heap storing {distance, node}\n        priority_queue<pair<int,int>, vector<pair<int,int>>, \n                       greater<pair<int,int>>> pq;\n\n        // Distance to source is 0\n        dist[src] = 0;\n\n        // Push source into heap\n        pq.push({0, src});\n\n        // Process nodes until heap is empty\n        while (!pq.empty()) {\n            // Extract node with minimum distance\n            int d = pq.top().first;\n            int node = pq.top().second;\n            pq.pop();\n\n            // Skip if this distance is outdated\n            if (d > dist[node]) continue;\n\n            // Traverse all adjacent neighbors\n            for (auto it : adj[node]) {\n                int next = it.first;\n                int wt = it.second;\n\n                // Relaxation check\n                if (dist[node] + wt < dist[next]) {\n                    // Update distance\n                    dist[next] = dist[node] + wt;\n\n                    // Push updated distance into heap\n                    pq.push({dist[next], next});\n                }\n            }\n        }\n        return dist;\n    }\n};\n\nint main() {\n    // Number of vertices\n    int V = 5;\n\n    // Adjacency list {neighbor, weight}\n    vector<vector<pair<int,int>>> adj(V);\n\n    // Example edges\n    adj[0].push_back({1, 2});\n    adj[0].push_back({2, 4});\n    adj[1].push_back({2, 1});\n    adj[1].push_back({3, 7});\n    adj[2].push_back({4, 3});\n    adj[3].push_back({4, 2});\n\n    // Run algorithm\n    Solution obj;\n    vector<int> dist = obj.dijkstra(V, adj, 0);\n\n    // Print shortest distances\n    for (int i = 0; i < V; i++) {\n        cout << \"Distance from 0 to \" << i << \" = \" << dist[i] << endl;\n    }\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to implement Dijkstra's Algorithm\n    public int[] dijkstra(int V, List<List<int[]>> adj, int src) {\n        // Distance array initialized to large value\n        int[] dist = new int[V];\n        Arrays.fill(dist, (int)1e9);\n\n        // Min-heap storing {distance, node}\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a,b) -> a[0]-b[0]);\n\n        // Distance to source is 0\n        dist[src] = 0;\n\n        // Push source into heap\n        pq.add(new int[]{0, src});\n\n        // Process nodes until heap is empty\n        while (!pq.isEmpty()) {\n            // Extract node with minimum distance\n            int[] cur = pq.poll();\n            int d = cur[0];\n            int node = cur[1];\n\n            // Skip if outdated distance\n            if (d > dist[node]) continue;\n\n            // Traverse all neighbors\n            for (int[] edge : adj.get(node)) {\n                int next = edge[0];\n                int wt = edge[1];\n\n                // Relaxation check\n                if (dist[node] + wt < dist[next]) {\n                    // Update distance\n                    dist[next] = dist[node] + wt;\n\n                    // Push into heap\n                    pq.add(new int[]{dist[next], next});\n                }\n            }\n        }\n        return dist;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Number of vertices\n        int V = 5;\n\n        // Adjacency list {neighbor, weight}\n        List<List<int[]>> adj = new ArrayList<>();\n        for (int i = 0; i < V; i++) adj.add(new ArrayList<>());\n\n        // Example edges\n        adj.get(0).add(new int[]{1, 2});\n        adj.get(0).add(new int[]{2, 4});\n        adj.get(1).add(new int[]{2, 1});\n        adj.get(1).add(new int[]{3, 7});\n        adj.get(2).add(new int[]{4, 3});\n        adj.get(3).add(new int[]{4, 2});\n\n        // Run algorithm\n        Solution obj = new Solution();\n        int[] dist = obj.dijkstra(V, adj, 0);\n\n        // Print shortest distances\n        for (int i = 0; i < V; i++) {\n            System.out.println(\"Distance from 0 to \" + i + \" = \" + dist[i]);\n        }\n    }\n}",
        "python": "import heapq\n\nclass Solution:\n    # Function to implement Dijkstra's Algorithm\n    def dijkstra(self, V, adj, src):\n        # Initialize distances\n        dist = [float('inf')] * V\n        dist[src] = 0\n\n        # Min-heap storing (distance, node)\n        pq = [(0, src)]\n\n        # Process nodes until heap is empty\n        while pq:\n            # Extract node with minimum distance\n            d, node = heapq.heappop(pq)\n\n            # Skip if outdated distance\n            if d > dist[node]:\n                continue\n\n            # Traverse neighbors\n            for next, wt in adj[node]:\n                # Relaxation check\n                if dist[node] + wt < dist[next]:\n                    # Update distance\n                    dist[next] = dist[node] + wt\n\n                    # Push into heap\n                    heapq.heappush(pq, (dist[next], next))\n\n        return dist\n\n\n# Driver code\nV = 5\nadj = [[] for _ in range(V)]\n\n# Example edges\nadj[0].append((1, 2))\nadj[0].append((2, 4))\nadj[1].append((2, 1))\nadj[1].append((3, 7))\nadj[2].append((4, 3))\nadj[3].append((4, 2))\n\n# Run algorithm\nobj = Solution()\ndist = obj.dijkstra(V, adj, 0)\n\n# Print shortest distances\nfor i in range(V):\n    print(f\"Distance from 0 to {i} = {dist[i]}\")",
        "javascript": "class Solution {\n    // Function to implement Dijkstra's Algorithm\n    dijkstra(V, adj, src) {\n        // Distance array initialized to Infinity\n        let dist = new Array(V).fill(Infinity);\n\n        // Distance to source is 0\n        dist[src] = 0;\n\n        // Min-heap simulated using array\n        let pq = [[0, src]]; // [distance, node]\n\n        // Process nodes until heap is empty\n        while (pq.length > 0) {\n            // Extract node with minimum distance\n            pq.sort((a, b) => a[0] - b[0]);\n            let [d, node] = pq.shift();\n\n            // Skip if outdated distance\n            if (d > dist[node]) continue;\n\n            // Traverse neighbors\n            for (let [next, wt] of adj[node]) {\n                // Relaxation check\n                if (dist[node] + wt < dist[next]) {\n                    // Update distance\n                    dist[next] = dist[node] + wt;\n\n                    // Push updated distance\n                    pq.push([dist[next], next]);\n                }\n            }\n        }\n        return dist;\n    }\n}\n\n// Driver code\nlet V = 5;\nlet adj = Array.from({ length: V }, () => []);\n\n// Example edges\nadj[0].push([1, 2]);\nadj[0].push([2, 4]);\nadj[1].push([2, 1]);\nadj[1].push([3, 7]);\nadj[2].push([4, 3]);\nadj[3].push([4, 2]);\n\n// Run algorithm\nlet obj = new Solution();\nlet dist = obj.dijkstra(V, adj, 0);\n\n// Print shortest distances\nfor (let i = 0; i < V; i++) {\n    console.log(`Distance from 0 to ${i} = ${dist[i]}`);\n}"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:20Z"
}