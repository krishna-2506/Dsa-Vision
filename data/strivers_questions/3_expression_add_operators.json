{
  "problem_id": "3",
  "problem_name": "Expression Add Operators",
  "step_no": 7,
  "step_name": "Recursion [PatternWise]",
  "substep_no": 3,
  "substep_name": "Trying out all Combos / Hard",
  "difficulty": "Hard",
  "youtube_url": null,
  "leetcode_url": "https://leetcode.com/problems/expression-add-operators/",
  "article_url": "https://takeuforward.org/data-structure/expression-add-operators",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/expression-add-operators",
  "problem_statement": "Given a string num that contains only digits and an integer target, return all possibilities to insert the binary operators '+', '-', and/or '*' between the digits of num so that the resultant expression evaluates to the target value.\n\nNote that operands in the returned expressions should not contain leading zeros.\n\nNote that a number can contain multiple digits.",
  "examples": [
    "Example 1:\nInput: num = \"123\", target = 6\nOutput: [\"1*2*3\",\"1+2+3\"]\nExplanation:Both \"1*2*3\" and \"1+2+3\" evaluate to 6.\n\nExample 2:\nInput: num = \"232\", target = 8\nOutput: [\"2*3+2\",\"2+3*2\"]\nExplanation:Both \"2*3+2\" and \"2+3*2\" evaluate to 8."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "Use backtracking with Depth-First Search (DFS) to solve the problem by exploring all possible expressions.\nFor each digit in the input string, add one of the three operators (+, -, *) and recursively explore the expressions.\nTrack the current expression and evaluate its result step by step.\nKey Considerations:\nLeading Zeros: Ensure valid numbers do not have leading zeros unless it's the number \"0\".\nOperators: Add one of the three operators (+, -, *) between digits and recursively check all combinations.\nOperand and Target Evaluation: At each recursion step, calculate the current value based on the operators applied, and continue until the full string is consumed.\nRecursive DFS Function:\nCurrent Index (start): This tracks the position in the string num that we are currently working on.\nCurrent Expression (expr): This keeps track of the current mathematical expression as a string.\nCurrent Value (current_value): The result of the expression calculated so far.\nPrevious Operand (last_operand): The last number added to the expression, used for multiplication handling since multiplication has higher precedence than addition and subtraction.\nBase Case:\nWhen the end of the string is reached, check if the final result equals the target.\nRecursion Logic:\nStart at the first character and try all substrings that form valid numbers.\nFor each valid substring, call the DFS function with the new expression formed by adding an operator.\nOnce the end is reached, evaluate if the expression evaluates to the target.\nAvoiding Leading Zeros:\nIf a number has multiple digits and the first character is '0', skip it as it's an invalid number.\nFinal Expression:\nOnce the recursion reaches the end, check if the evaluated expression matches the target value.\nIf valid, add it to the result list.\nReturn Results:\nAfter all recursive calls, return the list of valid expressions that evaluate to the target.",
      "time_complexity": "O(4^n)",
      "space_complexity": "O(4^n)",
      "time_complexity_details": "Time Complexity: O(4^n), since in each recursive call, we can choose 4 possibilities for each substring (three operators: +, -, *, or no operator in the case of the first number), resulting in an exponential time complexity with a branching factor of 4. \nSpace Complexity: O(n), since the space complexity is dominated by the recursion depth, which can go as deep as the length of the string. Additionally, we store the result expressions in a list, but this doesn't increase the space complexity beyond the recursion stack and the input size.",
      "space_complexity_details": "Time Complexity: O(4^n), since in each recursive call, we can choose 4 possibilities for each substring (three operators: +, -, *, or no operator in the case of the first number), resulting in an exponential time complexity with a branching factor of 4. \nSpace Complexity: O(n), since the space complexity is dominated by the recursion depth, which can go as deep as the length of the string. Additionally, we store the result expressions in a list, but this doesn't increase the space complexity beyond the recursion stack and the input size.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\n\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to evaluate the expression\n    void dfs(string& num, int target, int start, long long current_value, long long last_operand, string expression, vector<string>& result) {\n        // Base case: If we've reached the end of the string\n        if (start == num.size()) {\n            // If the expression evaluates to the target, add it to result\n            if (current_value == target)  \n                result.push_back(expression);\n            return;\n        }\n        \n        // Loop through all substrings starting from 'start' index\n        for (int i = start; i < num.size(); i++) {\n            // Skip leading zeros in numbers\n            if (i > start && num[start] == '0') return;\n            // Get the current number\n            string current_num = num.substr(start, i - start + 1); \n            long long current_num_val = stoll(current_num);\n            \n            // If we are at the first number, just start the expression\n            if (start == 0) {\n                dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result);\n            } else {\n                // Add the current number with '+'\n                dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result);\n                \n                // Add the current number with '-'\n                dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result);\n                \n                // Add the current number with '*'\n                dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result);\n            }\n        }\n    }\n    // To store the valid expressions\n    vector<string> addOperators(string num, int target) {\n        vector<string> result;  \n        // Start DFS with empty expression\n        dfs(num, target, 0, 0, 0, \"\", result);  \n        return result;\n    }\n};\n\nint main() {\n    string num = \"123\";  \n    int target = 6;      \n    Solution sol;        \n    \n    // Get all valid expressions\n    vector<string> result = sol.addOperators(num, target);\n    \n    // Print the results\n    for (const string& expr : result) {\n        cout << expr << \" \"; \n    }\n    return 0;\n}",
        "java": "import java.util.List;\nimport java.util.ArrayList;\n\nclass Solution {\n    // Function to evaluate the expression\n    public void dfs(String num, int target, int start, long current_value, long last_operand, String expression, List<String> result) {\n        // Base case: If we've reached the end of the string\n        if (start == num.length()) {\n            // If the expression evaluates to the target, add it to result\n            if (current_value == target) {\n                result.add(expression);\n            }\n            return;\n        }\n\n        // Loop through all substrings starting from 'start' index\n        for (int i = start; i < num.length(); i++) {\n            // Skip leading zeros in numbers\n            if (i > start && num.charAt(start) == '0') return;\n            // Get the current number\n            String current_num = num.substring(start, i + 1);\n            long current_num_val = Long.parseLong(current_num);\n\n            // If we are at the first number, just start the expression\n            if (start == 0) {\n                dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result);\n            } else {\n                // Add the current number with '+'\n                dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result);\n                \n                // Add the current number with '-'\n                dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result);\n                \n                // Add the current number with '*'\n                dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result);\n            }\n        }\n    }\n\n    public List<String> addOperators(String num, int target) {\n        // To store the valid expressions\n        List<String> result = new ArrayList<>();\n        // Start DFS with empty expression\n        dfs(num, target, 0, 0, 0, \"\", result);\n        return result;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        String num = \"123\";  \n        int target = 6;      \n        Solution sol = new Solution();\n        \n        List<String> result = sol.addOperators(num, target);\n        \n        for (String expr : result) {\n            System.out.print(expr + \" \");\n        }\n    }\n}",
        "python": "class Solution:\n    \n    # Function to evaluate the expression\n    def dfs(self, num, target, start, current_value, last_operand, expression, result):\n        # Base case: If we've reached the end of the string\n        if start == len(num):\n            # If the expression evaluates to the target\n            if current_value == target:  \n                result.append(expression)\n            return\n        \n        # Loop through all substrings starting from 'start' index\n        for i in range(start, len(num)):\n            # Skip leading zeros in numbers\n            if i > start and num[start] == '0':\n                return\n            \n            # Get the current number\n            current_num = num[start:i+1]  \n            # Convert current number to integer\n            current_num_val = int(current_num)  \n            \n            # If we are at the first number, just start the expression\n            if start == 0:\n                self.dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result)\n            else:\n                # Add the current number with '+'\n                self.dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result)\n                \n                # Add the current number with '-'\n                self.dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result)\n                \n                # Add the current number with '*'\n                self.dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result)\n\n    # To store the valid expressions\n    def addOperators(self, num, target):\n        result = []  \n        # Start DFS with empty expression\n        self.dfs(num, target, 0, 0, 0, \"\", result)  \n        return result\n\n# Testing the implementation\nif __name__ == \"__main__\":\n    num = \"123\"  \n    target = 6    \n    sol = Solution()\n    \n    result = sol.addOperators(num, target)\n    \n    for expr in result:\n        print(expr, end=\" \")  # Output each valid expression",
        "javascript": "class Solution {\n    \n    // Function to evaluate the expression\n    dfs(num, target, start, current_value, last_operand, expression, result) {\n        // Base case: If we've reached the end of the string\n        if (start === num.length) {\n            // If the expression evaluates to the target\n            if (current_value === target) {  \n                result.push(expression);\n            }\n            return;\n        }\n        \n        // Loop through all substrings starting from 'start' index\n        for (let i = start; i < num.length; i++) {\n            // Skip leading zeros in numbers\n            if (i > start && num[start] === '0') return;\n            \n            // Get the current number\n            let current_num = num.slice(start, i + 1);  \n            // Convert current number to integer\n            let current_num_val = parseInt(current_num);  \n            \n            // If we are at the first number, just start the expression\n            if (start === 0) {\n                this.dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result);\n            } else {\n                // Add the current number with '+'\n                this.dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result);\n                \n                // Add the current number with '-'\n                this.dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result);\n                \n                // Add the current number with '*'\n                this.dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result);\n            }\n        }\n    }\n\n    addOperators(num, target) {\n        // To store the valid expressions\n        let result = [];  \n        // Start DFS with empty expression\n        this.dfs(num, target, 0, 0, 0, \"\", result);  \n        return result;\n    }\n}\n\n// Testing the implementation\nconst num = \"123\";  \nconst target = 6;    \nconst sol = new Solution();  \n\nconst result = sol.addOperators(num, target);\n\nresult.forEach(expr => console.log(expr));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:05Z"
}