{
  "problem_id": "311",
  "problem_name": "Minimum insertions or deletions to convert string A to B",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 5,
  "substep_name": "DP on Strings",
  "difficulty": "Hard",
  "youtube_url": "https://www.youtube.com/watch?v=yMnH0jrir0Q",
  "leetcode_url": "https://leetcode.com/problems/delete-operation-for-two-strings/",
  "article_url": "https://takeuforward.org/data-structure/minimum-insertions-deletions-to-convert-string-dp-30/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/minimum-insertions-or-deletions-to-convert-string-a-to-b",
  "problem_statement": "We are given two strings, str1 and str2. We are allowed the following operations:\n1. Delete any number of characters from string str1.2. Insert any number of characters in string str1.\nReturn the minimum number of operations required to convert str1 to str2.",
  "examples": [
    "Input: str1 = \"kitten\", str2 = \"sitting\"\nOutput: 5\nExplanation: To transform \"kitten\" to \"sitting\", delete \"k\" and insert \"s\" to get \"sitten\", then delete 'e' and insert \"i\" to get \"sittin\", and insert \"g\" at the end to get \"sitting\".\n\nInput : str1 = \"flaw\", str2 = \"lawn\"\nOutput: 2\nExplanation : To transform \"flaw\" to \"lawn\", delete \"f\" and insert \"n\" at the end. Hence minimum number of operations required is 2\"."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Tabulation Approach",
      "algorithm": "We need to find the minimum operations required to convert string str1 to str2. Let us keep the 'minimum' criteria aside and think, what maximum operations will be required for this conversion?\n\nThe easiest way is to remove all the characters of str1 and then insert all the characters of str2. In this way, we will convert str1 to str2 with total of ‘n+m’ operations (where n and m are the length of strings str1 and str2 respectively). \n\nTo minimize the operations, we will first try to refrain from deleting those characters which are already present in str2. More extensively, we refrain from deleting those characters which are common and come in the same order. To minimize the operations, we would like to keep the maximum common characters coming in the same order intact. These maximum characters are the characters of the longest common subsequence. \n\nKeep all the characters of longest common subsequence in str1 and delete all the other characters.\n\nNext, we will insert all the remaining characters of str2 to str1. \n\nIn order to minimize the operations, we need to find the length of the longest common subsequence. Minimum Operations required = (n - k) + (m - k)\nHere n and m are the length of str1 and str2 respectively and k is the length of the longest common subsequence of str1 and str2.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find minimum insertions/deletions\n    int minOperations(string s1, string s2) {\n        // Get lengths of both strings\n        int n = s1.length();\n        int m = s2.length();\n\n        // Create DP table of size (n+1) x (m+1)\n        vector<vector<int>> dp(n + 1, vector<int>(m + 1, 0));\n\n        // Fill the table using bottom-up approach\n        for (int i = 1; i <= n; i++) {\n            for (int j = 1; j <= m; j++) {\n                // If characters match, extend LCS\n                if (s1[i - 1] == s2[j - 1])\n                    dp[i][j] = 1 + dp[i - 1][j - 1];\n                else\n                    dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);\n            }\n        }\n\n        // Get LCS length\n        int lcs = dp[n][m];\n\n        // Return total operations = deletions + insertions\n        return (n - lcs) + (m - lcs);\n    }\n};\n\nint main() {\n    Solution sol;\n    string s1 = \"heap\", s2 = \"pea\";\n\n    // Call function and print result\n    cout << \"Minimum operations: \" << sol.minOperations(s1, s2) << endl;\n    return 0;\n}",
        "java": "public class Solution {\n    // Function to find minimum insertions/deletions\n    public int minOperations(String s1, String s2) {\n        // Get lengths of both strings\n        int n = s1.length();\n        int m = s2.length();\n\n        // Create DP table of size (n+1) x (m+1)\n        int[][] dp = new int[n + 1][m + 1];\n\n        // Fill the table using bottom-up approach\n        for (int i = 1; i <= n; i++) {\n            for (int j = 1; j <= m; j++) {\n                // If characters match, extend LCS\n                if (s1.charAt(i - 1) == s2.charAt(j - 1))\n                    dp[i][j] = 1 + dp[i - 1][j - 1];\n                else\n                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);\n            }\n        }\n\n        // Get LCS length\n        int lcs = dp[n][m];\n\n        // Return total operations = deletions + insertions\n        return (n - lcs) + (m - lcs);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        String s1 = \"heap\", s2 = \"pea\";\n\n        // Call function and print result\n        System.out.println(\"Minimum operations: \" + sol.minOperations(s1, s2));\n    }\n}",
        "python": "class Solution:\n    # Function to find minimum insertions/deletions\n    def minOperations(self, s1, s2):\n        # Get lengths of both strings\n        n = len(s1)\n        m = len(s2)\n\n        # Create DP table of size (n+1) x (m+1)\n        dp = [[0] * (m + 1) for _ in range(n + 1)]\n\n        # Fill the table using bottom-up approach\n        for i in range(1, n + 1):\n            for j in range(1, m + 1):\n                # If characters match, extend LCS\n                if s1[i - 1] == s2[j - 1]:\n                    dp[i][j] = 1 + dp[i - 1][j - 1]\n                else:\n                    dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])\n\n        # Get LCS length\n        lcs = dp[n][m]\n\n        # Return total operations = deletions + insertions\n        return (n - lcs) + (m - lcs)\n\n# Driver code\nsol = Solution()\ns1 = \"heap\"\ns2 = \"pea\"\n\n# Call function and print result\nprint(\"Minimum operations:\", sol.minOperations(s1, s2))",
        "javascript": "class Solution {\n    // Function to find minimum insertions/deletions\n    minOperations(s1, s2) {\n        // Get lengths of both strings\n        const n = s1.length;\n        const m = s2.length;\n\n        // Create DP table of size (n+1) x (m+1)\n        const dp = Array.from({ length: n + 1 }, () => Array(m + 1).fill(0));\n\n        // Fill the table using bottom-up approach\n        for (let i = 1; i <= n; i++) {\n            for (let j = 1; j <= m; j++) {\n                // If characters match, extend LCS\n                if (s1[i - 1] === s2[j - 1]) {\n                    dp[i][j] = 1 + dp[i - 1][j - 1];\n                } else {\n                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);\n                }\n            }\n        }\n\n        // Get LCS length\n        const lcs = dp[n][m];\n\n        // Return total operations = deletions + insertions\n        return (n - lcs) + (m - lcs);\n    }\n}\n\n// Driver code\nconst sol = new Solution();\nconst s1 = \"heap\";\nconst s2 = \"pea\";\n\n// Call function and print result\nconsole.log(\"Minimum operations:\", sol.minOperations(s1, s2));"
      }
    },
    {
      "approach_name": "Space Optimized Approach",
      "algorithm": "If we look closely, we are essentially using only two rows in our DP table: dp[ind-1][ ] and dp[ind][ ]. This means we don’t need to maintain the entire 2D array. Instead, we can optimize space by using two 1D arrays — prev and cur, where prev represents dp[ind-1] and cur represents dp[ind].\n\nWe can simply replace all instances of dp[ind-1] with prev, and dp[ind] with cur. After completing each inner iteration, we can update prev = cur so that the current row becomes the previous row for the next step.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Class containing the solution\nclass Solution {\npublic:\n    // Function to calculate minimum operations\n    int minOperations(string s1, string s2) {\n        // Get the lengths of both strings\n        int n = s1.size(), m = s2.size();\n\n        // Create two 1D DP arrays\n        vector<int> prev(m + 1, 0), cur(m + 1, 0);\n\n        // Iterate through each character of s1\n        for (int i = 1; i <= n; i++) {\n\n            // Iterate through each character of s2\n            for (int j = 1; j <= m; j++) {\n\n                // If characters match, increment LCS length\n                if (s1[i - 1] == s2[j - 1])\n                    cur[j] = 1 + prev[j - 1];\n\n                // Else, take max from left or top\n                else\n                    cur[j] = max(prev[j], cur[j - 1]);\n            }\n\n            // Update prev row to current\n            prev = cur;\n        }\n\n        // Calculate total operations\n        int lcs = prev[m];\n        return (n - lcs) + (m - lcs);\n    }\n};\n\n// Main function\nint main() {\n    // Input strings\n    string s1 = \"heap\", s2 = \"pea\";\n\n    // Create object of Solution\n    Solution obj;\n\n    // Call function and print result\n    cout << obj.minOperations(s1, s2) << endl;\n\n    return 0;\n}",
        "java": "// Class containing the solution\nclass Solution {\n    // Function to calculate minimum operations\n    public int minOperations(String s1, String s2) {\n        // Get lengths of both strings\n        int n = s1.length(), m = s2.length();\n\n        // Create two DP arrays\n        int[] prev = new int[m + 1];\n        int[] cur = new int[m + 1];\n\n        // Loop through characters of s1\n        for (int i = 1; i <= n; i++) {\n\n            // Loop through characters of s2\n            for (int j = 1; j <= m; j++) {\n\n                // If characters match, increment LCS\n                if (s1.charAt(i - 1) == s2.charAt(j - 1))\n                    cur[j] = 1 + prev[j - 1];\n\n                // Else, take max from left or top\n                else\n                    cur[j] = Math.max(prev[j], cur[j - 1]);\n            }\n\n            // Update prev with cur\n            prev = cur.clone();\n        }\n\n        // Calculate total operations\n        int lcs = prev[m];\n        return (n - lcs) + (m - lcs);\n    }\n}\n\n// Main class\npublic class Main {\n    public static void main(String[] args) {\n        // Input strings\n        String s1 = \"heap\", s2 = \"pea\";\n\n        // Create object of Solution\n        Solution obj = new Solution();\n\n        // Call function and print result\n        System.out.println(obj.minOperations(s1, s2));\n    }\n}",
        "python": "# Class containing the solution\nclass Solution:\n    # Function to calculate minimum operations\n    def minOperations(self, s1: str, s2: str) -> int:\n        # Get lengths of strings\n        n, m = len(s1), len(s2)\n\n        # Initialize DP rows\n        prev = [0] * (m + 1)\n        cur = [0] * (m + 1)\n\n        # Loop through characters of s1\n        for i in range(1, n + 1):\n\n            # Loop through characters of s2\n            for j in range(1, m + 1):\n\n                # If characters match\n                if s1[i - 1] == s2[j - 1]:\n                    cur[j] = 1 + prev[j - 1]\n\n                # Else take max\n                else:\n                    cur[j] = max(prev[j], cur[j - 1])\n\n            # Update prev to cur\n            prev = cur[:]\n\n        # Calculate result\n        lcs = prev[m]\n        return (n - lcs) + (m - lcs)\n\n# Driver code\ns1 = \"heap\"\ns2 = \"pea\"\nobj = Solution()\nprint(obj.minOperations(s1, s2))",
        "javascript": "// Class containing the solution\nclass Solution {\n    // Function to calculate minimum operations\n    minOperations(s1, s2) {\n        // Get lengths of strings\n        const n = s1.length, m = s2.length;\n\n        // Initialize DP rows\n        let prev = new Array(m + 1).fill(0);\n        let cur = new Array(m + 1).fill(0);\n\n        // Loop through characters of s1\n        for (let i = 1; i <= n; i++) {\n\n            // Loop through characters of s2\n            for (let j = 1; j <= m; j++) {\n\n                // If characters match\n                if (s1[i - 1] === s2[j - 1])\n                    cur[j] = 1 + prev[j - 1];\n\n                // Else take max\n                else\n                    cur[j] = Math.max(prev[j], cur[j - 1]);\n            }\n\n            // Update prev to cur\n            prev = [...cur];\n        }\n\n        // Calculate result\n        const lcs = prev[m];\n        return (n - lcs) + (m - lcs);\n    }\n}\n\n// Driver code\nconst s1 = \"heap\";\nconst s2 = \"pea\";\nconst obj = new Solution();\nconsole.log(obj.minOperations(s1, s2));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:24Z"
}