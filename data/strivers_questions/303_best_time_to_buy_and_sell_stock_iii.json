{
  "problem_id": "303",
  "problem_name": "Best time to buy and sell stock III",
  "step_no": 16,
  "step_name": "Dynamic Programming [Patterns and Problems]",
  "substep_no": 6,
  "substep_name": "DP on Stocks",
  "difficulty": "Medium",
  "youtube_url": "https://youtu.be/-uQGzhYj8BQ",
  "leetcode_url": "https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iii/description/",
  "article_url": "https://takeuforward.org/data-structure/buy-and-sell-stock-iii-dp-37/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/best-time-to-buy-and-sell-stock-iii",
  "problem_statement": "We are given an array Arr[] of length n. It represents the price of a stock on ‘n’ days. The following guidelines need to be followed:\n\nExpand any one approach by clicking the given options in the bar. Clicking one approach on bar,\ncloses all other expands. You can manually expand more than one approach at a time",
  "examples": [
    "Example 1:\nArray={3, 3, 5, 0, 0, 1, 4}\nN=8\nMaxProfit = 6 as ((3-0) + (4-1))\nExplaination: Buy the stock on 3rd day at price 0 and sell the stock on 5th day at the price of 3. Then again buy stock at day 6 and sell the stock at 7th day at the price of 4"
  ],
  "total_approaches": 3,
  "approaches": [
    {
      "approach_name": "Memoization Approach",
      "algorithm": "Intuition\nEvery day, we can either skip trading or buy/sell a stock based on whether we are holding one and how many trades are left. To find the maximum profit, we try all possible choices using recursion.\n\nThink in terms of three things: the current day, whether we can buy or need to sell, and how many trades are left.\nIf we can buy: either skip or buy the stock.\nIf we need to sell: either skip or sell the stock (which completes one trade).\nReturn the maximum profit from these options.\n\nBase Cases\n\nIf all days are finished → profit is 0.\nIf no trades are left → profit is 0.\n\nMemoization\n\nUse a table to store answers for each combination of day, buy/sell state, and remaining trades.\nIf a value is already calculated, use it instead of recalculating.",
      "time_complexity": "O(N*2*3)",
      "space_complexity": "O(N*2*3)",
      "time_complexity_details": "Time Complexity: O(N*2*3) , There are N*2*3 states therefore at max ‘N*2*3’ new problems will be solved.",
      "space_complexity_details": "Space Complexity: O(N*2*3) + O(N) , We are using a recursion stack space(O(N)) and a 3D array ( O(N*2*3)).",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\nclass StockProfit{\nint getAns(vector<int>& Arr, int n, int ind, int buy, int cap, vector<vector<vector<int>>>& dp) {\n    // Base case: If we reach the end of the array or run out of allowed transactions, return 0.\n    if (ind == n || cap == 0)\n        return 0;\n\n    // If the result is already calculated, return it.\n    if (dp[ind][buy][cap] != -1)\n        return dp[ind][buy][cap];\n\n    int profit;\n\n    if (buy == 0) { // We can buy the stock\n        profit = max(0 + getAns(Arr, n, ind + 1, 0, cap, dp),\n                     -Arr[ind] + getAns(Arr, n, ind + 1, 1, cap, dp));\n    }\n\n    if (buy == 1) { // We can sell the stock\n        profit = max(0 + getAns(Arr, n, ind + 1, 1, cap, dp),\n                     Arr[ind] + getAns(Arr, n, ind + 1, 0, cap - 1, dp));\n    }\n\n    // Store and return the calculated profit.\n    return dp[ind][buy][cap] = profit;\n}\n\nint maxProfit(vector<int>& prices, int n) {\n    // Creating a 3D DP array of size [n][2][3]\n    vector<vector<vector<int>>> dp(n, vector<vector<int>>(2, vector<int>(3, -1)));\n\n    // Call the recursive function to calculate maximum profit\n    return getAns(prices, n, 0, 0, 2, dp);\n}\n\nint main() {\n    vector<int> prices = {3, 3, 5, 0, 0, 3, 1, 4};\n    int n = prices.size();\n\n    // Call the maxProfit function and print the result\n    cout << \"The maximum profit that can be generated is \" << maxProfit(prices, n);\n\n    return 0;\n}",
        "java": "import java.util.Arrays;\n\nclass StockProfit {\n    static int getAns(int[] Arr, int n, int ind, int buy, int cap, int[][][] dp) {\n        // Base case: If we have processed all stocks or have no capital left, return 0 profit\n        if (ind == n || cap == 0)\n            return 0;\n\n        // If the result for this state is already calculated, return it\n        if (dp[ind][buy][cap] != -1)\n            return dp[ind][buy][cap];\n\n        int profit;\n\n        if (buy == 0) { // We can buy the stock\n            profit = Math.max(0 + getAns(Arr, n, ind + 1, 0, cap, dp),\n                    -Arr[ind] + getAns(Arr, n, ind + 1, 1, cap, dp));\n        }\n\n        if (buy == 1) { // We can sell the stock\n            profit = Math.max(0 + getAns(Arr, n, ind + 1, 1, cap, dp),\n                    Arr[ind] + getAns(Arr, n, ind + 1, 0, cap - 1, dp));\n        }\n\n        // Store the calculated profit in the dp array and return it\n        return dp[ind][buy][cap] = profit;\n    }\n\n    static int maxProfit(int[] prices) {\n        int n = prices.length;\n\n        // Creating a 3D dp array of size [n][2][3]\n        int[][][] dp = new int[n][2][3];\n\n        // Initialize the dp array with -1\n        for (int i = 0; i < n; i++) {\n            for (int j = 0; j < 2; j++) {\n                Arrays.fill(dp[i][j], -1);\n            }\n        }\n\n        // Calculate and return the maximum profit\n        return getAns(prices, n, 0, 0, 2, dp);\n    }\n\n    public static void main(String[] args) {\n        int[] prices = {3, 3, 5, 0, 0, 3, 1, 4};\n        int n = prices.length;\n\n        // Calculate and print the maximum profit\n        System.out.println(\"The maximum profit that can be generated is \" + maxProfit(prices));\n    }\n}",
        "python": "class StockProfit{\ndef maxProfit(prices):\n    n = len(prices)\n    \n    # Create a 3D DP table with dimensions (n) x 2 x 3 and initialize it with -1 values\n    dp = [[[-1 for _ in range(3)] for _ in range(2)] for _ in range(n)]\n    \n    def getAns(ind, buy, cap):\n        # Recursive function to calculate the maximum profit\n        \n        if ind == n or cap == 0:\n            return 0  # Base case: If we have reached the end of the array or used up all transactions, return zero profit\n        \n        if dp[ind][buy][cap] != -1:\n            return dp[ind][buy][cap]  # If the result is already computed, return it\n        \n        profit = 0\n        \n        if buy == 0:\n            # We can buy the stock\n            profit = max(0 + getAns(ind + 1, 0, cap), -prices[ind] + getAns(ind + 1, 1, cap))\n        elif buy == 1:\n            # We can sell the stock\n            profit = max(0 + getAns(ind + 1, 1, cap), prices[ind] + getAns(ind + 1, 0, cap - 1))\n        \n        dp[ind][buy][cap] = profit  # Store the result in the DP table\n        return profit\n\n    return getAns(0, 0, 2)  # Start with buying (0) and 2 transactions available (cap=2)\n\ndef main():\n    prices = [3, 3, 5, 0, 0, 3, 1, 4]\n\n    max_profit = maxProfit(prices)\n    print(\"The maximum profit that can be generated is\", max_profit)\n\nif __name__ == \"__main__\":\n    main()",
        "javascript": "class StockProfit{\nfunction maxProfit(prices) {\n    const n = prices.length;\n\n    // Creating a 3D array to store dynamic programming values\n    const dp = new Array(n).fill(null).map(() =>\n        new Array(2).fill(null).map(() =>\n            new Array(3).fill(-1)\n        )\n    );\n\n    function getAns(ind, buy, cap) {\n        if (ind === n || cap === 0) return 0; // Base case\n\n        if (dp[ind][buy][cap] !== -1)\n            return dp[ind][buy][cap];\n\n        let profit;\n\n        if (buy === 0) { // We can buy the stock\n            profit = Math.max(\n                0 + getAns(ind + 1, 0, cap, dp),\n                -prices[ind] + getAns(ind + 1, 1, cap, dp)\n            );\n        }\n\n        if (buy === 1) { // We can sell the stock\n            profit = Math.max(\n                0 + getAns(ind + 1, 1, cap, dp),\n                prices[ind] + getAns(ind + 1, 0, cap - 1, dp)\n            );\n        }\n\n        dp[ind][buy][cap] = profit;\n        return profit;\n    }\n\n    // Call the recursive function to compute the result\n    return getAns(0, 0, 2);\n}\n\n// Main function\nfunction main() {\n    const prices = [3, 3, 5, 0, 0, 3, 1, 4];\n    const n = prices.length;\n\n    // Calculate the maximum profit\n    const maxProfitValue = maxProfit(prices);\n\n    console.log(\"The maximum profit that can be generated is\", maxProfitValue);\n}\n\n// Call the main function to start the program\nmain();"
      }
    },
    {
      "approach_name": "Tabulation Approach",
      "algorithm": "Tabulation Approach\nWe change the memoization method into a table-based method by creating a 3D table for all days, buy/sell states, and remaining trades.\nBase Cases\n\nIf we have finished all days, the profit is 0.\nIf no trades are left, the profit is 0.\nWe can just fill the whole table with 0 at the start to handle this.\n\nFilling the Table\n\nStart from the last day and move to the first day.\nOn each day, check both choices: either do the trade or skip it.\nTake the better (maximum profit) of the two choices and save it in the table.\n\nFinal Answer\nThe answer is the value at the first day when we can still buy and have two trades left.",
      "time_complexity": "O(N*2*3)",
      "space_complexity": "O(N*2*3)",
      "time_complexity_details": "Time Complexity: O(N*2*3) There are three nested loops that account for O(N*2*3) complexity.",
      "space_complexity_details": "Space Complexity: O(N*2*3), We are using an external array of size ‘N*2*3’. Stack Space is eliminated.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\nclass StockProfit{\nint maxProfit(vector<int>& Arr, int n) {\n    // Creating a 3D DP array of size [n+1][2][3] initialized to 0\n    vector<vector<vector<int>>> dp(n + 1, vector<vector<int>>(2, vector<int>(3, 0)));\n\n    // Base case: dp array is already initialized to 0, covering the base case.\n\n    for (int ind = n - 1; ind >= 0; ind--) {\n        for (int buy = 0; buy <= 1; buy++) {\n            for (int cap = 1; cap <= 2; cap++) {\n                if (buy == 0) { // We can buy the stock\n                    dp[ind][buy][cap] = max(0 + dp[ind + 1][0][cap],\n                                           -Arr[ind] + dp[ind + 1][1][cap]);\n                }\n\n                if (buy == 1) { // We can sell the stock\n                    dp[ind][buy][cap] = max(0 + dp[ind + 1][1][cap],\n                                           Arr[ind] + dp[ind + 1][0][cap - 1]);\n                }\n            }\n        }\n    }\n\n    // The result is stored in dp[0][0][2] which represents maximum profit after the final transaction.\n    return dp[0][0][2];\n}\n\nint main() {\n    vector<int> prices = {3, 3, 5, 0, 0, 3, 1, 4};\n    int n = prices.size();\n\n    // Call the maxProfit function and print the result\n    cout << \"The maximum profit that can be generated is \" << maxProfit(prices, n);\n\n    return 0;\n}",
        "java": "import java.util.*;\n\npublic class StockProfit {\n\n    public int maxProfit(int[] prices, int n) {\n        // DP table: dp[ind][buy][cap]\n        // ind → current index\n        // buy → 0 = can buy, 1 = can sell\n        // cap → remaining transactions (max 2)\n        int[][][] dp = new int[n + 1][2][3];\n\n        // Base case: initialized to 0 → no profit if no transactions left or no days left\n\n        // Fill DP table from the last day to the first\n        for (int ind = n - 1; ind >= 0; ind--) {\n            for (int buy = 0; buy <= 1; buy++) {\n                for (int cap = 1; cap <= 2; cap++) { // cap starts from 1 because 0 means no transactions left\n                    if (buy == 0) {\n                        // Option 1: skip buying\n                        // Option 2: buy today → subtract price, keep cap same\n                        dp[ind][buy][cap] = Math.max(\n                            0 + dp[ind + 1][0][cap],\n                            -prices[ind] + dp[ind + 1][1][cap]\n                        );\n                    } else {\n                        // Option 1: skip selling\n                        // Option 2: sell today → add price, reduce cap by 1\n                        dp[ind][buy][cap] = Math.max(\n                            0 + dp[ind + 1][1][cap],\n                            prices[ind] + dp[ind + 1][0][cap - 1]\n                        );\n                    }\n                }\n            }\n        }\n\n        // Final result: start at index 0, can buy, with 2 transactions left\n        return dp[0][0][2];\n    }\n\n    public static void main(String[] args) {\n        StockProfit sp = new StockProfit();\n        int[] prices = {3, 3, 5, 0, 0, 3, 1, 4};\n        System.out.println(\"The maximum profit that can be generated is \" +\n                sp.maxProfit(prices, prices.length));\n    }\n}",
        "python": "class StockProfit:\n    def max_profit(self, prices):\n        \"\"\"\n        Bottom-up DP for maximum profit with at most 2 transactions.\n        :param prices: List of stock prices\n        :return: Maximum achievable profit\n        \"\"\"\n        n = len(prices)\n        # DP table: dp[ind][buy][cap]\n        dp = [[[0] * 3 for _ in range(2)] for _ in range(n + 1)]\n\n        # Base case: already initialized to 0\n\n        # Fill table from the last day backwards\n        for ind in range(n - 1, -1, -1):\n            for buy in range(2):\n                for cap in range(1, 3):  # cap starts at 1\n                    if buy == 0:\n                        # Option 1: skip buying\n                        # Option 2: buy today\n                        dp[ind][buy][cap] = max(\n                            0 + dp[ind + 1][0][cap],\n                            -prices[ind] + dp[ind + 1][1][cap]\n                        )\n                    else:\n                        # Option 1: skip selling\n                        # Option 2: sell today\n                        dp[ind][buy][cap] = max(\n                            0 + dp[ind + 1][1][cap],\n                            prices[ind] + dp[ind + 1][0][cap - 1]\n                        )\n\n        return dp[0][0][2]\n\n\n# Driver code\nif __name__ == \"__main__\":\n    sp = StockProfit()\n    prices = [3, 3, 5, 0, 0, 3, 1, 4]\n    print(\"The maximum profit that can be generated is\", sp.max_profit(prices))",
        "javascript": "class StockProfit {\n    /**\n     * Bottom-up DP for maximum profit with at most 2 transactions\n     * @param {number[]} prices - Stock prices array\n     * @returns {number} Maximum achievable profit\n     */\n    maxProfit(prices) {\n        const n = prices.length;\n        // DP table: dp[ind][buy][cap]\n        const dp = Array.from({ length: n + 1 }, () =>\n            Array.from({ length: 2 }, () => Array(3).fill(0))\n        );\n\n        // Base case: already 0\n\n        // Fill from last day to first\n        for (let ind = n - 1; ind >= 0; ind--) {\n            for (let buy = 0; buy <= 1; buy++) {\n                for (let cap = 1; cap <= 2; cap++) {\n                    if (buy === 0) {\n                        // Option 1: skip buying\n                        // Option 2: buy today\n                        dp[ind][buy][cap] = Math.max(\n                            0 + dp[ind + 1][0][cap],\n                            -prices[ind] + dp[ind + 1][1][cap]\n                        );\n                    } else {\n                        // Option 1: skip selling\n                        // Option 2: sell today\n                        dp[ind][buy][cap] = Math.max(\n                            0 + dp[ind + 1][1][cap],\n                            prices[ind] + dp[ind + 1][0][cap - 1]\n                        );\n                    }\n                }\n            }\n        }\n\n        return dp[0][0][2];\n    }\n}\n\n// Driver code\nconst sp = new StockProfit();\nconst prices = [3, 3, 5, 0, 0, 3, 1, 4];\nconsole.log(\"The maximum profit that can be generated is\", sp.maxProfit(prices));"
      }
    },
    {
      "approach_name": "Space Optimization Approach",
      "algorithm": "Space Optimization\nWe only need the values from the next day to calculate the current day’s values, so we don’t need to keep the entire 3D table.\n\nKeep one table (ahead) to store the next day’s values and fill it with 0 initially.\nUse another table (cur) to calculate the current day’s values.\nFor each day, calculate all values using ahead and store them in cur.\nAfter finishing the day, copy cur into ahead for the next calculation.\nThe final answer is the value in ahead for the first day with two trades left.",
      "time_complexity": "O(N*2*3)",
      "space_complexity": "O(1)",
      "time_complexity_details": "Time Complexity: O(N*2*3) . There are three nested loops that account for O(N*2*3) complexity",
      "space_complexity_details": "Space Complexity: O(1),We are using two external arrays of size ‘2*3’.",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\nclass StockProfit{\nint maxProfit(vector<int>& Arr, int n) {\n    // Create two 2D arrays to store the profit information, one for the current state and one for the ahead state.\n    vector<vector<int>> ahead(2, vector<int>(3, 0));\n    vector<vector<int>> cur(2, vector<int>(3, 0));\n\n    for (int ind = n - 1; ind >= 0; ind--) {\n        for (int buy = 0; buy <= 1; buy++) {\n            for (int cap = 1; cap <= 2; cap++) {\n                if (buy == 0) { // We can buy the stock\n                    cur[buy][cap] = max(0 + ahead[0][cap], \n                                        -Arr[ind] + ahead[1][cap]);\n                }\n\n                if (buy == 1) { // We can sell the stock\n                    cur[buy][cap] = max(0 + ahead[1][cap],\n                                        Arr[ind] + ahead[0][cap - 1]);\n                }\n            }\n        }\n        // Update the ahead state with the current state for the next iteration.\n        ahead = cur;\n    }\n\n    return ahead[0][2];\n}\n\nint main() {\n    vector<int> prices = {3, 3, 5, 0, 0, 3, 1, 4};\n    int n = prices.size();\n\n    // Call the maxProfit function and print the result\n    cout << \"The maximum profit that can be generated is \" << maxProfit(prices, n);\n\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass StockProfit {\n    // Function to calculate the maximum profit with at most 2 transactions\n    public int maxProfit(int[] prices, int n) {\n        // 'ahead' stores future state, 'cur' stores current state\n        int[][] ahead = new int[2][3];\n        int[][] cur = new int[2][3];\n\n        // Iterate from the last day to the first day (reverse order)\n        for (int ind = n - 1; ind >= 0; ind--) {\n            for (int buy = 0; buy <= 1; buy++) { // 0 = buy possible, 1 = sell possible\n                for (int cap = 1; cap <= 2; cap++) { // At most 2 transactions\n                    if (buy == 0) {\n                        // Option 1: Skip buying, Option 2: Buy stock\n                        cur[buy][cap] = Math.max(ahead[0][cap],\n                                -prices[ind] + ahead[1][cap]);\n                    } else {\n                        // Option 1: Skip selling, Option 2: Sell stock\n                        cur[buy][cap] = Math.max(ahead[1][cap],\n                                prices[ind] + ahead[0][cap - 1]);\n                    }\n                }\n            }\n            // Move current state to ahead for the next iteration\n            for (int i = 0; i < 2; i++) {\n                System.arraycopy(cur[i], 0, ahead[i], 0, 3);\n            }\n        }\n\n        return ahead[0][2]; // Maximum profit starting at day 0 with 2 transactions\n    }\n\n    public static void main(String[] args) {\n        int[] prices = {3, 3, 5, 0, 0, 3, 1, 4};\n        int n = prices.length;\n        StockProfit sp = new StockProfit();\n        System.out.println(\"The maximum profit that can be generated is \" + sp.maxProfit(prices, n));\n    }\n}",
        "python": "class StockProfit:\n    def maxProfit(self, prices, n):\n        # 'ahead' stores future state, 'cur' stores current state\n        ahead = [[0] * 3 for _ in range(2)]\n        cur = [[0] * 3 for _ in range(2)]\n\n        # Iterate from the last day to the first day\n        for ind in range(n - 1, -1, -1):\n            for buy in range(2):  # 0 = buy possible, 1 = sell possible\n                for cap in range(1, 3):  # At most 2 transactions\n                    if buy == 0:\n                        # Option 1: Skip buying, Option 2: Buy stock\n                        cur[buy][cap] = max(ahead[0][cap],\n                                            -prices[ind] + ahead[1][cap])\n                    else:\n                        # Option 1: Skip selling, Option 2: Sell stock\n                        cur[buy][cap] = max(ahead[1][cap],\n                                            prices[ind] + ahead[0][cap - 1])\n            # Move current state to ahead for the next iteration\n            ahead = [row[:] for row in cur]\n\n        return ahead[0][2]  # Maximum profit starting at day 0 with 2 transactions\n\n\n# Driver code\nprices = [3, 3, 5, 0, 0, 3, 1, 4]\nn = len(prices)\nsp = StockProfit()\nprint(\"The maximum profit that can be generated is\", sp.maxProfit(prices, n))",
        "javascript": "class StockProfit {\n    // Function to calculate the maximum profit with at most 2 transactions\n    maxProfit(prices, n) {\n        // 'ahead' stores future state, 'cur' stores current state\n        let ahead = Array.from({ length: 2 }, () => Array(3).fill(0));\n        let cur = Array.from({ length: 2 }, () => Array(3).fill(0));\n\n        // Iterate from the last day to the first day\n        for (let ind = n - 1; ind >= 0; ind--) {\n            for (let buy = 0; buy <= 1; buy++) { // 0 = buy possible, 1 = sell possible\n                for (let cap = 1; cap <= 2; cap++) { // At most 2 transactions\n                    if (buy === 0) {\n                        // Option 1: Skip buying, Option 2: Buy stock\n                        cur[buy][cap] = Math.max(ahead[0][cap],\n                            -prices[ind] + ahead[1][cap]);\n                    } else {\n                        // Option 1: Skip selling, Option 2: Sell stock\n                        cur[buy][cap] = Math.max(ahead[1][cap],\n                            prices[ind] + ahead[0][cap - 1]);\n                    }\n                }\n            }\n            // Move current state to ahead for the next iteration\n            ahead = cur.map(row => [...row]);\n        }\n\n        return ahead[0][2]; // Maximum profit starting at day 0 with 2 transactions\n    }\n}\n\n// Driver code\nconst prices = [3, 3, 5, 0, 0, 3, 1, 4];\nconst n = prices.length;\nconst sp = new StockProfit();\nconsole.log(\"The maximum profit that can be generated is\", sp.maxProfit(prices, n));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:25Z"
}