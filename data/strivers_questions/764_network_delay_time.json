{
  "problem_id": "764",
  "problem_name": "Network Delay Time",
  "step_no": 15,
  "step_name": "Graphs [Concepts & Problems]",
  "substep_no": 4,
  "substep_name": "Shortest Path Algorithms and Problems",
  "difficulty": "Medium",
  "youtube_url": null,
  "leetcode_url": "https://leetcode.com/problems/network-delay-time/",
  "article_url": "https://takeuforward.org/data-structure/network-delay-time",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/network-delay-time",
  "problem_statement": "You are given a directed weighted graph representing a communication network with n nodes, numbered from 1 to n.\n\nThe graph is provided as an edge list times, where each record is of the form (ui, vi, wi) where,\nui is the source node of the directed edge\nvi is the target node of the directed edge\nwi is the time it takes for a signal to travel from ui to vi (non-negative and integer)\n\nA single signal is injected at node k at time 0.\n\nThe signal propagates 1-way along the directed edges and whenever it reaches a node, that node immediately retransmits the signal to all of its outgoing neighbors, each traversal taking exactly the edge’s weight wi units of time.\n\nReturn the minimum time required for every node in the network to receive the signal.\n\nIf some node is unreachable, return -1.",
  "examples": [
    "Input: times = [[2,1,1],[2,3,1],[3,4,1]], n = 4, k = 2\nOutput: 2\nExplanation: 2 -> 1 (1 unit) and 2 -> 3 -> 4 ( 2 unit )\nThe last node (4) gets the signal at time 2.\n\nInput: times = [[1,2,1]], n = 2, k = 1\nOutput: 1\nExplanation: Direct edge 1 -> 2 delivers signal in 1 unit of time."
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "We want to find the time it takes for a signal to reach all nodes from a starting point K.\nThis is equivalent to finding the shortest time (minimum delay) from K to every other node, which is exactly what Dijkstra’s algorithm does for graphs with non-negative edge weights.\nIt works by greedily selecting the nearest unvisited node, then relaxing its neighbors.\n \nCreate a graph using an adjacency list to store edges and their weights.\nUse a distance array to store the shortest time to reach each node, initialized to infinity except the source node, which is 0.\nUse a min-heap (priority queue) to always pick the node with the current shortest time.\nFor each node picked from the heap, check all its neighbors. If reaching a neighbor through the current node gives a shorter time, update the neighbor’s time and push it into the heap.\nRepeat until the heap is empty. At the end, check the maximum value in the distance array.\nIf any node's time is still infinity, return -1 (unreachable). Otherwise, return the maximum time.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find minimum time for every node to receive signal\n    int networkDelayTime(vector<vector<int>>& times, int n, int k) {\n        // Create adjacency list\n        vector<vector<pair<int, int>>> adj(n + 1);\n        for (auto& time : times) {\n            int u = time[0], v = time[1], w = time[2];\n            adj[u].push_back({v, w});\n        }\n\n        // Initialize min-heap\n        priority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;\n        pq.push({0, k});\n\n        // Initialize distance array\n        vector<int> dist(n + 1, INT_MAX);\n        dist[k] = 0;\n\n        // Process nodes\n        while (!pq.empty()) {\n            // Get the node with the smallest time\n            int time = pq.top().first;\n            int node = pq.top().second;\n            pq.pop();\n\n            // Traverse all neighbors of the current node\n            for (auto& [nbr, wt] : adj[node]) {\n                // If shorter path is found to neighbor\n                if (dist[nbr] > time + wt) {\n                    // Update distance\n                    dist[nbr] = time + wt;\n                    // Push updated time to the priority queue\n                    pq.push({dist[nbr], nbr});\n                }\n            }\n        }\n\n        // Get maximum time\n        int ans = *max_element(dist.begin() + 1, dist.end());\n        return ans == INT_MAX ? -1 : ans;\n    }\n};\n\nint main() {\n    Solution sol;\n    vector<vector<int>> times = {{2,1,1},{2,3,1},{3,4,1}};\n    int n = 4, k = 2;\n    cout << sol.networkDelayTime(times, n, k) << endl;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Function to find minimum time for every node to receive signal\n    public int networkDelayTime(int[][] times, int n, int k) {\n        // Create adjacency list\n        List<List<int[]>> adj = new ArrayList<>();\n        for (int i = 0; i <= n; i++) adj.add(new ArrayList<>());\n        for (int[] time : times) {\n            int u = time[0], v = time[1], w = time[2];\n            adj.get(u).add(new int[]{v, w});\n        }\n\n        // Initialize min-heap\n        PriorityQueue<int[]> pq = new PriorityQueue<>(Comparator.comparingInt(a -> a[0]));\n        pq.offer(new int[]{0, k});\n\n        // Initialize distance array\n        int[] dist = new int[n + 1];\n        Arrays.fill(dist, Integer.MAX_VALUE);\n        dist[k] = 0;\n\n        // Process nodes\n        while (!pq.isEmpty()) {\n            // Get node with smallest time\n            int[] curr = pq.poll();\n            int time = curr[0], node = curr[1];\n\n            // Traverse all neighbors of the node\n            for (int[] nbr : adj.get(node)) {\n                int v = nbr[0], wt = nbr[1];\n\n                // If shorter path to neighbor is found\n                if (dist[v] > time + wt) {\n                    // Update distance\n                    dist[v] = time + wt;\n                    // Add updated distance to priority queue\n                    pq.offer(new int[]{dist[v], v});\n                }\n            }\n        }\n\n        // Get maximum time\n        int ans = 0;\n        for (int i = 1; i <= n; i++) {\n            if (dist[i] == Integer.MAX_VALUE) return -1;\n            ans = Math.max(ans, dist[i]);\n        }\n        return ans;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[][] times = {{2,1,1},{2,3,1},{3,4,1}};\n        int n = 4, k = 2;\n        System.out.println(sol.networkDelayTime(times, n, k));\n    }\n}",
        "python": "import heapq\n\nclass Solution:\n    # Function to find minimum time for every node to receive signal\n    def networkDelayTime(self, times, n, k):\n        # Create adjacency list\n        adj = [[] for _ in range(n + 1)]\n        for u, v, w in times:\n            adj[u].append((v, w))\n\n        # Initialize min-heap\n        pq = [(0, k)]\n\n        # Initialize distance array\n        dist = [float('inf')] * (n + 1)\n        dist[k] = 0\n\n        # Process nodes\n        while pq:\n            # Pop node with smallest time\n            time, node = heapq.heappop(pq)\n\n            # Traverse neighbors of current node\n            for nbr, wt in adj[node]:\n                # If shorter path is found\n                if dist[nbr] > time + wt:\n                    # Update distance\n                    dist[nbr] = time + wt\n                    # Push updated distance to heap\n                    heapq.heappush(pq, (dist[nbr], nbr))\n\n        # Get maximum time\n        ans = max(dist[1:])\n        return -1 if ans == float('inf') else ans\n\n# Main function\nif __name__ == \"__main__\":\n    sol = Solution()\n    times = [[2,1,1],[2,3,1],[3,4,1]]\n    n, k = 4, 2\n    print(sol.networkDelayTime(times, n, k))",
        "javascript": "class Solution {\n    // Function to find minimum time for every node to receive signal\n    networkDelayTime(times, n, k) {\n        // Create adjacency list\n        const adj = Array.from({ length: n + 1 }, () => []);\n        for (let [u, v, w] of times) {\n            adj[u].push([v, w]);\n        }\n\n        // Initialize min-heap\n        const pq = [[0, k]];\n\n        // Initialize distance array\n        const dist = Array(n + 1).fill(Infinity);\n        dist[k] = 0;\n\n        // Process nodes\n        while (pq.length > 0) {\n            // Sort and get node with smallest time\n            pq.sort((a, b) => a[0] - b[0]);\n            const [time, node] = pq.shift();\n\n            // Traverse neighbors of current node\n            for (let [nbr, wt] of adj[node]) {\n                // If shorter path to neighbor is found\n                if (dist[nbr] > time + wt) {\n                    // Update distance\n                    dist[nbr] = time + wt;\n                    // Add updated time to the queue\n                    pq.push([dist[nbr], nbr]);\n                }\n            }\n        }\n\n        // Get maximum time\n        let ans = Math.max(...dist.slice(1));\n        return ans === Infinity ? -1 : ans;\n    }\n}\n\n// Main function\nconst sol = new Solution();\nconst times = [[2,1,1],[2,3,1],[3,4,1]];\nconst n = 4, k = 2;\nconsole.log(sol.networkDelayTime(times, n, k));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:20Z"
}