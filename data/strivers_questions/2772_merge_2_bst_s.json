{
  "problem_id": "2772",
  "problem_name": "Merge 2 BST's",
  "step_no": 14,
  "step_name": "Binary Search Trees [Concept and Problems]",
  "substep_no": 2,
  "substep_name": "Practice Problems",
  "difficulty": "Hard",
  "youtube_url": "https://youtu.be/D2jMcmxU4bs",
  "leetcode_url": "https://leetcode.com/problems/binary-search-tree-iterator/",
  "article_url": "https://takeuforward.org/data-structure/bst-iterator",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/bst-iterator",
  "problem_statement": "Given two BSTs, return elements of merged BSTs in sorted form.",
  "examples": [
    "Input: root1 = [5, 3, 6, 2, 4], root2 = [2, 1, 3, Null, Null, Null, 7, 6]\nOutput: [1, 2, 2, 3, 3, 4, 5, 6, 6, 7]\nExplanation: After merging and sorting the two BST we get [1, 2, 2, 3, 3, 4, 5, 6, 6, 7].\n\nInput: root1 = [12, 9, Null, 6, 11], root2 = [8, 5, 10, 2]\nOutput: [2, 5, 6, 8, 9, 10, 11, 12]\nExplanation: After merging and sorting the two BST we get [2, 5, 6, 8, 9, 10, 11, 12]."
  ],
  "total_approaches": 2,
  "approaches": [
    {
      "approach_name": "Brute Force Approach",
      "algorithm": "A naive approach to solve this problem is to simply traverse both of the trees using any technique and store all the elements in a list. Once we have all the elements, we can simply sort our list to get the elements of merged BST in sorted form.\n \n\nTraverse both BSTs using any traversal method (preorder, inorder, or postorder) and collect all elements in a single list.\nInsert elements from both trees into this list without worrying about order during traversal.\nSort the list using a sorting algorithm (like quicksort, mergesort, or built-in sort).\nReturn the sorted list as the merged elements of both BSTs.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Structure for a BST node\nstruct Node {\n    int data;\n    Node* left;\n    Node* right;\n    // Constructor to initialize node\n    Node(int val) : data(val), left(NULL), right(NULL) {}\n};\n\nclass Solution {\npublic:\n    // Function to traverse a BST and store elements in list\n    void traverse(Node* root, vector<int>& elements) {\n        // If root is NULL, stop recursion\n        if (!root) return;\n        // Traverse left subtree\n        traverse(root->left, elements);\n        // Store current node's data\n        elements.push_back(root->data);\n        // Traverse right subtree\n        traverse(root->right, elements);\n    }\n\n    // Function to merge elements of two BSTs\n    vector<int> mergeBSTs(Node* root1, Node* root2) {\n        // Vector to store all elements\n        vector<int> elements;\n        // Traverse first BST and collect elements\n        traverse(root1, elements);\n        // Traverse second BST and collect elements\n        traverse(root2, elements);\n        // Sort the collected elements\n        sort(elements.begin(), elements.end());\n        // Return the sorted elements\n        return elements;\n    }\n};\n\n// Driver code\nint main() {\n    // Creating first BST\n    Node* root1 = new Node(2);\n    root1->left = new Node(1);\n    root1->right = new Node(4);\n\n    // Creating second BST\n    Node* root2 = new Node(3);\n    root2->left = new Node(0);\n    root2->right = new Node(5);\n\n    // Creating Solution object\n    Solution sol;\n    // Merging BSTs\n    vector<int> result = sol.mergeBSTs(root1, root2);\n\n    // Printing merged sorted elements\n    for (int val : result) {\n        cout << val << \" \";\n    }\n}",
        "java": "import java.util.*;\n\n// Structure for a BST node\nclass Node {\n    int data;\n    Node left, right;\n    // Constructor to initialize node\n    Node(int val) {\n        data = val;\n        left = null;\n        right = null;\n    }\n}\n\nclass Solution {\n    // Function to traverse a BST and store elements in list\n    void traverse(Node root, List<Integer> elements) {\n        // If root is NULL, stop recursion\n        if (root == null) return;\n        // Traverse left subtree\n        traverse(root.left, elements);\n        // Store current node's data\n        elements.add(root.data);\n        // Traverse right subtree\n        traverse(root.right, elements);\n    }\n\n    // Function to merge elements of two BSTs\n    List<Integer> mergeBSTs(Node root1, Node root2) {\n        // List to store all elements\n        List<Integer> elements = new ArrayList<>();\n        // Traverse first BST and collect elements\n        traverse(root1, elements);\n        // Traverse second BST and collect elements\n        traverse(root2, elements);\n        // Sort the collected elements\n        Collections.sort(elements);\n        // Return the sorted elements\n        return elements;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Creating first BST\n        Node root1 = new Node(2);\n        root1.left = new Node(1);\n        root1.right = new Node(4);\n\n        // Creating second BST\n        Node root2 = new Node(3);\n        root2.left = new Node(0);\n        root2.right = new Node(5);\n\n        // Creating Solution object\n        Solution sol = new Solution();\n        // Merging BSTs\n        List<Integer> result = sol.mergeBSTs(root1, root2);\n\n        // Printing merged sorted elements\n        for (int val : result) {\n            System.out.print(val + \" \");\n        }\n    }\n}",
        "python": "# Structure for a BST node\nclass Node:\n    def __init__(self, val):\n        # Store node data\n        self.data = val\n        # Pointer to left child\n        self.left = None\n        # Pointer to right child\n        self.right = None\n\nclass Solution:\n    # Function to traverse a BST and store elements in list\n    def traverse(self, root, elements):\n        # If root is None, stop recursion\n        if not root:\n            return\n        # Traverse left subtree\n        self.traverse(root.left, elements)\n        # Store current node's data\n        elements.append(root.data)\n        # Traverse right subtree\n        self.traverse(root.right, elements)\n\n    # Function to merge elements of two BSTs\n    def mergeBSTs(self, root1, root2):\n        # List to store all elements\n        elements = []\n        # Traverse first BST and collect elements\n        self.traverse(root1, elements)\n        # Traverse second BST and collect elements\n        self.traverse(root2, elements)\n        # Sort the collected elements\n        elements.sort()\n        # Return the sorted elements\n        return elements\n\n# Driver code\nif __name__ == \"__main__\":\n    # Creating first BST\n    root1 = Node(2)\n    root1.left = Node(1)\n    root1.right = Node(4)\n\n    # Creating second BST\n    root2 = Node(3)\n    root2.left = Node(0)\n    root2.right = Node(5)\n\n    # Creating Solution object\n    sol = Solution()\n    # Merging BSTs\n    result = sol.mergeBSTs(root1, root2)\n\n    # Printing merged sorted elements\n    print(*result)",
        "javascript": "// Structure for a BST node\nclass Node {\n    constructor(val) {\n        // Store node data\n        this.data = val;\n        // Pointer to left child\n        this.left = null;\n        // Pointer to right child\n        this.right = null;\n    }\n}\n\nclass Solution {\n    // Function to traverse a BST and store elements in list\n    traverse(root, elements) {\n        // If root is null, stop recursion\n        if (!root) return;\n        // Traverse left subtree\n        this.traverse(root.left, elements);\n        // Store current node's data\n        elements.push(root.data);\n        // Traverse right subtree\n        this.traverse(root.right, elements);\n    }\n\n    // Function to merge elements of two BSTs\n    mergeBSTs(root1, root2) {\n        // Array to store all elements\n        let elements = [];\n        // Traverse first BST and collect elements\n        this.traverse(root1, elements);\n        // Traverse second BST and collect elements\n        this.traverse(root2, elements);\n        // Sort the collected elements\n        elements.sort((a, b) => a - b);\n        // Return the sorted elements\n        return elements;\n    }\n}\n\n// Driver code\n// Creating first BST\nlet root1 = new Node(2);\nroot1.left = new Node(1);\nroot1.right = new Node(4);\n\n// Creating second BST\nlet root2 = new Node(3);\nroot2.left = new Node(0);\nroot2.right = new Node(5);\n\n// Creating Solution object\nlet sol = new Solution();\n// Merging BSTs\nlet result = sol.mergeBSTs(root1, root2);\n\n// Printing merged sorted elements\nconsole.log(result.join(\" \"));"
      }
    },
    {
      "approach_name": "Optimal Approach",
      "algorithm": "We know that inorder traversal of any BST returns the elements in sorted order. Thus, we will use this property to optimize our solution. We can simply traverse both the BSTs one by one and store their elements in separate lists. Since, both these lists are individually sorted, we can just merge these sorted lists to get our elements of merged BST in sorted form.\n \n\nTraverse both BSTs using inorder traversal method and collect all elements in separate lists.\nCreate an additional list to store the elements of the two sorted lists.\nMerge the sorted lists using a two pointer approach.\nReturn the sorted list as the merged elements of both BSTs.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Structure for a binary tree node\nstruct Node {\n    int data;\n    Node* left;\n    Node* right;\n    // Constructor to initialize node\n    Node(int val) : data(val), left(NULL), right(NULL) {}\n};\n\nclass Solution {\npublic:\n    // Function to perform inorder traversal and store elements\n    void inorderTraversal(Node* root, vector<int>& arr) {\n        // Base case\n        if (!root) return;\n        // Traverse left subtree\n        inorderTraversal(root->left, arr);\n        // Store current node data\n        arr.push_back(root->data);\n        // Traverse right subtree\n        inorderTraversal(root->right, arr);\n    }\n\n    // Function to merge two sorted arrays\n    vector<int> mergeArrays(vector<int>& arr1, vector<int>& arr2) {\n        // Initialize result array\n        vector<int> merged;\n        // Initialize pointers\n        int i = 0, j = 0;\n        // Merge until one array ends\n        while (i < arr1.size() && j < arr2.size()) {\n            if (arr1[i] < arr2[j]) merged.push_back(arr1[i++]);\n            else merged.push_back(arr2[j++]);\n        }\n        // Add remaining elements\n        while (i < arr1.size()) merged.push_back(arr1[i++]);\n        while (j < arr2.size()) merged.push_back(arr2[j++]);\n        return merged;\n    }\n\n    // Function to merge two BSTs in sorted order\n    vector<int> mergeBSTs(Node* root1, Node* root2) {\n        // Arrays to store inorder traversals\n        vector<int> arr1, arr2;\n        // Perform inorder traversal on first BST\n        inorderTraversal(root1, arr1);\n        // Perform inorder traversal on second BST\n        inorderTraversal(root2, arr2);\n        // Merge and return the result\n        return mergeArrays(arr1, arr2);\n    }\n};\n\n// Driver code\nint main() {\n    // Create first BST\n    Node* root1 = new Node(3);\n    root1->left = new Node(1);\n    root1->right = new Node(5);\n\n    // Create second BST\n    Node* root2 = new Node(4);\n    root2->left = new Node(2);\n    root2->right = new Node(6);\n\n    // Create Solution object\n    Solution sol;\n    // Merge the BSTs\n    vector<int> result = sol.mergeBSTs(root1, root2);\n\n    // Print result\n    for (int val : result) cout << val << \" \";\n    return 0;\n}",
        "java": "import java.util.*;\n\n// Node structure for BST\nclass Node {\n    int data;\n    Node left, right;\n    Node(int val) {\n        data = val;\n        left = right = null;\n    }\n}\n\nclass Solution {\n    // Function to perform inorder traversal\n    void inorderTraversal(Node root, List<Integer> arr) {\n        // Base case\n        if (root == null) return;\n        // Traverse left subtree\n        inorderTraversal(root.left, arr);\n        // Store current node data\n        arr.add(root.data);\n        // Traverse right subtree\n        inorderTraversal(root.right, arr);\n    }\n\n    // Function to merge two sorted lists\n    List<Integer> mergeArrays(List<Integer> arr1, List<Integer> arr2) {\n        // Initialize result list\n        List<Integer> merged = new ArrayList<>();\n        // Initialize pointers\n        int i = 0, j = 0;\n        // Merge until one list ends\n        while (i < arr1.size() && j < arr2.size()) {\n            if (arr1.get(i) < arr2.get(j)) merged.add(arr1.get(i++));\n            else merged.add(arr2.get(j++));\n        }\n        // Add remaining elements\n        while (i < arr1.size()) merged.add(arr1.get(i++));\n        while (j < arr2.size()) merged.add(arr2.get(j++));\n        return merged;\n    }\n\n    // Function to merge two BSTs\n    List<Integer> mergeBSTs(Node root1, Node root2) {\n        // Lists to store inorder traversals\n        List<Integer> arr1 = new ArrayList<>();\n        List<Integer> arr2 = new ArrayList<>();\n        // Perform inorder traversal\n        inorderTraversal(root1, arr1);\n        inorderTraversal(root2, arr2);\n        // Merge and return\n        return mergeArrays(arr1, arr2);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Create first BST\n        Node root1 = new Node(3);\n        root1.left = new Node(1);\n        root1.right = new Node(5);\n\n        // Create second BST\n        Node root2 = new Node(4);\n        root2.left = new Node(2);\n        root2.right = new Node(6);\n\n        // Create Solution object\n        Solution sol = new Solution();\n        // Merge BSTs\n        List<Integer> result = sol.mergeBSTs(root1, root2);\n\n        // Print result\n        for (int val : result) System.out.print(val + \" \");\n    }\n}",
        "python": "# Node structure for BST\nclass Node:\n    def __init__(self, val):\n        self.data = val\n        self.left = None\n        self.right = None\n\nclass Solution:\n    # Function to perform inorder traversal\n    def inorderTraversal(self, root, arr):\n        # Base case\n        if not root:\n            return\n        # Traverse left subtree\n        self.inorderTraversal(root.left, arr)\n        # Store current node data\n        arr.append(root.data)\n        # Traverse right subtree\n        self.inorderTraversal(root.right, arr)\n\n    # Function to merge two sorted lists\n    def mergeArrays(self, arr1, arr2):\n        # Initialize result\n        merged = []\n        # Initialize pointers\n        i = j = 0\n        # Merge until one list ends\n        while i < len(arr1) and j < len(arr2):\n            if arr1[i] < arr2[j]:\n                merged.append(arr1[i])\n                i += 1\n            else:\n                merged.append(arr2[j])\n                j += 1\n        # Add remaining elements\n        merged.extend(arr1[i:])\n        merged.extend(arr2[j:])\n        return merged\n\n    # Function to merge two BSTs\n    def mergeBSTs(self, root1, root2):\n        # Lists to store inorder traversals\n        arr1, arr2 = [], []\n        # Perform inorder traversals\n        self.inorderTraversal(root1, arr1)\n        self.inorderTraversal(root2, arr2)\n        # Merge and return\n        return self.mergeArrays(arr1, arr2)\n\n# Driver code\nif __name__ == \"__main__\":\n    # Create first BST\n    root1 = Node(3)\n    root1.left = Node(1)\n    root1.right = Node(5)\n\n    # Create second BST\n    root2 = Node(4)\n    root2.left = Node(2)\n    root2.right = Node(6)\n\n    # Create Solution object\n    sol = Solution()\n    # Merge BSTs\n    result = sol.mergeBSTs(root1, root2)\n\n    # Print result\n    print(*result)",
        "javascript": "// Node structure for BST\nclass Node {\n    constructor(val) {\n        this.data = val;\n        this.left = null;\n        this.right = null;\n    }\n}\n\nclass Solution {\n    // Function to perform inorder traversal\n    inorderTraversal(root, arr) {\n        // Base case\n        if (!root) return;\n        // Traverse left subtree\n        this.inorderTraversal(root.left, arr);\n        // Store current node data\n        arr.push(root.data);\n        // Traverse right subtree\n        this.inorderTraversal(root.right, arr);\n    }\n\n    // Function to merge two sorted arrays\n    mergeArrays(arr1, arr2) {\n        // Initialize result\n        let merged = [];\n        // Initialize pointers\n        let i = 0, j = 0;\n        // Merge until one array ends\n        while (i < arr1.length && j < arr2.length) {\n            if (arr1[i] < arr2[j]) merged.push(arr1[i++]);\n            else merged.push(arr2[j++]);\n        }\n        // Add remaining elements\n        while (i < arr1.length) merged.push(arr1[i++]);\n        while (j < arr2.length) merged.push(arr2[j++]);\n        return merged;\n    }\n\n    // Function to merge two BSTs\n    mergeBSTs(root1, root2) {\n        // Arrays to store inorder traversals\n        let arr1 = [], arr2 = [];\n        // Perform inorder traversals\n        this.inorderTraversal(root1, arr1);\n        this.inorderTraversal(root2, arr2);\n        // Merge and return\n        return this.mergeArrays(arr1, arr2);\n    }\n}\n\n// Driver code\nlet root1 = new Node(3);\nroot1.left = new Node(1);\nroot1.right = new Node(5);\n\nlet root2 = new Node(4);\nroot2.left = new Node(2);\nroot2.right = new Node(6);\n\nlet sol = new Solution();\nlet result = sol.mergeBSTs(root1, root2);\n\nconsole.log(result.join(\" \"));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:18Z"
}