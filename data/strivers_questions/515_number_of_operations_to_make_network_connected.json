{
  "problem_id": "515",
  "problem_name": "Number of operations to make network connected",
  "step_no": 15,
  "step_name": "Graphs [Concepts & Problems]",
  "substep_no": 5,
  "substep_name": "MinimumSpanningTree/Disjoint Set and Problems",
  "difficulty": "Hard",
  "youtube_url": "https://youtu.be/FYrl7iz9_ZU",
  "leetcode_url": "https://leetcode.com/problems/number-of-operations-to-make-network-connected/",
  "article_url": "https://takeuforward.org/data-structure/number-of-operations-to-make-network-connected-dsu-g-49/",
  "plus_url": "https://takeuforward.org/plus/dsa/problems/number-of-operations-to-make-network-connected",
  "problem_statement": "You are given a graph with n vertices and m edges. You can remove one edge from anywhere and add that edge between any two vertices in one operation. Find the minimum number of operations that will be required to make the graph connected. If it is not possible to make the graph connected, return -1.",
  "examples": [
    "Input: N = 4, M = 3, Edge[] =[ [0, 1], [ 0, 2], [1, 2]]\n\nOutput: 1\nExplanation: We need a minimum of 1 operation to make the two components connected. We can remove the edge (1,2) and add the edge between node 2 and node 3 like the following:\n\nInput: N = 9, M = 8, Edge[] = [[0,1],[0,2],[0,3],[1,2],[2,3],[4,5],[5,6],[7,8]]\n\nOutput: 1\nExplanation : We need a minimum of 2 operations to make the two components connected. We can remove the edge (0,2) and add the edge between node 3 and node 4 and we can remove the edge (0,3) and add it between nodes 6 and 8 like the following:"
  ],
  "total_approaches": 1,
  "approaches": [
    {
      "approach_name": "Approach",
      "algorithm": "Our objective in this question is to make the given graph connected. Now, we can do this by just connecting the different components of a given graph with some edges like the following:\n\n But according to the question, we cannot add random edges from outside rather we can remove any given edge and add that to a new position to fulfill the purpose. In order to connect two different components of a graph we need to connect any node of the first component to any node of the second component.\n From the method of connecting the components, discussed above, we can conclude that we need a minimum of nuber of components-1 edges to make the graph connected.\n \n\n Thus we can conclude that if graph has more than n-1 extra edges, then we connect all components.\n \nInitialize DSU (Disjoint Set Union) structure to keep track of connected components in the graph.\nIterate over all the edges, and for each edge, check if both nodes are already connected using DSU.\nIf the nodes are already connected (i.e., they have the same parent), it means the edge is extra (redundant), so count it as an extra edge.\nIf the nodes are not connected, perform a union operation to connect the components.\nAfter processing all edges, count the number of connected components by counting the number of unique parents in DSU.\nTo make the graph connected, we need (components - 1) edges. If the number of extra edges is at least (components - 1), then we can connect the graph using those, otherwise, it’s impossible.\nReturn (components - 1) if possible, else return -1.",
      "time_complexity": "O(N)",
      "space_complexity": "O(1)",
      "time_complexity_details": "",
      "space_complexity_details": "",
      "codes": {
        "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Class to manage Disjoint Set Union\nclass DSU {\npublic:\n    vector<int> parent, rank;\n\n    // Initialize DSU with n nodes\n    DSU(int n) {\n        parent.resize(n);\n        rank.resize(n, 0);\n\n        // Initially, every node is its own parent\n        for(int i = 0; i < n; i++) {\n            parent[i] = i;\n        }\n    }\n\n    // Find with path compression\n    int find(int x) {\n        if(parent[x] != x)\n            parent[x] = find(parent[x]);\n        return parent[x];\n    }\n\n    // Union by rank\n    void unite(int x, int y) {\n        int px = find(x);\n        int py = find(y);\n\n        if(px == py) return;\n\n        if(rank[px] < rank[py])\n            parent[px] = py;\n        else if(rank[px] > rank[py])\n            parent[py] = px;\n        else {\n            parent[py] = px;\n            rank[px]++;\n        }\n    }\n};\n\n// Solution class to calculate operations\nclass Solution {\npublic:\n    // Function to find minimum operations to make the graph connected\n    int makeConnected(int n, vector<vector<int>>& connections) {\n        // If edges are less than n - 1, we can't connect\n        if(connections.size() < n - 1) return -1;\n\n        DSU dsu(n);\n\n        // Connect components\n        for(auto& edge : connections) {\n            dsu.unite(edge[0], edge[1]);\n        }\n\n        // Count number of unique components\n        unordered_set<int> components;\n        for(int i = 0; i < n; i++) {\n            components.insert(dsu.find(i));\n        }\n\n        // Return number of operations needed\n        return components.size() - 1;\n    }\n};\n\n// Driver code\nint main() {\n    int n = 6;\n    vector<vector<int>> connections = {{0,1},{0,2},{0,3},{1,4}};\n    Solution sol;\n    cout << sol.makeConnected(n, connections) << endl;\n    return 0;\n}",
        "java": "import java.util.*;\n\nclass Solution {\n    // Find parent with path compression\n    private int find(int[] parent, int x) {\n        if (parent[x] != x)\n            parent[x] = find(parent, parent[x]);\n        return parent[x];\n    }\n\n    // Union by rank\n    private void union(int[] parent, int[] rank, int x, int y) {\n        int px = find(parent, x);\n        int py = find(parent, y);\n        if (px == py) return;\n\n        if (rank[px] < rank[py]) parent[px] = py;\n        else if (rank[px] > rank[py]) parent[py] = px;\n        else {\n            parent[py] = px;\n            rank[px]++;\n        }\n    }\n    \n    // Function to find minimum operations to make the graph connected\n    public int makeConnected(int n, int[][] connections) {\n        // If not enough edges, return -1\n        if (connections.length < n - 1) return -1;\n\n        int[] parent = new int[n];\n        int[] rank = new int[n];\n\n        // Initialize parent\n        for (int i = 0; i < n; i++) parent[i] = i;\n\n        // Union all edges\n        for (int[] edge : connections)\n            union(parent, rank, edge[0], edge[1]);\n\n        // Count unique parents\n        Set<Integer> components = new HashSet<>();\n        for (int i = 0; i < n; i++)\n            components.add(find(parent, i));\n\n        return components.size() - 1;\n    }\n}\n\n// Main class\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[][] connections = {{0,1},{0,2},{0,3},{1,4}};\n        int n = 6;\n        System.out.println(sol.makeConnected(n, connections));\n    }\n}",
        "python": "class DSU:\n    def __init__(self, n):\n        # Initialize parent and rank\n        self.parent = [i for i in range(n)]\n        self.rank = [0] * n\n\n    # Find with path compression\n    def find(self, x):\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])\n        return self.parent[x]\n\n    # Union by rank\n    def union(self, x, y):\n        px = self.find(x)\n        py = self.find(y)\n        if px == py:\n            return\n        if self.rank[px] < self.rank[py]:\n            self.parent[px] = py\n        elif self.rank[px] > self.rank[py]:\n            self.parent[py] = px\n        else:\n            self.parent[py] = px\n            self.rank[px] += 1\n\nclass Solution:\n    # Function to find minimum operations to make the graph connected\n    def makeConnected(self, n, connections):\n        # If not enough edges\n        if len(connections) < n - 1:\n            return -1\n\n        dsu = DSU(n)\n\n        # Union edges\n        for u, v in connections:\n            dsu.union(u, v)\n\n        # Count unique components\n        components = set()\n        for i in range(n):\n            components.add(dsu.find(i))\n\n        return len(components) - 1\n\n# Driver code\nn = 6\nconnections = [[0,1],[0,2],[0,3],[1,4]]\nsol = Solution()\nprint(sol.makeConnected(n, connections))",
        "javascript": "class DSU {\n    constructor(n) {\n        this.parent = Array(n).fill(0).map((_, i) => i);\n        this.rank = Array(n).fill(0);\n    }\n\n    // Find with path compression\n    find(x) {\n        if (this.parent[x] !== x)\n            this.parent[x] = this.find(this.parent[x]);\n        return this.parent[x];\n    }\n\n    // Union by rank\n    union(x, y) {\n        let px = this.find(x);\n        let py = this.find(y);\n        if (px === py) return;\n\n        if (this.rank[px] < this.rank[py]) this.parent[px] = py;\n        else if (this.rank[px] > this.rank[py]) this.parent[py] = px;\n        else {\n            this.parent[py] = px;\n            this.rank[px]++;\n        }\n    }\n}\n\nclass Solution {\n    // Function to find minimum operations to make the graph connected\n    makeConnected(n, connections) {\n        // If not enough edges\n        if (connections.length < n - 1) return -1;\n\n        const dsu = new DSU(n);\n\n        // Union all edges\n        for (let [u, v] of connections) {\n            dsu.union(u, v);\n        }\n\n        // Count unique components\n        const components = new Set();\n        for (let i = 0; i < n; i++) {\n            components.add(dsu.find(i));\n        }\n\n        return components.size - 1;\n    }\n}\n\n// Driver code\nconst n = 6;\nconst connections = [[0,1],[0,2],[0,3],[1,4]];\nconst sol = new Solution();\nconsole.log(sol.makeConnected(n, connections));"
      }
    }
  ],
  "scraped_at": "2026-09-14T07:24:21Z"
}